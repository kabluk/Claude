---
description: Everything in /analyze plus product architecture, schema, URL structure, and a 30-day plan
argument-hint: [niche]
---

Use the **niche-finder** subagent to run its `/deepdive` workflow for the niche: $ARGUMENTS

Follow the agent's full evaluation framework and output rules (see `.claude/agents/niche-finder.md`). Return everything from `/analyze` plus: product architecture, tech stack (Next.js, Supabase, Tailwind, Algolia), database schema, Schema.org JSON-LD, URL structure, internal linking, content generation plan, indexing strategy, launch roadmap, and a 30-day execution plan.
