---
description: Sweep 50–100 potential directory niches and return only the top opportunities
---

Use the **niche-finder** subagent to run its `/discover` workflow.

Follow the agent's full evaluation framework and output rules (see `.claude/agents/niche-finder.md`). Sweep 50–100 potential niches across the hidden-opportunity sectors and combination patterns, filter automatically with the reject rules, and return only the top opportunities, each with a score and verdict.

Optional focus area: $ARGUMENTS
