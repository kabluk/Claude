---
description: Generate 20–50 derivative micro-niches from a base niche
argument-hint: [niche]
---

Use the **niche-finder** subagent to run its `/expand` workflow for the niche: $ARGUMENTS

Follow the agent's full evaluation framework and output rules (see `.claude/agents/niche-finder.md`). Generate 20–50 derivative micro-niches using the combination patterns (Location + Service, Software + Industry, Certification + Country, etc.), scoring the most promising ones.
