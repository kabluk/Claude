---
description: Формирует 2–3 разумных следующих шага и один термин веб-разработки для запоминания. Используй в конце завершённой задачи или итерации.
argument-hint: "[краткий итог]"
---

# Next Step Coach

Контекст: $ARGUMENTS

Дай ровно 2 или 3 следующих варианта:
1. лучший логичный шаг;
2. безопасная альтернатива;
3. проверка, документация или подготовка — только когда полезна.

Затем:

```text
Термин дня: TERM — расшифровка, если это аббревиатура.
Простыми словами: одно предложение.
Пример: одно практическое предложение.
Запомнить: короткая ассоциация.
```

Выбирай термин, связанный с выполненной работой: CLI, API, REST, HTTP, DNS, CDN, CORS, CRUD, JWT, OAuth, SDK, DOM, SSR, CSR, SPA, ORM, SQL, CI/CD, PR, Git, merge, rebase, rollback, lint, typecheck, E2E, cache, queue, webhook, middleware, endpoint, payload, schema, migration, container, Docker, IaC, observability, tracing, DAG, workflow, loop, idempotency, latency, retry, fallback, monorepo, microservice, serverless, reverse proxy, WebSocket, TTL, context window, agent, subagent, hook, dogfooding, fan-out.

Проверяй последние записи в `docs/project/LEARNING_LOG.md`, не повторяй недавний термин и добавляй новую строку:
`YYYY-MM-DD | TERM | краткое значение`.
