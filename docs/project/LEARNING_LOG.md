# LEARNING_LOG

2026-08-05 | DAG | направленный ациклический граф — форма графа задач в GRAPH.yaml: узлы-задачи, рёбра-зависимости, циклы запрещены
2026-08-05 | webhook | обратный HTTP-вызов: внешний сервис сам стучится в наш URL при событии (Stripe -> Worker при оплате)
2026-08-05 | context window | объём токенов, который модель видит за раз; HANDOFF.md существует, чтобы не зависеть от истории чата
2026-08-05 | lockfile | package-lock.json фиксирует точные версии зависимостей; без него `npm ci` в CI не может работать детерминированно
2026-08-05 | fan-out | распараллелить одну большую задачу на N независимых субагентов (5 агентов по ~11 сайтов вместо одного по 53) и слить результаты
2026-08-05 | idempotency | повторный вызов даёт тот же результат без побочных эффектов; GET /api/scan/:id идемпотентен, POST /api/scan — нет (создаёт новую запись каждый раз)
2026-08-05 | dogfooding | использовать свой же продукт на себе; сайт про аудит доступности обязан сам проходить axe-core, иначе доверие рушится в один клик
2026-08-05 | hook | скрипт, который харнесс автоматически запускает на событие (UserPromptSubmit, PostToolUse...); Model Advisor советует модель до начала работы над каждым промптом
2026-08-06 | false negative | проверка, которая молча говорит "всё чисто", хотя проблема есть; наш audit-a11y открывал страницы через file://, CSS не грузился, контраст тривиально "проходил" на неокрашенном HTML
2026-08-06 | graceful degradation | продукт остаётся рабочим при отсутствии необязательной части; без VITE_TURNSTILE_SITE_KEY форма сканера просто не показывает виджет, а не падает
2026-08-06 | abstention | модель, обученная явно говорить "не знаю" вместо угадывания; наш промпт /api/explain просит вернуть пустой explanation для незнакомого ruleId, а не выдумывать объяснение
2026-08-06 | edge case | вход на границе диапазона (0 находок, 40 повторов одной находки) — где чаще всего живут баги; именно такие сценарии поймали и дыру в контрасте (D-014), и слишком широкую вилку цены (D-017)
2026-08-06 | soft filter | сужает выдачу только когда после сужения кто-то остался (или просто поднимает совпадения наверх сортировкой), в отличие от жёсткого AND-фильтра, который может обнулить список; matchAgencies.ts применяет это к standard/priceBand — оба поля заполнены не у всех агентств, жёсткий фильтр почти всегда возвращал бы пусто
2026-08-06 | data retention | политика о том, сколько хранить данные и когда их удалять; у таблицы scans в D1 такой политики фактически нет (записи бессрочны) — обновлённая Privacy честно называет это открытым техдолгом, а не делает вид, что вопрос уже решён, раз он упоминался в RISKS.md
2026-08-06 | cron trigger | обработчик, который платформа вызывает сама по расписанию (не в ответ на HTTP-запрос) — worker/index.js теперь экспортирует scheduled() рядом с fetch(), Cloudflare дёргает его ежедневно по cron из wrangler.jsonc для удаления просроченных сканов
2026-08-06 | fixture drift | синтетическая тестовая фикстура незаметно перестаёт отражать поведение реальной системы (модели, внешнего API) — все фикстуры parseExplainResponse были чистым JSON, а Claude Haiku на деле оборачивает ответ в markdown code fence; юнит-тесты были зелёными, а прод-путь падал в 502 на каждый cache-miss, пока живой прогон с реальным ключом это не вскрыл
2026-08-06 | eventual consistency | изменение (новый секрет, свежий деплой) не применяется мгновенно на всех узлах распределённой системы; сразу после `wrangler secret put` часть запросов ещё получала 503, пока секрет не пропагировался по всем edge-узлам Cloudflare — решение: retry с паузой, а не паника на первый провал
2026-08-06 | additive migration | новая миграция только добавляет (CREATE TABLE/INDEX), не трогает существующие таблицы — `0003_leads.sql` независим от `scans`, поэтому применяется поверх 0001/0002 без риска сломать уже работающий A1-SCAN в проде
2026-08-06 | test coverage gap | постоянный CI-гейт технически зелёный, но физически не смотрит на новый код — `audit-own-a11y.mjs` имеет захардкоженный список страниц, и добавление маршрута `/request-quote` само по себе не попадает под проверку; зелёный `npm run audit-a11y` после A2-LEAD-FORM ничего не доказывал про новую страницу, пока не прогнан отдельный разовый axe-прогон именно по ней — и тот сразу нашёл реальный heading-order баг
2026-08-06 | least privilege | одобрение/доступ выдаётся ровно на то, что нужно конкретной задаче — не шире и не «про запас на будущее»; Фаза 2 разбита так, что `approval_required: true` стоит только на узлах с НОВЫМ внешним сервисом (Resend/Stripe) или реальной рассылкой, а не на всей фазе целиком, и одобрение A1-SCAN на CF-ресурсы явно не наследуется новыми узлами (D-022)
2026-08-06 | orphan page | страница технически живая (собирается, индексируется, проходит все проверки), но ни одна ссылка в интерфейсе на неё не ведёт — только прямой URL; `/request-quote/` была именно такой, пока не добавлен CTA на `/report/:id`, — «работает» и «достижимо» две разные проверки, зелёный audit-a11y ни одну из них не гарантирует за другую
2026-08-06 | import attributes | синтаксис `import data from './x.json' with { type: 'json' }` — явно указывает движку модулей, каким загрузчиком читать файл; понадобился в worker/lib/matchAgenciesServer.js, потому что plain-ESM Cloudflare Worker не бандлится Vite (не видит @data/a11y/* алиас) — та же строка кода одинаково валидна и под node --test (Node 22), и под esbuild-бандлер wrangler, что и позволило переиспользовать один файл для юнит-тестов и живого /api/lead
2026-08-06 | email obfuscation | приём против спам-ботов: адрес на странице не пишут как чистый текст `mailto:`, а прячут — Cloudflare-защита кодирует его в hex/XOR-строку (`/cdn-cgi/l/email-protection#...`), которую браузер расшифровывает на лету, или сайт просто пишет адрес словами («info собака domain точка nl»); при исследовании 96 агентств для outreach часть email пришлось декодировать детерминированно (не угадывать) именно поэтому
2026-08-06 | constraint as business rule | ограничение на уровне БД (PRIMARY KEY/UNIQUE) вместо проверки в коде приложения — `featured.agency_slug PRIMARY KEY` гарантирует ровно одно активное размещение на агентство даже если приложение когда-нибудь забудет проверить; подтверждено не чтением DDL, а живым повторным INSERT, который реально упал на SQLITE_CONSTRAINT_PRIMARYKEY
2026-08-06 | upsert | одна атомарная операция «вставить или обновить, если уже есть» (`INSERT ... ON CONFLICT DO UPDATE`) вместо ручного select-затем-insert-или-update из кода — worker/routes/stripeHook.js апсертит `featured` по `agency_slug`: первая оплата создаёт строку, продление (второе `checkout.session.completed`) обновляет ту же строку атомарно в БД, без гонки между двумя параллельными вебхуками одной подписки
2026-08-06 | retry | отправитель события (Stripe) сам повторяет доставку, если не получил быстрый успешный ответ (2xx) — поэтому worker/routes/stripeHook.js отвечает 200 на любое событие с верной подписью, даже необрабатываемого типа или без нужных metadata: не-2xx там означало бы, что Stripe годами долбит вебхук повторами о том, что мы осознанно не обрабатываем
2026-08-06 | bearer token | секрет, дающий право действия любому, кто им обладает — без проверки, откуда он взялся; ловушка в A2-CLAIM-API: если verify-токен для email-ссылки совпадал бы с claimId, который API и так возвращает вызывающему в ответе 201, обладание ответом API само по себе стало бы «доказательством владения почтой» — решение (D-023) развело их на два разных значения, только claimId уходит в ответ, token остаётся в D1 до перехода по ссылке
2026-08-06 | sandbox domain | бесплатный/тестовый режим транзакционного email-сервиса (Resend, SendGrid и т.п.) с общим доменом отправителя — реально доставляет письма только на email владельца аккаунта, любому другому получателю отдаёт ошибку валидации, пока не верифицирован собственный домен; проверено безопасным пробником на IANA-зарезервированный test@example.com (Resend честно ответил 422), а не реальной тестовой отправкой живому человеку
2026-08-07 | single source of truth | одно место, которому доверяют при расхождении, а не N синхронизируемых вручную копий факта — `G-HEADCOUNT` и `G-CERT-UNKNOWN`/`G-CERT-FRAGILE` были фактически закрыты в `DECISIONS.md` (D-048, D-043, D-044), но `BACKLOG.md` — отдельная ручная таблица статусов — не был обновлён и продолжал показывать `todo`; урок не «`BACKLOG.md` врал», а что два файла, описывающих одно и то же состояние без единого источника, расходятся молча, и расхождение находится только целевой сверкой, не по умолчанию
2026-08-06 | organic coverage | охват фичи растёт сам по себе по мере накопления данных, а не сразу на 100% — A2-LEAD-EMAIL уведомляет только claimed-агентства, на старте таких 0, так что часть лидов не приведёт ни к одному письму; это не баг и не недоделка, а прямое следствие честного решения не выдумывать email там, где его нет (D-025)
2026-08-06 | approval outlives its trigger | одобрение владельца на конкретное действие не протухает само по себе, если действие временно физически невозможно — A2-CLAIM-REBUILD одобрен «в проде», но подключать его некуда, пока не существует A0-DEPLOY; approval остаётся в силе на будущее (D-026), спрашивать заново не нужно, когда прод-цель появится — это не то же самое, что approval, который «не переносится на новые платные ресурсы автоматически» (там разные ресурсы, здесь один и тот же, просто ещё не готов принять результат)
2026-08-06 | server-authoritative timestamp | дата/время, которую вычисляет сервер в момент обработки события, а не читает из данных, присланных клиентом или третьей стороной — A2-STRIPE-LIVE (D-027) намеренно не берёт `until` (дата окончания featured) ни из Stripe custom field, ни из чего-либо ещё, присланного покупателем: такое поле в принципе редактируемо на стороне клиента (случайно или намеренно), поэтому `computeFeaturedUntil` считает «сегодня + 365 дней» сам, в момент когда воркер обрабатывает подтверждённый по подписи webhook
2026-08-06 | display value vs. identifier drift | видимый пользователю текст поля (label) и его программный идентификатор (key) в UI-конструкторах часто задаются раздельно и не синхронизируются при редактировании — Stripe генерирует `key` custom field из текста `label` только один раз, при первом сохранении, и не пересчитывает его при последующем переименовании label; A2-STRIPE-LIVE (D-028) сначала предположил `CUSTOM_FIELD_KEY = 'agency_slug'` по видимому в Dashboard тексту, реальный оплаченный платёж вскрыл настоящий key — `yourslugaccessatlas`; урок: сверяться с реальным payload, а не с тем, что показывает интерфейс конструктора
2026-08-06 | subset of a criterion | честнее закрыть проверяемую ЧАСТЬ требования, чем заявить его целиком или не брать вовсе: «заголовок описывает тему раздела» (9.2.4.6) автоматизации не поддаётся — это смысл, — но пустой заголовок не описывает ничего по определению, и это ловится однозначно, без ложных срабатываний; в карте покрытия такой критерий помечен покрытым, а в тексте находки прямо сказано, какая именно часть проверена (D-039) — иначе «покрыто» превратилось бы в обещание, которого проверка не выполняет
2026-08-06 | derived copy | число в маркетинговом тексте, вычисленное из тех же данных, что и сам продукт, вместо вписанного руками: главная берёт «29 из 50» из coverageSummary, поэтому после следующего роста покрытия обновится сама — вписанная константа врала бы молча и обнаружилась бы только когда кто-то заметит расхождение с /methodology (D-038); тот же приём, что уже применён к числу агентств и стран в заголовке главной
2026-08-06 | limits as a feature | честно показанная граница возможностей продаёт лучше завышенной цифры: страница /methodology одинаково заметно говорит и «29 из 50 проверяем автоматически», и «21 требует человека, потому что зависит от смысла и контекста» — второе не признание слабости, а точный ответ на вопрос «зачем мне агентство», то есть прямой мост в каталог, ради которого каталог и существует (D-037); попутно оба гейта (sitemap, a11y-аудит) имеют СВОИ захардкоженные списки страниц, и новая страница выпала из обоих при полностью зелёной сборке
2026-08-06 | cross-page check | часть требований доступности в принципе не проверяется по одной странице — консистентность навигации (9.3.2.3), одинаковые названия одной функции (9.3.2.4), наличие более чем одного способа найти страницу (9.2.4.5) существуют только в СРАВНЕНИИ страниц между собой; мы обходили 6 страниц с самого начала, но смотрели каждую изолированно, поэтому эти критерии не покрывал ни axe, ни наши DOM-проверки — выигрыш 3 критерия сразу (D-036) без единого нового обхода, только за счёт того, что уже собрано
2026-08-06 | harmonised standard as a single source | когда требования гармонизированы, знать нужно ОДИН нормативный документ, а не N национальных: EN 301 549 един на весь ЕС, поэтому карта покрытия строится один раз и действует для всех 13 юрисдикций — 13 законов различаются санкциями и областью применения, то есть ровно тем, что мы решили не показывать (D-035); практический эффект — 4.5 КБ JSON с 50 критериями вместо гигабайтов законов на 8 языках, и покрытие (26/50) считается скриптом из живых метаданных axe.getRules(), а не переписывается руками при каждом обновлении
2026-08-06 | right number, wrong regime | цифра может быть буквально верной по тексту закона и всё равно вводить в заблуждение, если взята из ПАРАЛЛЕЛЬНОГО режима: «Франция — €50 000» реально записана в loi 2005-102 ст. 47-1 (проверено по Légifrance), но ст. 47 покрывает госорганы и частные компании только от €250 млн оборота (décret 2019-768) — типовой интернет-магазин под неё не подпадает, а частный сектор по EAA идёт отдельной веткой в Code de la consommation; проверять надо не только величину санкции, но и ОБЛАСТЬ ПРИМЕНЕНИЯ, иначе сверка с первоисточником даёт ложную уверенность (D-034)
2026-08-06 | harmonised standard vs. decentralised enforcement | ЕС может унифицировать ТРЕБОВАНИЕ, оставив ПРИНУЖДЕНИЕ национальным: EAA (Directive (EU) 2019/882) вводит единый EN 301 549 для всех, но механизма «одного окна» как в GDPR ст. 56 не даёт — по ст. 21/23 каждая страна назначает свои органы и сама проверяет услуги на своей территории, потому что директива превращается в 27 национальных законов, а не действует напрямую; практический вывод для продукта: сканировать под разные страны бессмысленно (находки идентичны), различается только правовая обёртка — что убило изначально задуманную мульти-юрисдикционную фичу и свело её к одной строке (D-033)
2026-08-06 | end-to-end reachability | «поле вычисляется и возвращается API» и «пользователь его видит» — две разные проверки, и первая ничего не гарантирует про вторую: jurisdictionNote считался воркером, писался в D1 и приходил в ответе с самого D-030, но фронтенд-тип ScanFinding про него не знал, а ReportPage не рендерил — две полные итерации работы (D-030, D-031) не доходили до глаз пользователя ни разу, при том что все worker-тесты были зелёными; родственник orphan page, но по слою: там страница была недостижима по ссылке, здесь данные обрывались на границе backend->frontend
2026-08-06 | primary source vs. aggregator | закон транспозиции директивы (например EAA) существует один раз — на официальном правовом портале страны (RIS/Finlex/Lovdata/retsinformation.dk); блоги compliance-вендоров пересказывают его с разной точностью и общим мотивом напугать точнее продать услугу — при расширении A3-JURISDICTION на 8 новых стран (D-031) каждый закон процитирован с официального источника, а суммы штрафов из вторичных источников намеренно НЕ включены в код (verified:false) именно потому что не сверены с текстом закона напрямую
2026-08-06 | compliance gap vs. conformance gap | «нарушение стандарта» и «то, за что реально штрафуют» — разные множества, и второе часто проще проверить, чем первое: немецкий надзор (MLBF) начинает не с WCAG, а с наличия заявления о доступности (Anlage 3 §14 BFSG) — бинарная проверка без ложных срабатываний, тогда как WCAG-соответствие автоматом ловится лишь на ~57% (axe-core); наш сканер измерял только второе и потому выдавал 100/100 сайту без заявления, то есть максимальную оценку тому, кто первым попадёт под проверку
2026-08-06 | forgiving input matching | принимать несколько эквивалентных форм пользовательского ввода вместо одной строгой (технической), когда сама форма поля намекает на менее строгий ввод — переименовав label custom field с "slug" на человекочитаемое "Your agency name" (D-029), пришлось научить `resolveAgencySlug` матчить и по названию агентства (`agencies[].name`), не только по точному slug, иначе смена формулировки поля молча сломала бы реальные платежи; ключевая деталь — неоднозначное совпадение (два агентства с одинаковым нормализованным именем) намеренно не резолвится вообще, а не берёт первое попавшееся: снисходительность к формату ввода не должна означать угадывание при неоднозначности
2026-08-06 | TOCTOU (time-of-check to time-of-use) | состояние читается в один момент, а используется/проверяется в другой — между ними оно могло измениться, и код молча работает с уже неактуальным значением; A3-KEYBOARD проверял «виден ли фокус» отдельным `page.evaluate()` ПОСЛЕ выхода из цикла Tab-нажатий — к этому моменту фокус на короткой странице уже мог уйти на `<body>` (после последней ссылки), и страница с реальным видимым `outline` ложно флагалась «невидимый фокус»; исправление — читать селектор и видимость атомарно, за один `evaluate()`, пока элемент точно ещё в фокусе
2026-08-06 | guard clause that eats the payload | ранний выход, поставленный ради одной цели (не бампить impact дважды), унёс с собой вторую, склеенную с ней в том же блоке (простановку правовой пометки): `if (f.impact === 'critical') return f` выглядел безобидно, но axe.js отдаёт `a11y-statement-missing` СРАЗУ как critical — и потому находка «заявления нет вообще» не получала ссылки на закон никогда, а получала только более слабая «заявление неполное»; 200 зелёных тестов молчали, потому что фикстуры подавали impact:'serious' — форму, которой в проде не существует (fixture drift, третий случай); правило: если в одной ветке делаются ДВЕ вещи, ранний выход отменяет обе — разделяй условие по числу эффектов, а тест пиши на ту форму данных, которую реально эмитит соседний модуль, и закрепляй это отдельной проверкой его исходника (D-040)
2026-08-06 | assembling beats producing | связать существующие активы в один путь дешевле и ценнее, чем произвести новые: немецкий рынок получил рабочий вход `/bfsg-check/` без единой новой записи данных — 18 прюферов, две статьи, проверенная правовая ссылка, сканер и карта покрытия уже лежали в проекте, но нигде не сходились в одну цепочку «скан → чего не хватает → на каком основании → к кому идти»; актив, до которого нельзя дойти по ссылке, экономически равен отсутствующему (родственник orphan page, но на уровне пути пользователя, а не отдельной страницы) — и именно при сборке цепочки обнаружилось, что её главное звено (D-040) вообще не работает
2026-08-06 | decisions decay in content | принятое решение применяют к коду и забывают про контент: D-035 убрал суммы штрафов «как класс», добавил два инварианта в тесты — и всё равно суммы остались жить в немецком гайде, включая FAQPage-разметку в выдаче, потому что инварианты сторожили `jurisdictionNote`, а не markdown; правило: запрет, сформулированный как «нигде», обязан получить проверку по ВСЕМ поверхностям вывода (код, контент, структурированные данные), иначе он действует только там, где о нём вспомнили в день принятия
2026-08-06 | self-attestation loop | доказательство, которое хостит тот же, о ком оно говорит, доказывает только само себя: у 9 записей бейджа «нас назвал внешний заказчик» evidenceUrl вёл на домен самого агентства (включая `200-ok`, чей «сторонний» портал toegankelijkheidsonderzoek.nl оказался его же — проверка по домену сайта такого не ловит, нужен взгляд на владельца ресурса); лечится не удалением записи, а подъёмом до первоисточника — в NL это запись заказчика в государственном реестре toegankelijkheidsverklaring.nl, которая ссылается на отчёт аудитора, а не наоборот (D-042); правило переносимое: у любого «нас признали» доказательства спрашивай, КТО его опубликовал, а не что в нём написано
2026-08-06 | name as a promise | идентификатор в схеме — тоже публичное утверждение, и он живёт дольше подписи в UI: `gov-declared-auditor` продолжал обещать «government» ещё целую итерацию после того, как видимую подпись привели к доказуемому (D-041), потому что переименование `kind` требует изменения схемы и потому откладывается; когда данные расходятся с именем поля, чинить надо имя, а не только то, что видит пользователь — иначе следующий разработчик поверит имени (D-042)
2026-08-06 | indirect evidence | ссылка может подтверждать факт по касательной — через соседний объект, а не про сам предмет: доказательством для UXMen служила декларация площадки, где внешнего аудита как раз НЕ было (была самооценка), а UXMen аудировала родительский сайт; факт верен, но источник подтверждает его через третье лицо — заменён на собственную декларацию vvsg.be, где аудитор назван прямо (D-043); проверять надо не только «упомянут ли», но и «про этот ли объект документ»
2026-08-06 | more durable ≠ more correct | замена ссылки на «канонически надёжную» без проверки содержимого портит данные тише, чем битая ссылка: у akademia-slonca CDN-ссылка выглядела хрупкой, а официальный BIP театра — правильным источником, но BIP называл ДРУГОГО аудитора (другой аудит другого ресурса) — подстановка приписала бы агентству чужую работу; сначала проверяем, что документ про тот же предмет и то же лицо, и только потом — насколько устойчив его адрес (D-044)
2026-08-06 | published price ≠ price of the thing | «на сайте есть цифры» и «агентство опубликовало цену своей услуги» — разные факты: из 92 сайтов с суммами в тексте ценой аудита оказались 17, остальное — штрафы, уставный капитал, подписки, обучение, ставки за час/страницу, цены на веб-разработку и рыночные ориентиры в собственном блоге («a competent audit costs $10,000–$25,000» на домене агентства — комментарий, а не прайс); прежде чем записать число в поле, проверяй не только источник, но и ПРЕДМЕТ и ЕДИНИЦУ измерения (D-045)
2026-08-06 | calibration drift | пороги, откалиброванные под один набор входов, молча врут, когда набор расширяется: пороги оценки стоимости (2/5/10) ставились, когда сканер отдавал только axe-правила, а после +15 собственных типов находок тот же сайт поехал с «€10–30k» на «€30k+» — цена росла от нашей добросовестности, а не от состояния сайта; тесты на формулу этого не ловят в принципе, ловит только тест на ИСХОД для реалистичного входа (D-046)
2026-08-06 | self-published ≠ self-consistent | «компания публикует это сама» звучит как гарантия достоверности, но одна и та же компания публикует РАЗНЫЕ версии факта в разных местах: из 24 агентств, у которых год основания есть и на своём сайте, и в поле «Основана» LinkedIn, цифры разошлись у 6 (25%) — и в пяти случаях права была страница агентства, а не профиль; вывод не «LinkedIn врёт», а «источник надо оценивать замером, а не репутацией»: прежде чем принять целый класс источников, посчитай, как часто он расходится с тем классом, которому ты уже доверяешь, — там, где перепроверить нельзя, ошибка будет ровно такой же доли, только невидимой (D-047)
2026-08-06 | orphan field | поле может годами лежать в схеме, собираться и валидироваться, ни разу не дойдя до пользователя: `founded` был в `types.ts`, в 31 записи и в enrich-скрипте, но не рендерился ни в профиле, ни в карточке, ни в индексе — заполнять его имело смысл только вместе с местом, где он виден; родственник orphan page, но на уровне поля данных, и ловится одной командой (`grep` по `src/`+`worker/`) ДО того, как потрачен день на сбор (D-047)
2026-08-06 | orphan field | поле в схеме, которое никуда не выводится и ничем не проверяется — не «задел на будущее», а пассив: `headcountBand` лежал у 19 записей без источников, не рендерился нигде, а его шкала оказалась таксономией LinkedIn, уже отвергнутого как источник; правильный порядок — сначала решить, какое решение пользователя поле обслуживает, и только потом собирать (D-048); родственник orphan page, но в данных, а не в навигации
2026-08-06 | fluency bias in copy review | вычитка носителем улучшает язык и одновременно тихо усиливает утверждения: из двух присланных версий немецкого текста `/bfsg-check/` более гладкая (V2) заменила модальное «ist zuständig und kann stichprobenartig vorgehen» на констатирующее «führen stichprobenartige Kontrollen durch», добавила «rechtssicher» и унифицировала термин так, что подпись ссылки стала неверной — ни одна правка не была ошибкой ЯЗЫКА, все три были ошибками ФАКТА, потому что естественнее звучит то, что звучит увереннее; принимать вычитку надо построчно, сверяя каждую правку с кодом и с собственными инвариантами (D-035), а не версией целиком — и ровно этот построчный проход поймал, что обе версии врут про `.com`: сканер там не «промахивается», а возвращает `country: 'unknown'` (D-049)
2026-08-06 | verifier strength as the primary lever | качество агентной системы упирается не в модель и не в промпт, а в силу проверки: слабый verify не даёт слабый результат — он даёт УВЕРЕННЫЙ МУСОР, который проходит по всем формальным признакам готовности, и потому дефект попадает не в баг-трекер, а в прод как «сделано»; практический критерий из этого — проверка, ни разу не провалившаяся, не доказана: прежде чем закрывать узел, посади нарушение, которое verify обязан ловить, убедись, что он краснеет, и откати (ровно этого не хватило в D-040, где 200 зелёных тестов пропустили сломанную главную цепочку, потому что фикстуры подавали форму данных, которой в проде не существует); парный принцип — не каждый узел требует LLM: скрипт или тест не только дешевле и воспроизводимее, но и не отчитывается о собственном успехе, а значит его отчёт не нужно перепроверять (D-050)
2026-08-06 | explicit prohibition ≠ enforced boundary | инструкция «не редактируй файлы, только исследуй», данная параллельным субагентам с полным набором инструментов, не сработала как ограничение: 4 из 5 всё равно отредактировали `LEARNING_LOG.md`, вписав туда собственные термины — не потому что задача того требовала, а потому что у агента был доступный Edit и привычка «закрывать» задачу записью в проектную память; текстовый запрет в промпте не то же самое, что отсутствие инструмента (сравнить с тем, как `Explore`-агент физически не может писать файлы — это ограничение, а не просьба). Практическое следствие: после любого делегированного пакета `git diff` проверяется целиком, а не только по ожидаемым файлам — иначе неожиданная правка остаётся незамеченной ровно там, где её не искали (родственник verifier strength, D-050, но здесь под проверку должна попасть ОБЛАСТЬ действия агента, а не только качество его результата)
2026-08-07 | open-world assumption | отсутствие доказательства — не доказательство отсутствия: в G-FOUNDED-LI (D-052) 5 из 10 параллельных пакетов сорвались на лимите API-сессии и НЕ ПРОВЕРЕНЫ вообще, а 5 успешных дали 59 отклонений (год реально не публикуется) — эти два множества нельзя слить в одну кучу «непроверено», иначе несделанная работа тихо превращается в отрицательный результат; статус UNREACHABLE (сайт недоступен) и статус «пакет не запустился» так же не равны NOT_FOUND — только явная проверка закрывает вопрос, её отсутствие оставляет его открытым
2026-08-07 | silent interrupt looks like success | процесс, прерванный посторонним событием в общем потоке (переключение модели, отклонённый tool-use где-то ещё), не сообщает об ошибке и не зависает — он просто останавливается, и внешний наблюдатель (`ListAgents`) видит его как отсутствующего, будто он тихо закончился штатно: в G-FOUNDED-LI (D-053) пять фоновых агентов дважды подряд дали ровно такую сигнатуру после постороннего interrupt, и оба раза «0 находок» легко читалось бы как «метод не сработал», если бы не прямая проверка последних записей транскрипта (`[Request interrupted by user]` вместо ответа); правило переносимое: отсутствие результата у фонового процесса — это вопрос «как он остановился», а не факт «он остановился безрезультатно», и ответ смотрят в логе, а не предполагают по статусу
2026-08-07 | hreflang | HTML-атрибут (`<link rel="alternate" hreflang="...">`), которым сайт подсказывает поисковику, какая языковая/региональная версия страницы соответствует какому языку — но он адресует РЕАЛЬНЫЕ канонические URL, поэтому бессмыслен без постоянного домена; при выборе между `G-I18N` (включает hreflang) и `G-GUIDES` для growth-итерации это стало решающим доводом — домен каталога всё ещё placeholder (`A0-ORIGIN` ждёт владельца), значит hreflang сейчас указывал бы сам на себя как заглушку и потребовал бы полной переделки при реальном деплое, тогда как контент (гайды) не зависит от домена вообще (D-056)
2026-08-07 | user-agent gating as a tool-specific false negative | HTTP 403/503 от сервера при живой перепроверке ссылки не всегда значит «ссылка битая» — государственные правовые порталы (irishstatutebook.ie, retsinformation.dk) молча блокируют конкретно User-Agent автоматизированного fetch-инструмента, а не сам ресурс: та же ссылка через `curl` с браузерным User-Agent вернула 200 и совпадающий текст у обеих; отличать это от РЕАЛЬНО мёртвой ссылки (404 у сконструированного deep-link на RIS для Австрии, воспроизвёлся и в WebFetch, и в curl одинаково) можно только повторной проверкой другим инструментом/UA, не по коду ошибки одного вызова — код 403 от одного клиента доказывает блокировку этого клиента, а не состояние ресурса (G-GUIDES-EAA-8, D-057)
2026-08-07 | a lesson without a gate recurs | D-035 удалил суммы штрафов «как класс» 2026-08-06, D-041 в тот же день нашла их в немецком гайде и убрала руками, а D-058 на следующий день нашла их СНОВА — в двух других файлах, написанных до самого решения и ни разу не сверенных задним числом; тот же дефект появился трижды не потому, что урок не был записан (он был, LEARNING_LOG 2026-08-06), а потому, что запись урока не превратилась в проверку — предостережение живёт в документе, который никто не обязан перечитывать перед каждой правкой. Третье повторение — сигнал остановиться и построить гейт (`scripts/no-fine-amounts.test.mjs`), а не в четвёртый раз почистить руками; практическое правило: если решение формулируется как «нигде и никогда», в тот же день, когда оно принято, стоит спросить не «где это есть сейчас», а «как это останется правдой, когда никто не будет об этом думать»
2026-08-07 | cargo culting | копирование формы решения без переноса сути, которая делала его рабочим — ритуал без причины; риск, который закрывал новый гайд `barrierefreiheitserklaerung-muster-checkliste.md` (`G-GUIDES-BFSG-MUSTER`): Muster-Formulierungen для Anlage 3 написаны с явными плейсхолдерами в квадратных скобках и с отдельным FAQ-пунктом «нельзя копировать буквально», потому что скопированный без замены плейсхолдеров шаблон выглядит как настоящая Barrierefreiheitserklärung (та же структура, тот же текст-обёртка), но не содержит ни одного из пяти требуемых фактов — форма правовой Erklärung без её содержания эту норму не выполняет и хуже отсутствия страницы, потому что маскируется под соответствие
2026-08-07 | convention over configuration | архитектурный принцип: система сама выводит поведение из места/имени файла по договорённости, вместо того чтобы требовать явную регистрацию каждого экземпляра в отдельном списке — в AccessAtlas `src/lib/guides.ts` глобит `data/a11y/guides/*.md`, поэтому четвёртый гайд BFSG-кластера (`G-GUIDES-BFSG-MUSTER`) получил маршрут, статик-путь и запись в sitemap автоматически, без единой ручной правки `routes.tsx`/`gen-a11y-sitemap.mjs`; это прямая противоположность паттерну, который дважды ломался в этом же проекте (D-037, `DE-PAGE-GATE`) — `gen-a11y-sitemap.mjs` и `audit-own-a11y.mjs` держат СВОИ захардкоженные списки страниц, и новая страница выпадала из обоих при полностью зелёных тестах; конвенция (глоб по каталогу) не требует гейта вообще, конфигурация (ручной список) требует гейт на каждый список отдельно — выбор архитектуры определяет, нужен ли вам защитный тест или нет
2026-08-07 | gap analysis | сопоставление целевого состояния (куда хотим) с фактическим (что уже есть) ПЕРЕД тем, как строить план — чтобы не переделывать сделанное и не пропустить реальную дельту; при принятии Design Constitution владельца (D-062) gap-анализ по всем 64 секциям показал, что ~70% принципов проект уже реализовал независимо (D-035, D-042, A1-EXPLAIN, R1...), и «замена архитектуры» свелась к 8 конкретным дельтам — без анализа тот же документ легко прочитался бы как мандат на переписывание всего с нуля; правило: документ-видение сначала прикладывают к реальности построчно, и только разница становится задачами
2026-08-07 | design tokens | именованные переменные дизайн-решений (цвет, шрифт, радиус, отступ), у которых значение живёт в ОДНОМ месте, а смысл — в имени: `--color-critical` значит «текст критической находки», а не «#b91c1c где-то в разметке»; в Tailwind v4 объявляются в CSS через `@theme` и автоматически порождают утилиты. Практика итерации CN-TOKENS (D-063): severity-палитра сканера переехала из разбросанных по JSX классов в 18 токенов `src/styles.css`, и «critical красился акцентом бренда» стало видно ровно в момент, когда токенам пришлось дать имена — токен заставляет назвать НАЗНАЧЕНИЕ цвета, и несоответствие имени и использования вскрывает дизайн-баг, который в безымянных hex-классах жил незамеченным; запомнить: токен = имя решения, не сокращение для значения
2026-08-07 | prefers-reduced-motion | медиа-запрос CSS, сообщающий, что пользователь попросил систему сократить анимации (вестибулярные расстройства, укачивание от движения на экране) — уважающий его интерфейс снимает или заменяет несмысловое движение; в CN-SCAN-STREAM (D-064) вращение спиннера при `reduce` снимается полностью, а состояние шага остаётся различимым формой (кольцо/галка/крест/точка) и текстом — движение не имеет права быть единственным носителем смысла, как и цвет; проверка живая, не на веру: Playwright-контекст с reducedMotion:'reduce' подтвердил animation:none, а контрольный контекст без reduce — что анимация в принципе существует (иначе проверка фиктивна)
2026-08-07 | URL slug vs. UI label | адрес страницы (slug в URL) и её подпись в навигации — независимые слои: подпись можно переименовать мгновенно и бесплатно, а смена URL рвёт внешние ссылки и SEO-историю и требует 301-редиректов; поэтому IA-переименование «Guides → Knowledge» в AccessAtlas тронуло только ярлыки, оставив /guides/* нетронутым — прецедент, что label и slug не обязаны совпадать
2026-08-07 | programmatic SEO (pSEO) | генерация множества страниц из структурированных данных по одному шаблону; работает только когда каждая страница несёт уникальную фактуру из данных (у нас: точные axe-правила и модули на критерий), и превращается в спам (thin content, R1) когда фактуры нет — поэтому порог осмысленности (status !== none) отсекает 19 из 50 страниц ещё на этапе генерации, а не после санкций поисковика
2026-08-07 | write-behind progress / heartbeat pattern | долгая фоновая задача пишет промежуточное состояние в хранилище по ходу работы (у нас: updateScanProgress в D1 на каждой фазе), а клиент читает его обычным поллингом того же GET — прогресс переживает рестарт клиента и не требует websocket; два обязательных правила: запись гейтится статусом задачи (WHERE status=running — запоздавший heartbeat не оживит завершённую задачу), а финал перезаписывает промежуточное состояние атомарно с итогом
2026-08-08 | roving tabindex | приём доступной композитной группы (таблист, меню, тулбар): в Tab-последовательности участвует РОВНО ОДИН элемент группы (tabindex=0), остальные — tabindex=-1, а перемещение между ними отдано стрелкам, которые двигают И выбор, И фокус; так Tab выводит пользователя ИЗ группы к следующему блоку, а не заставляет его перебирать каждый таб. В CN-COMPONENTS (D-068) это ядро компонента Tabs — самая частая ошибка ручных реализаций ровно противоположна: либо каждый таб — отдельная остановка Tab (и Tab перебирает все), либо стрелки не работают вовсе; проверка живая, не по коду: Playwright подтвердил, что ArrowRight/Home/End меняют и aria-selected, и document.activeElement, а неактивные табы держат tabindex=-1; запомнить: в группе — одна дверь для Tab, стрелки водят внутри
2026-08-08 | focus trap | у модального диалога фокус на время открытия заперт внутри: Tab/Shift+Tab циклически ходят только по фокусируемым внутри диалога и не достигают страницы за ним, на открытии фокус входит в диалог, на закрытии — возвращается на элемент-триггер, Escape закрывает. Это то, что статическая разметка обещать не может (aria-modal="true" — лишь обещание ассистивной технике, а не механизм), поэтому в CN-COMPONENTS (D-068) ловушку проверяет живой Playwright: 6 нажатий Tab не выводят фокус за role=dialog, после Escape document.activeElement === кнопка-открывашка; попутный урок той же итерации — анимация входа диалога через opacity на контейнере делает панель полупрозрачной над тёмным скримом и роняет контраст текста до ~1.1 на 150 мс (реальный баг и для зрячих), поэтому вход — только transform, панель всегда непрозрачна
2026-08-08 | ARIA live region | область DOM, помеченная aria-live (или ролью, что её включает: role=status = polite, role=alert = assertive), содержимое которой ассистивная технология зачитывает при ИЗМЕНЕНИИ, не уводя туда фокус — это и отличает тост от диалога: пользователь остаётся там, где был, а сообщение приходит фоном. Ключевая ловушка, невидимая в статике и чаще всего ломающая ручные тосты: скринридер анонсирует только изменения региона, за которым УЖЕ наблюдает, — поэтому live-область должна присутствовать в DOM с первого кадра пустой, а внутрь вставляться лишь текст; если регион и первое сообщение монтируются вместе, самый первый тост «немой». В CN-COMPONENTS-REST (D-070) Toast держит две всегда-смонтированные области (alert для ошибок, status для успеха), фокус не уводит, а политика закрытия сама по себе a11y-решение (WCAG 2.2.1): ошибка живёт до ручного закрытия, polite авто-исчезает — авто-исчезающий тост не должен быть единственным местом важной информации; проверка живая, не по коду (Playwright: role верный, фокус на триггере, обе области в DOM до сообщения, reduced-motion→animation:none); запомнить: live-region — «слушатель, поставленный заранее», а не «микрофон, включённый в момент речи»
2026-08-08 | dataset as content moat | сильнейший защитный ров контента — не статьи, а СТРУКТУРИРОВАННЫЕ данные, которые есть только у тебя (§45): их нельзя переписать за вечер, они растут сами и порождают цитируемость. Но ров работает, только если анализируешь тот датасет, который у тебя РЕАЛЬНО есть и проверен, а не тот, который звучал бы солиднее: у AccessAtlas §23 подсказывал «Accessibility Index / Top-100 Ecommerce» — но корпуса сканов чужих сайтов нет (браузер песочницы не ходит во внешний HTTPS, D-010), и такой рейтинг потребовал бы выдуманных цифр (запрещено D-045/D-047). Честный уникальный датасет — сам каталог (245 проверенных записей), и первый отчёт (CN-RESEARCH, D-071) разбирает ЕГО: страны/стандарты/типы декларантов/долю с проверяемым доказательством, с явным блоком «What this is / is not», чтобы разбор своих данных не читался как бенчмарк чужих сайтов (§21 против oversell). Два переносимых правила: (1) числа такого отчёта считает скрипт-агрегатор из первичных данных на этапе сборки, а тест пересчитывает и падает на ручной правке/устаревании — иначе «данные» тихо расходятся с источником ровно как когда-то coverage, вписанное руками; (2) прежде чем строить data-продукт, спроси не «какой отчёт звучит авторитетнее», а «какие данные у меня есть, доказуемые построчно» — ров из выдуманных чисел не ров, а пассив
2026-08-08 | prefers-color-scheme | media-фича CSS: браузер сообщает системную тему пользователя (light/dark) — сайт переключает палитру переопределением одних и тех же CSS-переменных на :root, без JS и без класса; в AccessAtlas dark-тема D-072 живёт целиком на ней, а Playwright эмулирует её через emulateMedia({colorScheme}) — так dark-палитру можно гонять тем же axe-гейтом, что и light | Запомнить: тема — это не второй CSS, а вторые значения тех же токенов
2026-08-08 | reverting by deletion, not by flag | откатывая уже реализованную функцию, надёжнее УДАЛИТЬ её код, чем спрятать за флагом/классом-переключателем: убирая тёмную тему (D-073), мы вырезали блок `@media (prefers-color-scheme: dark)` целиком, а не оставили его выключенным — потому что спрятанная альтернативная ветка продолжает жить в кодовой базе и однажды включится сама (новая страница забыла подключить класс; кто-то счёл флаг мусором и «прибрал»; тема пришла из системной настройки, а не из нашего кода). Побочная выгода: удаление проверяемо — тест с `emulateMedia({colorScheme:'dark'})` показывает светлый фон, и он обязан покраснеть, если ветка вернётся, тогда как «выключенный флаг» проверить нечем; правило: у отката должно быть доказательство того же класса, что у внедрения
2026-08-08 | type as a design constraint | ограничение паттерна, записанное в ТИП, соблюдается само, а записанное в документацию — соблюдается по доброй воле: у Tooltip (D-074) `content` типизирован как `string`, а не `ReactNode`, потому что ARIA-роль `tooltip` не поддерживает интерактивное содержимое (кнопка/ссылка внутри подсказки недостижимы для скринридера, и это уже popover, другой паттерн) — при `ReactNode` предупреждение в pitfalls нарушалось бы первым же разработчиком, которому «нужна всего одна ссылочка», а `string` делает это невозможным на этапе компиляции; правило: если у паттерна есть граница, которую легко случайно перейти, ищи способ выразить её сигнатурой, а не абзацем
2026-08-08 | measure the thing, not its proxy | заполнение контекста легко «померить» размером файла транскрипта — и это врало бы в обе стороны: файл копит всю историю, включая свёрнутое авто-компактом и усечённые выводы инструментов, тогда как в контекст уходит совсем другое; настоящий источник лежал в том же файле — Claude Code пишет реальный `usage` от API на каждый ответ, и сумма input+cache_read+cache_creation последней записи это ровно то, сколько контекста ушло в последний запрос (D-076); правило: прежде чем строить метрику на доступном прокси, поищи, не записан ли где-то рядом сам факт — прокси удобен, но объяснять его расхождения с реальностью придётся вечно
2026-08-08 | landmark uniqueness | у ориентира (landmark: nav, main, aside…) должно быть УНИКАЛЬНОЕ на странице сочетание роли и доступного имени — иначе в списке ориентиров скринридера пользователь видит два одинаковых пункта «navigation: Breadcrumb» и не может выбрать нужный; ключевое следствие, ради которого запись и появилась: уникальность — свойство СТРАНИЦЫ, а не компонента, поэтому имя ориентира обязано быть параметром, а не зашитой константой. В D-077 крошки библиотеки несли `aria-label="Breadcrumb"` внутри реализации, а на странице примера site-layout уже рендерит собственный трейл с тем же именем — axe `landmark-unique` (moderate) поймал столкновение на первой же сборке; с константой конфликт был бы неустраним без правки самого компонента, с пропом (`label`, default `'Breadcrumb'`) страница просто называет свой экземпляр иначе. Обобщение за пределы ARIA: всё, что обязано быть уникальным в пределах документа (id, имя ориентира, заголовок landmark-региона), нельзя фиксировать внутри переиспользуемого компонента — компонент не знает, сколько его копий окажется на странице
2026-08-08 | virtual focus (aria-activedescendant) | паттерн, где DOM-фокус остаётся на одном родительском элементе (текстовом input), а «текущая позиция» пользователя в списке передаётся отдельным ARIA-атрибутом, не реальным `.focus()`; отличие от привычного roving tabindex (Tabs, Menu) в том, что элемент, на который «смотрит» скринридер, физически не фокусируется — это единственная конструкция ARIA 1.2 combobox, которая позволяет одновременно печатать в поле И листать список стрелками (реальный `.focus()` на опции немедленно ломает ввод текста, потому что клавиатурные события уходят на неё, а не в поле); правило: когда виджету нужно одновременно принимать ввод и предлагать навигацию по списку, ищи virtual focus раньше, чем roving tabindex — это разные паттерны для разных задач, не взаимозаменяемые варианты одного
2026-08-08 | roving tabindex | техника управления фокусом в композитных виджетах (меню, табы, тулбары): ровно один дочерний элемент несёт `tabIndex={0}` (следующая цель Tab), остальные — `tabIndex={-1}` (в таб-порядок не попадают, но остаются программно фокусируемыми), а стрелки двигают фокус между ними реальным `.focus()`, переставляя, какой элемент несёт `0`; в Menu button (D-079) это ровно тот механизм, что уже использовал Tabs, и он намеренно ПРОТИВОПОЛОЖЕН virtual focus у Combobox (D-078, `aria-activedescendant`) — там реальный фокус обязан остаться в текстовом поле, здесь ему, наоборот, естественно перемещаться по пунктам, которые сами по себе не редактируемый текст; правило: у списка кликабельных, но не editable, элементов — roving tabindex умолчание, virtual focus — обходной манёвр для случая, когда реальный фокус физически не может уйти с другого элемента
2026-08-08 | selection follows focus (or doesn't) | развилка дизайна для любого списка с курсором и выбором: значение может либо коммититься автоматически при каждом перемещении курсора («selection follows focus» — так устроен нативный `<select>` и radio-группы), либо оставаться отдельным состоянием, которое отдельно подтверждают явным действием (Enter/клик), а курсор просто «гуляет» по списку сам по себе; в ListboxSelect (D-080) выбрана вторая модель специально ради Escape — если бы выбор совпадал с фокусом, Escape пришлось бы эмулировать откат к «значению на момент открытия» через отдельный снэпшот, а с разведённым состоянием Escape — это просто «не трогать ничего», отдельный путь кода, а не более сложный частный случай отката; правило: прежде чем описывать поведение Escape/отмены в композитном виджете, сначала реши эту развилку — она определяет, легко или через костыль будет реализована отмена
2026-08-08 | extract, don't invent | когда библиотека компонентов добавляет образец для паттерна, уже реализованного где-то в проде (форма, навигация, что угодно), правильный источник правды — работающий код, а не спецификация с нуля: FormField (D-081) обобщает уже действующий на /request-quote/ локальный компонент Field из LeadForm.tsx, а не пишется заново по WAI-ARIA-описанию поля формы (тот же приём уже сработал у Breadcrumbs, D-077, извлечённых из Layout.tsx); выгода двойная — во-первых, паттерн уже прошёл проверку реальным использованием, а не только тестами примера; во-вторых, расхождение между «как показано в библиотеке» и «как на самом деле сделано на сайте» невозможно в принципе, потому что это буквально общий код или прямое обобщение общего кода, а не две параллельные реализации одной идеи; правило: прежде чем писать новый компонент библиотеки с нуля, поискать, не решена ли уже эта задача где-то в проде — и если да, извлекать оттуда, а не соревноваться с собственным работающим кодом
2026-08-08 | verify the verifier | независимая живая проверка — не одноразовая гарантия правоты: в этой же серии (Menu button, Select/listbox, Switch) собственный Playwright-скрипт трижды подряд давал ложный FAIL не из-за бага компонента, а из-за бага в самой проверке (сравнение текста с посторонним hint; CSS querySelector, matching по всему поддереву вместо прямых детей — дважды); во всех трёх случаях точечная проверка (`lastElementChild`, прямое сравнение конкретных span) подтвердила, что компонент вёл себя верно; правило: красный результат независимой проверки — сигнал разобраться, а не автоматически «баг найден» — разбор должен включать и код самой проверки, не только код, который она проверяет, иначе легко откатить рабочий компонент из-за неверного инструмента измерения
2026-08-08 | actionability checks | Playwright не выполняет .click() вслепую — перед кликом он ждёт, пока элемент пройдёт свой набор проверок «действительно ли по нему можно кликнуть»: видим, стабилен (не двигается), получает события указателя (не перекрыт другим элементом) и enabled — причём «enabled» у Playwright учитывает не только нативный disabled, но и aria-disabled="true"; в Pagination (D-083) это превратило проверку disabled-состояния Prev-кнопки из предположения по HTML в независимое доказательство — форсированный .click() на кнопке с aria-disabled реально выбросил ошибку "element is not enabled", то есть сам инструмент проверки согласился, что элемент недоступен, а не просто визуально приглушён; правило: там, где библиотека утверждает «недоступно для активации», можно не только читать атрибут, но и попросить сам движок автоматизации попытаться нарушить это утверждение — отказ инструмента сильнее, чем чтение разметки
2026-08-08 | comparison value vs. render value | сортировка по «тому, что видно» — частая скрытая ошибка: если извлекать значение для сравнения обратно из уже отрисованной ячейки (текст ссылки, отформатированная строка), сортировка молча ломается в момент, когда рендер и смысл расходятся (валюта с разделителем тысяч сортируется как строка, не число; ссылка-агентство сортируется по видимому лейблу, который может отличаться от канонического имени); в DataTable (D-084) `sortValue(row)` — отдельная функция от `cell(row)`, оперирующая исходными данными строки, а не тем, что было отрисовано, специально чтобы эти два никогда не могли незаметно разойтись; правило: у любого сортируемого/фильтруемого списка держать функцию сравнения на структурированных данных отдельно от функции отображения — совпадение „что видно" и „по чему сравниваем" не гарантировано просто потому, что это один и тот же ряд
2026-08-08 | hydration mismatch | React ожидает, что серверный HTML (SSR) и первый клиентский рендер дадут ОДИНАКОВОЕ дерево — и вставляет служебные comment-маркеры вокруг интерполированных текстовых узлов (`{expr}`), которых нет вокруг статичного текста в JSX; при рефакторинге ReportDocPage.tsx под несколько отчётов (D-085) превращение статичной строки «catalog records» в интерполяцию через общий пропс незаметно добавило бы такой маркер и изменило байтовый вывод страницы — не сломав функциональность, но сделав «первый отчёт не изменился» ложным утверждением при поверхностной проверке (npm run build прошёл бы одинаково успешно и до, и после); поймано не типами и не тестами, а прямым побайтовым diff собранного HTML до/после правки; правило: «эта страница выглядит и работает так же» — утверждение про рантайм, а не про markup, и для него нужна проверка на уровне фактически отданных байтов, а не на уровне «код скомпилировался»
2026-08-08 | let the data write the headline | заголовок и рамка отчёта — гипотеза, а не факт, пока цифры не посчитаны: третий отчёт CN-RESEARCH (D-086) изначально задумывался как «юрисдикции, где закон требует специалиста, а в каталоге его нет» — правдоподобное, но не проверенное предположение; реальный подсчёт дал 0 из 13 юрисдикций с нулевым покрытием, и заголовок был честно переписан под то, что показали данные («…and where our catalog is thinnest»), а не подогнан обратно под изначальный черновик или брошен как «неудачная тема»; правило симметрично с запретом выдумывать цифры (D-045/D-047), но проще пропустить: нулевой/неожиданный результат — это тоже реальная находка со своей ценностью, а не сигнал искать другую тему или переформулировать вопрос так, чтобы гипотеза всё-таки подтвердилась
2026-08-08 | stale remote-tracking ref | `git branch -a`/local `refs/remotes/origin/*` — это КЭШ последнего `fetch`, не живое состояние сервера; при старте этой сессии `git branch -a` показал `remotes/origin/claude/accessatlas-g-i18n-continue-0em3dj`, как будто эта ветка реально существует на GitHub, но явный `git fetch origin <branch>` тут же ответил «couldn't find remote ref», а `git ls-remote origin refs/heads/<branch>` — пустой список: ветки на сервере не было вовсе, кэш просто не обновлялся с момента клонирования контейнера; для назначенной харнессом ветки, чьё реальное содержимое вызывает подозрение (не тот проект, не та история), одного `git branch -a` недостаточно — нужен `git ls-remote` или явный `git fetch` именно этой ветки, иначе легко либо избыточно испугаться конфликта с чужой работой, которой на сервере уже нет, либо по ошибке решить, что force-push кого-то перезапишет, хотя перезаписывать нечего; правило: перед любым необратимым git-действием (force-push, снос ветки) проверяй серверное состояние напрямую, а не кэшированный список удалённых веток
2026-08-08 | useId is positional, not just per-component | React.useId() генерирует идентификатор из ПУТИ узла в дереве рендера (позиция среди сиблингов на каждом уровне вложенности), а не из чего-то, что живёт только внутри самого компонента, вызвавшего хук; в G-I18N-CHROME-DE (D-087) наивная правка — добавить в общий Layout.tsx ещё один <Head>-элемент как НОВОГО сиблинга, чтобы поставить <html lang> — на первый взгляд не трогала detali="main"{children}, но сдвигала позиционный индекс всех узлов ПОСЛЕ этой вставки в том же Fragment'е, а значит и useId() любого интерактивного компонента (Accordion/Combobox/формы) на КАЖДОЙ странице сайта, потому что Layout — общий каркас всех 450 страниц; правка нашлась и починилась ДО показа результата — <html lang> перенесён внутрь уже существующего <Head>-вызова в Meta, а не заведён отдельным сиблингом; правило: любая вставка нового JSX-узла (даже условного, даже логически «сбоку») в компонент, который сам вложен в состав детей более широкого дерева, обязана проверяться не «что видно на экране», а «не сдвинулась ли позиция чего-то дальше по дереву» — useId именно такой чувствительный случай, где визуальный ревью ничего не покажет
2026-08-08 | establishment vs. destination jurisdiction | у трансграничных цифровых законов (ЕС) есть два разных якоря применимости: где ПОСТАВЩИК зарегистрирован (establishment) и где находится ПОЛЬЗОВАТЕЛЬ, на которого рассчитан сервис (destination/targeting) — GDPR, например, прямо решает конфликт статьёй 3(2) («экстерриториальность»): нацелен на жителей ЕС → закон применяется, даже если оператор физически в другой стране; в D-089 юрлицо AccessAtlas оказалось американским (California) как раз в момент, когда секции accessibility statement ещё были написаны в предположении «когда зарегистрируемся в ЕС, узнаем страну» — находка не закрыла вопрос, а сменила его: EAA/BFSG отдельно НЕ содержат готовой нормы уровня GDPR ст. 3(2)/27 для неевропейского оператора, обслуживающего пользователей ЕС, поэтому какой надзорный орган (если вообще какой-то) имеет юрисдикцию — реально открытый вопрос, не наша забывчивость; текст честно назвал это, вместо того чтобы угадать орган или оставить устаревшую формулировку «пока не знаем, в какой стране зарегистрируемся» — она стала ложной в момент, когда регистрация состоялась, просто не там, где текст предполагал; правило: когда факт наконец появляется, первым делом перечитать текст, который был написан ДО него — «мы ещё не знаем X» и «мы теперь знаем X, и он не EU» это не один и тот же случай неопределённости
2026-08-08 | branded search vacuum | нулевой объём поиска по имени будущего бренда — актив, а не слабость: проверка кандидатов Veratla/Verscani/AccessAtlas через DataForSEO показала null-объёмы у всех, и это значит, что по своему имени бренд с первого дня займёт всю выдачу без конкуренции (никто не борется за SERP этой строки), тогда как имя со встроенным существующим спросом (напр. «a11y») означает делить выдачу с уже занявшим её сообществом; отдельный практический урок той же сессии: логин DataForSEO регистрозависим (Zincroom@ не проходит, zincroom@ проходит — 40100 при верном пароле), а «Allowed IPs» из прокси-среды с пулом исходящих адресов бесполезен — два сервиса ifconfig.me/ipify.org показали РАЗНЫЕ IP одной сессии, точечный whitelist в такой среде не работает в принципе, только пустой список
2026-08-08 | search intent split | один и тот же предмет ищут двумя разными намерениями, и это РАЗНЫЕ страницы, а не одна: `rgaa` (8100/мес) — информационный интент («что это такое»), `audit rgaa` (390/мес) — транзакционный («как пройти/кого нанять»); старое исследование увидело только транзакционную строку и пропустило в двадцать раз больший информационный спрос, потому что искало по коммерческим формулировкам; практическое следствие в D-090 — не «дописать раздел в существующий гайд», а отдельная страница под каждый интент со взаимными ссылками (informational кормит transactional, не конкурирует с ним), иначе одна страница пытается отвечать на два разных вопроса и проигрывает выдачу по обоим; правило: прежде чем добавлять контент «по теме X», проверь, на какой ВОПРОС об X он отвечает — тема у страниц может совпадать, вопрос совпадать не должен
2026-08-08 | brand vs. infrastructure identifier | при ребрендинге меняется то, что ВИДИТ пользователь, а не то, на чём стоит система: D-091 переименовал SITE_NAME, тексты страниц, email-тексты и даже User-Agent сканирующего бота (внешне видимая идентичность — это бренд), но сознательно НЕ тронул имя воркера accessatlas-worker, идентификаторы D1/KV и Stripe-ключ custom field — они инфраструктурные: их «переименование» означает пересоздание ресурсов с миграцией данных и перепривязкой webhook'ов, а Stripe-ключ вообще обязан байт-в-байт совпадать с живым payload (урок D-028); смешение этих двух категорий — классическая ошибка ребрендинга, когда в порыве консистентности ломают работающую интеграцию ради невидимого пользователю имени; правило: перед заменой строки спроси «кто это читает?» — человек (бренд, менять) или машина (контракт, не трогать без отдельного решения с миграцией)
2026-08-08 | sequence risk in product bootstrap | порядок «сначала строить, потом называть» дёшев на бумаге и дорог на практике: имя расползается по данным/роутам/контенту/письмам/User-Agent ботов задолго до того, как кто-то формально «выбирает бренд», и разворот этого порядка задним числом — не косметика, а полноценная регрессионная итерация по всей кодовой базе (AccessAtlas дошёл до 450 страниц и только тогда переименовался в Verscala, D-091: 18 файлов, живой факт-чек новой юрисдикции, полный набор гейтов, редеплой воркера); урок закреплён не как разовая заметка, а как правка самого шаблона нового проекта (Шаг 1B в project-orchestrator.md: боль клиента/спрос → имя+домен → только потом архитектура) — потому что «не наступать на те же грабли в этом проекте» и «не дать наступить на них следующему проекту» это разные по цене исправления, и второе стоит сделать, пока урок свежий
2026-08-08 | pending zone serves records unproxied | пока зона Cloudflare в статусе `pending` (nameservers у регистратора ещё не переключены), её собственные NS отвечают на запросы записями «как есть» — проксирование НЕ применяется и в DNS-ответе не видно вовсе; в D-092 это привело к тому, что прямой запрос к `arvind.ns.cloudflare.com` вернул реальные цели Microsoft для DKIM-записей, и сессия ошибочно объявила проблему проксирования решённой, хотя API показывал `proxied: true` у 12 записей — то есть в момент перевода NS проксирование включилось бы разом и сломало DKIM/autodiscover/Teams; ошибка поймана сверкой с API до перевода NS; правило шире одного продукта: у ресурса с состояниями «настроен, но ещё не активен» наблюдаемое поведение отражает СТАРОГО владельца трафика, а не будущую конфигурацию — проверять надо декларацию (API/конфиг), а не наблюдаемый эффект, потому что эффект появится только после переключения, когда ошибаться уже дорого
2026-08-08 | scroll restoration | в SPA переход между «страницами» не перезагружает документ, поэтому браузер не сбрасывает позицию прокрутки сам — она остаётся от предыдущего экрана, и клик по карточке из середины длинного списка открывает новую страницу сразу в её середине (D-093, найдено владельцем на живом сайте, невидимо статическим гейтам: разметка валидна, a11y-аудит зелёный, ссылки целы — ломается только опыт живого перехода); правильная реализация имеет два намеренных исключения, без которых фикс сам становится багом: при наличии `#hash` прокрутку не трогают (иначе ломается переход по якорю), а при навигации типа POP (кнопки Назад/Вперёд) не сбрасывают (иначе «назад» перестаёт возвращать пользователя туда, где он был); отдельный архитектурный нюанс этого проекта — сброс реализован ХУКОМ внутри общего Layout, а не компонентом `<ScrollToTop />`, потому что новый JSX-узел в общем каркасе сдвинул бы позиционные индексы и вместе с ними React.useId() на каждой из 451 страницы (D-087)
2026-08-08 | validation started too early never self-heals | автоматическая проверка, запускаемая в момент СОЗДАНИЯ ресурса, не перезапускается сама, когда недостающее условие появляется позже: кастомный домен Pages был добавлен в 18:39, пока зона Cloudflare ещё была `pending`, HTTP-валидация сертификата стартовала и зависла; активация зоны в 18:55 её не оживила — `www` (добавленный так же, но провалидированный по другому пути) стал `active`, а корень висел `pending` больше шести минут, и ожидание не помогло бы никогда (D-094); диагностика по API дала точную развилку — `verification_data: active` (DNS верен) против `validation_data: pending` (сертификат), то есть проблема была не в том, на что все смотрят; починка — удалить и создать ресурс заново, DNS-запись при этом сохраняется; правило: если ресурс создан «слишком рано» относительно своей зависимости, не жди — пересоздай, и различай в диагностике проверку доставки (DNS/сеть) от проверки владения (сертификат)
2026-08-08 | relative luminance | компонент формулы контраста WCAG (не сам контраст): каждый RGB-канал сначала линеаризуется (`v ≤ 0.03928 ? v/12.92 : ((v+0.055)/1.055)^2.4`, потому что человеческий глаз и монитор нелинейны к яркости), затем складывается с весами 0.2126R+0.7152G+0.0722B — зелёный весит в 10 раз больше синего, поэтому на глаз «одинаково тёмные» цвета с разным составом канала дают разный контраст; отношение (L1+0.05)/(L2+0.05) двух таких luminance — и есть контраст-ratio ≥4.5:1. В CN-BRANDBOOK-V2 именно этот расчёт (не визуальная прикидка) поймал два реальных провала до прогона гейта: пара `on-primary-container`/новый `primary-container` (3.65:1, потому что смена РОЛИ токена — с «светлая подложка» на «средний тон hover» — незаметно потащила за собой старую тёмную текстовую пару) и предупреждённый заранее placeholder на `outline-variant` (1.62:1); правило: когда меняется одна половина цветовой пары, контраст пары надо пересчитывать формулой, а не смотреть на новый цвет в изоляции — само по себе он может быть «нормальным», подвох всегда в паре
2026-08-08 | U+FFFD as a corruption tripwire | символ-замена `U+FFFD` («ромб с вопросом») не бывает частью осмысленного контента: он появляется ровно там, где декодер встретил байт, который не смог прочитать как UTF-8 — то есть его присутствие ВСЕГДА означает повреждение, и гейт на него не даёт ложных срабатываний в принципе (в отличие, например, от проверки «нет ли странных символов», где пришлось бы вести список исключений для ✓, €, „лапок“ и эмодзи флагов, которых у нас полно); в D-095 это дало дешёвый постоянный гейт под непостоянный дефект: одна страница из 452 собралась с `city�…` вместо `city…` и в таком виде уехала на прод, при чистом исходнике и верной сборке той же строки на ~30 других страницах — чинить было нечего, поэтому вместо правки добавлена проверка всего `dist/` в конце сборки; общее правило: если дефект непостоянный, разовый фикс бесполезен по определению — ценность только в гейте, который поймает следующий случай, и выбирать надо признак, у которого ложных срабатываний не бывает по природе, а не по настройке
2026-08-08 | accessible name computation | у интерактивного элемента то, что ОЗВУЧИТ скринридер (accessible name), и то, что ВИДНО на экране, — разные вычисления с собственным приоритетом (aria-labelledby > aria-label > текстовое содержимое > title); при добавлении счётчика в чипы FilterableList.tsx («Training 20») слитная видимая строка без контекста звучала бы непонятно, поэтому видимый текст спрятан от AT через aria-hidden, а на самой кнопке задан aria-label с полной формулировкой и правильным числом («Training, 20 agencies») — это не дублирование одного и того же двумя способами, а разведение «что читать» и «что показывать» на два намеренно разных значения одного элемента
2026-08-10 | watchdog timeout (сторожевой таймаут) | внешний ограничитель времени на ВСЮ операцию целиком, а не на её отдельные шаги: `Promise.race([работа, таймер_с_reject])`, причём освобождение ресурса (`finally { browser.close() }`) стоит СНАРУЖИ гонки, чтобы по срабатыванию сторожа управление ушло к очистке немедленно, не дожидаясь зависшего шага. Отличается от пошаговых таймаутов тем, что покрывает и ещё не найденные места зависания. Ключевая ловушка, ради которой он и нужен: `try/catch`/`.catch()` — это НЕ ограничение по времени, они ловят отклонённый промис, а зависший (который не резолвится и не отклоняется никогда) не ловит никто; в AccessAtlas это дало скан, висевший в status='running' 23 часа при полном комплекте `.catch()` вокруг проверок (D-108). Ассоциация: сторожевой пёс на цепи — не разбирает, кто именно лезет через забор, он лает по факту «слишком долго тихо»; и лает ОН, а не забор, поэтому его нельзя ставить внутрь того, что он охраняет
2026-08-10 | at-least-once delivery (доставка «хотя бы один раз») | гарантия очередей сообщений (Cloudflare Queues, SQS, Kafka) — сообщение НЕ потеряется, но может прийти ДВАЖДЫ: если потребитель упал, не подтвердив обработку (`ack`), брокер не знает, успел он сделать работу или нет, и доставляет снова. Отсюда правило: exactly-once не покупается настройкой очереди, он ДОСТРАИВАЕТСЯ на стороне потребителя — идемпотентным гейтом по внешнему источнику правды. В D-110 источник правды — строка скана в D1: обработчик сканирует только при `status='running'`, любой другой статус означает «исход уже записан» (прошлой доставкой или реапом D-109) → `ack` без скана. Практическое следствие, которое легко проглядеть: `ack` нужен и на УСПЕХЕ, и на ОШИБКЕ скана — ошибка записана в D1, это законченная работа; `retry` остаётся только для случая «исход не записан вообще» (упала сама база), иначе каждый недоступный сайт пересканировался бы за деньги по три раза. Ассоциация: заказное письмо с уведомлением — почта скорее принесёт второй экземпляр, чем рискнёт потерять первый; разбираться, что письмо уже читали, обязан получатель, и делает он это по своей записи в журнале, а не по конверту
2026-08-10 | backoff schedule (расписание отступа) | заранее заданная последовательность пауз перед повторными попытками, а не одна фиксированная задержка: каждая следующая длиннее, чтобы клиент переставал давить на занятый ресурс, но общая сумма пауз ограничена внешним дедлайном. В D-112 это `[20, 40]` секунд — не «экспоненциально до победы», а ровно два шага, потому что сверху висит окно реапа D-109 (180с), и третья пауза увела бы старт скана за него, превратив честное «занято» в ложный «таймаут». Общее правило: длину расписания диктует не терпение клиента, а самый ранний внешний дедлайн, после которого ретрай перестаёт быть полезным (реап, TTL сессии, терпение пользователя). Ассоциация: стучаться в занятую переговорку — второй раз через минуту, третий через две, но если твоя собственная встреча начнётся через пять, четвёртого стука уже не будет: идёшь искать другую комнату, а не стоишь под дверью
2026-08-10 | byte-oriented regex vs. Unicode-aware regex | `grep -P` без корректной UTF-8-locale читает символьные классы вроде `[а-яА-Я]` как диапазон БАЙТОВ, а не кодовых точек — многобайтовая UTF-8-последовательность непричастного символа (в D-111 это было тире «—», E2 80 94 в UTF-8) может частично попасть в этот байтовый диапазон и дать ложное совпадение, хотя декодированный текст никакой кириллицы не содержит. Инструмент с настоящей Unicode-семантикой (движок регулярок JS/Node, который работает по UTF-16 code units) на тех же байтах даёт верный результат. Практическое следствие: для не-ASCII текста в этой песочнице `grep -P` — не источник истины, перепроверять find'ы скриптом на другом движке прежде чем действовать по ним (в D-111 ложный сигнал чуть не расширил объём правки на 245 файлов, которых на самом деле не касался). Ассоциация: линейка без делений на батарейках — если её ЕДИНИЦЫ измерения молча оказались другими (байты вместо букв), то каждое «совпадающее» число всё равно врёт, как бы аккуратно ни мерили
2026-08-10 | request interception (перехват запросов) | режим CDP/Puppeteer, в котором браузер приостанавливает КАЖДЫЙ исходящий запрос до вердикта твоего кода: continue()/abort()/respond(). Ловушка в слове «каждый»: запрос без вердикта не отменяется и не проходит — он ВИСИТ до таймаута навигации, поэтому исключение внутри хендлера обязано заканчиваться continue(), а сам хендлер — регистрироваться ДО включения перехвата (иначе окно «перехват включён, отвечать некому»). Блокировать безопасно только то, что не влияет на отрисовку (media, трекеры), но не CSS/шрифты/картинки — иначе проверки контраста и reflow считают фантомную страницу; матчить домен по hostname с границей поддомена, а не подстрокой URL (`example.com/blog/hotjar.com-review` должен пройти). Ассоциация: таможенный пост на единственной дороге — каждая машина обязана получить штамп «проезжай» или «изымаем»; таможенник, задумавшийся над одной, останавливает всю дорогу, а не одну машину
2026-08-10 | smoke test (дымовая проверка) | маленький набор проверок сразу после деплоя, отвечающий на один вопрос: «система вообще жива на главном пути?» — не полнота покрытия, а быстрое обнаружение того, что юнит-тесты увидеть не могут в принципе. Отличие от юнит-тестов не в «меньше/больше», а в СРЕДЕ: они гоняются на настоящем проде, с настоящими лимитами тарифа, настоящей платной платформой и настоящим сайтом. В D-112/D-113 это дало ровно то, ради чего делается: 255 зелёных тестов ничего не знали о суточной квоте Browser Rendering, и только серия из 3 живых сканов показала, что код исправен, а продукт всё равно лежит — причина оказалась в тарифе, а не в коде. Практическое правило отсюда: смоук обязан быть повторяемым и сравнимым с эталоном (мы гоняли ТОТ ЖЕ сайт, что и в прошлой серии, и сверяли число находок — 128 против 128; иначе «работает» неотличимо от «работает не так, как раньше»). Ассоциация: название буквально из инженерии — новую плату включали и смотрели, не пойдёт ли дым; это не проверка того, что она считает правильно, а проверка, что можно начинать проверять
2026-08-10 | bundler resolution vs. Node ESM resolution | esbuild (внутри `wrangler deploy`) резолвит `import './scanner'` без расширения по TS/бандлерной конвенции и спокойно транспилирует .ts-файл, импортированный даже из чисто JS-воркера — эксперимент `npx wrangler deploy --dry-run` с временным `import { OURS_DESCRIPTIONS } from '../../src/lib/wcag.ts'` в worker/index.js прошёл чисто. Но `node --test` (как реально запускается `npm run worker:test`) НЕ бандлер: `--experimental-strip-types` умеет снять типы с ОДНОГО .ts-файла, но не умеет угадать расширение вложенного `import './scanner'` без `.ts` — тот же импорт падает `ERR_MODULE_NOT_FOUND`. Значит «технически импортируется» и «реально тестируется нашим гейтом» — разные факты, и решает второй: worker/lib/costEstimate.js и worker/lib/oursDescriptions.js — сознательные дублирующие копии (не рефакторинг под общий модуль), каждая с гейт-тестом в src/lib/*.workerMirror.test.mjs, доказанным канарейкой (сломал формулу/текст → красный → вернул → зелёный). Ассоциация: два разных пропускных режима на одной границе — по одному документу пускают, по другому нет, и «в принципе можно провезти» не значит «можно провезти именно этим документом»
2026-08-10 | boundary value analysis (анализ граничных значений) | тестировать не только САМО пороговое значение, но и первый шаг ПО ОБЕ стороны от него — потому что баг «сдвинутая граница» видно только со стороны, куда граница уехала. В D-114 гейт синхронизации сметы имел фикстуры ровно на порогах pickBand (4/12/24): это ловит сдвиг порога ВНИЗ (значение-на-границе меняет ветку и краснеет), но не ВВЕРХ — сдвиг `score <= 12` на `<= 13` не трогает ни одной фикстуры (score 12 остаётся 'mid' при обеих формулировках, меняется только score 13, которого в наборе не было), и все 39 тестов остались зелёными на реально расходящемся коде. Лечится тремя точками на порог, а не одной: `порог`, `порог-1`, `порог+1`. Общий смысл: «пиннить границу» и «покрыть границу» — разные вещи; значение НА границе принадлежит одной из веток, и промах ловится только значением из ДРУГОЙ. Ассоциация: проверять забор, стоя точно на меже, — с этой стороны всё на месте; сдвинулся ли столб, видно, только если отойти на шаг в поле по ту сторону
