# LEARNING_LOG

2026-08-05 | DAG | направленный ациклический граф — форма графа задач в GRAPH.yaml: узлы-задачи, рёбра-зависимости, циклы запрещены
2026-08-05 | webhook | обратный HTTP-вызов: внешний сервис сам стучится в наш URL при событии (Stripe -> Worker при оплате)
2026-08-05 | context window | объём токенов, который модель видит за раз; HANDOFF.md существует, чтобы не зависеть от истории чата
2026-08-05 | lockfile | package-lock.json фиксирует точные версии зависимостей; без него `npm ci` в CI не может работать детерминированно
2026-08-05 | fan-out | распараллелить одну большую задачу на N независимых субагентов (5 агентов по ~11 сайтов вместо одного по 53) и слить результаты
2026-08-05 | idempotency | повторный вызов даёт тот же результат без побочных эффектов; GET /api/scan/:id идемпотентен, POST /api/scan — нет (создаёт новую запись каждый раз)
2026-08-05 | dogfooding | использовать свой же продукт на себе; сайт про аудит доступности обязан сам проходить axe-core, иначе доверие рушится в один клик
2026-08-05 | hook | скрипт, который харнесс автоматически запускает на событие (UserPromptSubmit, PostToolUse...); Model Advisor советует модель до начала работы над каждым промптом
2026-08-06 | false negative | проверка, которая молча говорит "всё чисто", хотя проблема есть; наш audit-a11y открывал страницы через file://, CSS не грузился, контраст тривиально "проходил" на неокрашенном HTML
2026-08-06 | graceful degradation | продукт остаётся рабочим при отсутствии необязательной части; без VITE_TURNSTILE_SITE_KEY форма сканера просто не показывает виджет, а не падает
2026-08-06 | abstention | модель, обученная явно говорить "не знаю" вместо угадывания; наш промпт /api/explain просит вернуть пустой explanation для незнакомого ruleId, а не выдумывать объяснение
2026-08-06 | edge case | вход на границе диапазона (0 находок, 40 повторов одной находки) — где чаще всего живут баги; именно такие сценарии поймали и дыру в контрасте (D-014), и слишком широкую вилку цены (D-017)
2026-08-06 | soft filter | сужает выдачу только когда после сужения кто-то остался (или просто поднимает совпадения наверх сортировкой), в отличие от жёсткого AND-фильтра, который может обнулить список; matchAgencies.ts применяет это к standard/priceBand — оба поля заполнены не у всех агентств, жёсткий фильтр почти всегда возвращал бы пусто
2026-08-06 | data retention | политика о том, сколько хранить данные и когда их удалять; у таблицы scans в D1 такой политики фактически нет (записи бессрочны) — обновлённая Privacy честно называет это открытым техдолгом, а не делает вид, что вопрос уже решён, раз он упоминался в RISKS.md
2026-08-06 | cron trigger | обработчик, который платформа вызывает сама по расписанию (не в ответ на HTTP-запрос) — worker/index.js теперь экспортирует scheduled() рядом с fetch(), Cloudflare дёргает его ежедневно по cron из wrangler.jsonc для удаления просроченных сканов
2026-08-06 | fixture drift | синтетическая тестовая фикстура незаметно перестаёт отражать поведение реальной системы (модели, внешнего API) — все фикстуры parseExplainResponse были чистым JSON, а Claude Haiku на деле оборачивает ответ в markdown code fence; юнит-тесты были зелёными, а прод-путь падал в 502 на каждый cache-miss, пока живой прогон с реальным ключом это не вскрыл
2026-08-06 | eventual consistency | изменение (новый секрет, свежий деплой) не применяется мгновенно на всех узлах распределённой системы; сразу после `wrangler secret put` часть запросов ещё получала 503, пока секрет не пропагировался по всем edge-узлам Cloudflare — решение: retry с паузой, а не паника на первый провал
2026-08-06 | additive migration | новая миграция только добавляет (CREATE TABLE/INDEX), не трогает существующие таблицы — `0003_leads.sql` независим от `scans`, поэтому применяется поверх 0001/0002 без риска сломать уже работающий A1-SCAN в проде
2026-08-06 | test coverage gap | постоянный CI-гейт технически зелёный, но физически не смотрит на новый код — `audit-own-a11y.mjs` имеет захардкоженный список страниц, и добавление маршрута `/request-quote` само по себе не попадает под проверку; зелёный `npm run audit-a11y` после A2-LEAD-FORM ничего не доказывал про новую страницу, пока не прогнан отдельный разовый axe-прогон именно по ней — и тот сразу нашёл реальный heading-order баг
2026-08-06 | least privilege | одобрение/доступ выдаётся ровно на то, что нужно конкретной задаче — не шире и не «про запас на будущее»; Фаза 2 разбита так, что `approval_required: true` стоит только на узлах с НОВЫМ внешним сервисом (Resend/Stripe) или реальной рассылкой, а не на всей фазе целиком, и одобрение A1-SCAN на CF-ресурсы явно не наследуется новыми узлами (D-022)
2026-08-06 | orphan page | страница технически живая (собирается, индексируется, проходит все проверки), но ни одна ссылка в интерфейсе на неё не ведёт — только прямой URL; `/request-quote/` была именно такой, пока не добавлен CTA на `/report/:id`, — «работает» и «достижимо» две разные проверки, зелёный audit-a11y ни одну из них не гарантирует за другую
2026-08-06 | import attributes | синтаксис `import data from './x.json' with { type: 'json' }` — явно указывает движку модулей, каким загрузчиком читать файл; понадобился в worker/lib/matchAgenciesServer.js, потому что plain-ESM Cloudflare Worker не бандлится Vite (не видит @data/a11y/* алиас) — та же строка кода одинаково валидна и под node --test (Node 22), и под esbuild-бандлер wrangler, что и позволило переиспользовать один файл для юнит-тестов и живого /api/lead
2026-08-06 | email obfuscation | приём против спам-ботов: адрес на странице не пишут как чистый текст `mailto:`, а прячут — Cloudflare-защита кодирует его в hex/XOR-строку (`/cdn-cgi/l/email-protection#...`), которую браузер расшифровывает на лету, или сайт просто пишет адрес словами («info собака domain точка nl»); при исследовании 96 агентств для outreach часть email пришлось декодировать детерминированно (не угадывать) именно поэтому
2026-08-06 | constraint as business rule | ограничение на уровне БД (PRIMARY KEY/UNIQUE) вместо проверки в коде приложения — `featured.agency_slug PRIMARY KEY` гарантирует ровно одно активное размещение на агентство даже если приложение когда-нибудь забудет проверить; подтверждено не чтением DDL, а живым повторным INSERT, который реально упал на SQLITE_CONSTRAINT_PRIMARYKEY
2026-08-06 | upsert | одна атомарная операция «вставить или обновить, если уже есть» (`INSERT ... ON CONFLICT DO UPDATE`) вместо ручного select-затем-insert-или-update из кода — worker/routes/stripeHook.js апсертит `featured` по `agency_slug`: первая оплата создаёт строку, продление (второе `checkout.session.completed`) обновляет ту же строку атомарно в БД, без гонки между двумя параллельными вебхуками одной подписки
2026-08-06 | retry | отправитель события (Stripe) сам повторяет доставку, если не получил быстрый успешный ответ (2xx) — поэтому worker/routes/stripeHook.js отвечает 200 на любое событие с верной подписью, даже необрабатываемого типа или без нужных metadata: не-2xx там означало бы, что Stripe годами долбит вебхук повторами о том, что мы осознанно не обрабатываем
2026-08-06 | bearer token | секрет, дающий право действия любому, кто им обладает — без проверки, откуда он взялся; ловушка в A2-CLAIM-API: если verify-токен для email-ссылки совпадал бы с claimId, который API и так возвращает вызывающему в ответе 201, обладание ответом API само по себе стало бы «доказательством владения почтой» — решение (D-023) развело их на два разных значения, только claimId уходит в ответ, token остаётся в D1 до перехода по ссылке
2026-08-06 | sandbox domain | бесплатный/тестовый режим транзакционного email-сервиса (Resend, SendGrid и т.п.) с общим доменом отправителя — реально доставляет письма только на email владельца аккаунта, любому другому получателю отдаёт ошибку валидации, пока не верифицирован собственный домен; проверено безопасным пробником на IANA-зарезервированный test@example.com (Resend честно ответил 422), а не реальной тестовой отправкой живому человеку
2026-08-06 | organic coverage | охват фичи растёт сам по себе по мере накопления данных, а не сразу на 100% — A2-LEAD-EMAIL уведомляет только claimed-агентства, на старте таких 0, так что часть лидов не приведёт ни к одному письму; это не баг и не недоделка, а прямое следствие честного решения не выдумывать email там, где его нет (D-025)
2026-08-06 | approval outlives its trigger | одобрение владельца на конкретное действие не протухает само по себе, если действие временно физически невозможно — A2-CLAIM-REBUILD одобрен «в проде», но подключать его некуда, пока не существует A0-DEPLOY; approval остаётся в силе на будущее (D-026), спрашивать заново не нужно, когда прод-цель появится — это не то же самое, что approval, который «не переносится на новые платные ресурсы автоматически» (там разные ресурсы, здесь один и тот же, просто ещё не готов принять результат)
2026-08-06 | server-authoritative timestamp | дата/время, которую вычисляет сервер в момент обработки события, а не читает из данных, присланных клиентом или третьей стороной — A2-STRIPE-LIVE (D-027) намеренно не берёт `until` (дата окончания featured) ни из Stripe custom field, ни из чего-либо ещё, присланного покупателем: такое поле в принципе редактируемо на стороне клиента (случайно или намеренно), поэтому `computeFeaturedUntil` считает «сегодня + 365 дней» сам, в момент когда воркер обрабатывает подтверждённый по подписи webhook
2026-08-06 | display value vs. identifier drift | видимый пользователю текст поля (label) и его программный идентификатор (key) в UI-конструкторах часто задаются раздельно и не синхронизируются при редактировании — Stripe генерирует `key` custom field из текста `label` только один раз, при первом сохранении, и не пересчитывает его при последующем переименовании label; A2-STRIPE-LIVE (D-028) сначала предположил `CUSTOM_FIELD_KEY = 'agency_slug'` по видимому в Dashboard тексту, реальный оплаченный платёж вскрыл настоящий key — `yourslugaccessatlas`; урок: сверяться с реальным payload, а не с тем, что показывает интерфейс конструктора
2026-08-06 | subset of a criterion | честнее закрыть проверяемую ЧАСТЬ требования, чем заявить его целиком или не брать вовсе: «заголовок описывает тему раздела» (9.2.4.6) автоматизации не поддаётся — это смысл, — но пустой заголовок не описывает ничего по определению, и это ловится однозначно, без ложных срабатываний; в карте покрытия такой критерий помечен покрытым, а в тексте находки прямо сказано, какая именно часть проверена (D-039) — иначе «покрыто» превратилось бы в обещание, которого проверка не выполняет
2026-08-06 | derived copy | число в маркетинговом тексте, вычисленное из тех же данных, что и сам продукт, вместо вписанного руками: главная берёт «29 из 50» из coverageSummary, поэтому после следующего роста покрытия обновится сама — вписанная константа врала бы молча и обнаружилась бы только когда кто-то заметит расхождение с /methodology (D-038); тот же приём, что уже применён к числу агентств и стран в заголовке главной
2026-08-06 | limits as a feature | честно показанная граница возможностей продаёт лучше завышенной цифры: страница /methodology одинаково заметно говорит и «29 из 50 проверяем автоматически», и «21 требует человека, потому что зависит от смысла и контекста» — второе не признание слабости, а точный ответ на вопрос «зачем мне агентство», то есть прямой мост в каталог, ради которого каталог и существует (D-037); попутно оба гейта (sitemap, a11y-аудит) имеют СВОИ захардкоженные списки страниц, и новая страница выпала из обоих при полностью зелёной сборке
2026-08-06 | cross-page check | часть требований доступности в принципе не проверяется по одной странице — консистентность навигации (9.3.2.3), одинаковые названия одной функции (9.3.2.4), наличие более чем одного способа найти страницу (9.2.4.5) существуют только в СРАВНЕНИИ страниц между собой; мы обходили 6 страниц с самого начала, но смотрели каждую изолированно, поэтому эти критерии не покрывал ни axe, ни наши DOM-проверки — выигрыш 3 критерия сразу (D-036) без единого нового обхода, только за счёт того, что уже собрано
2026-08-06 | harmonised standard as a single source | когда требования гармонизированы, знать нужно ОДИН нормативный документ, а не N национальных: EN 301 549 един на весь ЕС, поэтому карта покрытия строится один раз и действует для всех 13 юрисдикций — 13 законов различаются санкциями и областью применения, то есть ровно тем, что мы решили не показывать (D-035); практический эффект — 4.5 КБ JSON с 50 критериями вместо гигабайтов законов на 8 языках, и покрытие (26/50) считается скриптом из живых метаданных axe.getRules(), а не переписывается руками при каждом обновлении
2026-08-06 | right number, wrong regime | цифра может быть буквально верной по тексту закона и всё равно вводить в заблуждение, если взята из ПАРАЛЛЕЛЬНОГО режима: «Франция — €50 000» реально записана в loi 2005-102 ст. 47-1 (проверено по Légifrance), но ст. 47 покрывает госорганы и частные компании только от €250 млн оборота (décret 2019-768) — типовой интернет-магазин под неё не подпадает, а частный сектор по EAA идёт отдельной веткой в Code de la consommation; проверять надо не только величину санкции, но и ОБЛАСТЬ ПРИМЕНЕНИЯ, иначе сверка с первоисточником даёт ложную уверенность (D-034)
2026-08-06 | harmonised standard vs. decentralised enforcement | ЕС может унифицировать ТРЕБОВАНИЕ, оставив ПРИНУЖДЕНИЕ национальным: EAA (Directive (EU) 2019/882) вводит единый EN 301 549 для всех, но механизма «одного окна» как в GDPR ст. 56 не даёт — по ст. 21/23 каждая страна назначает свои органы и сама проверяет услуги на своей территории, потому что директива превращается в 27 национальных законов, а не действует напрямую; практический вывод для продукта: сканировать под разные страны бессмысленно (находки идентичны), различается только правовая обёртка — что убило изначально задуманную мульти-юрисдикционную фичу и свело её к одной строке (D-033)
2026-08-06 | end-to-end reachability | «поле вычисляется и возвращается API» и «пользователь его видит» — две разные проверки, и первая ничего не гарантирует про вторую: jurisdictionNote считался воркером, писался в D1 и приходил в ответе с самого D-030, но фронтенд-тип ScanFinding про него не знал, а ReportPage не рендерил — две полные итерации работы (D-030, D-031) не доходили до глаз пользователя ни разу, при том что все worker-тесты были зелёными; родственник orphan page, но по слою: там страница была недостижима по ссылке, здесь данные обрывались на границе backend->frontend
2026-08-06 | primary source vs. aggregator | закон транспозиции директивы (например EAA) существует один раз — на официальном правовом портале страны (RIS/Finlex/Lovdata/retsinformation.dk); блоги compliance-вендоров пересказывают его с разной точностью и общим мотивом напугать точнее продать услугу — при расширении A3-JURISDICTION на 8 новых стран (D-031) каждый закон процитирован с официального источника, а суммы штрафов из вторичных источников намеренно НЕ включены в код (verified:false) именно потому что не сверены с текстом закона напрямую
2026-08-06 | compliance gap vs. conformance gap | «нарушение стандарта» и «то, за что реально штрафуют» — разные множества, и второе часто проще проверить, чем первое: немецкий надзор (MLBF) начинает не с WCAG, а с наличия заявления о доступности (Anlage 3 §14 BFSG) — бинарная проверка без ложных срабатываний, тогда как WCAG-соответствие автоматом ловится лишь на ~57% (axe-core); наш сканер измерял только второе и потому выдавал 100/100 сайту без заявления, то есть максимальную оценку тому, кто первым попадёт под проверку
2026-08-06 | forgiving input matching | принимать несколько эквивалентных форм пользовательского ввода вместо одной строгой (технической), когда сама форма поля намекает на менее строгий ввод — переименовав label custom field с "slug" на человекочитаемое "Your agency name" (D-029), пришлось научить `resolveAgencySlug` матчить и по названию агентства (`agencies[].name`), не только по точному slug, иначе смена формулировки поля молча сломала бы реальные платежи; ключевая деталь — неоднозначное совпадение (два агентства с одинаковым нормализованным именем) намеренно не резолвится вообще, а не берёт первое попавшееся: снисходительность к формату ввода не должна означать угадывание при неоднозначности
2026-08-06 | TOCTOU (time-of-check to time-of-use) | состояние читается в один момент, а используется/проверяется в другой — между ними оно могло измениться, и код молча работает с уже неактуальным значением; A3-KEYBOARD проверял «виден ли фокус» отдельным `page.evaluate()` ПОСЛЕ выхода из цикла Tab-нажатий — к этому моменту фокус на короткой странице уже мог уйти на `<body>` (после последней ссылки), и страница с реальным видимым `outline` ложно флагалась «невидимый фокус»; исправление — читать селектор и видимость атомарно, за один `evaluate()`, пока элемент точно ещё в фокусе
2026-08-06 | guard clause that eats the payload | ранний выход, поставленный ради одной цели (не бампить impact дважды), унёс с собой вторую, склеенную с ней в том же блоке (простановку правовой пометки): `if (f.impact === 'critical') return f` выглядел безобидно, но axe.js отдаёт `a11y-statement-missing` СРАЗУ как critical — и потому находка «заявления нет вообще» не получала ссылки на закон никогда, а получала только более слабая «заявление неполное»; 200 зелёных тестов молчали, потому что фикстуры подавали impact:'serious' — форму, которой в проде не существует (fixture drift, третий случай); правило: если в одной ветке делаются ДВЕ вещи, ранний выход отменяет обе — разделяй условие по числу эффектов, а тест пиши на ту форму данных, которую реально эмитит соседний модуль, и закрепляй это отдельной проверкой его исходника (D-040)
2026-08-06 | assembling beats producing | связать существующие активы в один путь дешевле и ценнее, чем произвести новые: немецкий рынок получил рабочий вход `/bfsg-check/` без единой новой записи данных — 18 прюферов, две статьи, проверенная правовая ссылка, сканер и карта покрытия уже лежали в проекте, но нигде не сходились в одну цепочку «скан → чего не хватает → на каком основании → к кому идти»; актив, до которого нельзя дойти по ссылке, экономически равен отсутствующему (родственник orphan page, но на уровне пути пользователя, а не отдельной страницы) — и именно при сборке цепочки обнаружилось, что её главное звено (D-040) вообще не работает
2026-08-06 | decisions decay in content | принятое решение применяют к коду и забывают про контент: D-035 убрал суммы штрафов «как класс», добавил два инварианта в тесты — и всё равно суммы остались жить в немецком гайде, включая FAQPage-разметку в выдаче, потому что инварианты сторожили `jurisdictionNote`, а не markdown; правило: запрет, сформулированный как «нигде», обязан получить проверку по ВСЕМ поверхностям вывода (код, контент, структурированные данные), иначе он действует только там, где о нём вспомнили в день принятия
