version: 1
updated_at: 2026-08-13
limits:
  max_parallel: 3
  max_nodes_per_iteration: 8
  default_max_attempts: 4
nodes:
  - id: A0-ENRICH
    title: Применить enrich-патчи описаний (119 записей), починить slug the-pixel, валидация + сборка
    owner: data-engineer
    status: done
    depends_on: []
    inputs: [data/a11y/enrich/, scripts/enrich-a11y.mjs, data/a11y/agencies.json]
    scope: [data/a11y/agencies.json, data/a11y/enrich/us-batch.json]
    outputs: [data/a11y/agencies.json]
    verify:
      - node scripts/enrich-a11y.mjs --dry   # 0 неизвестных slug
      - node scripts/enrich-a11y.mjs
      - node scripts/build-a11y.mjs          # 253 агентства, без ошибок
      - npm run typecheck
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-05 (commit e0c9a85): the-pixel — не баг, запись корректно в excluded.json (не оказывает услуг доступности); патч в us-batch.json был устаревшим дублем, удалён. Описания 81→200/253 (79.1%). Побочно: package-lock.json отсутствовал в репо — добавлен, иначе npm ci в CI не мог работать."

  - id: A0-DESC-REST
    title: Описания оставшимся профилям без description (~53) — только проверяемые факты
    owner: data-engineer
    status: done
    depends_on: [A0-ENRICH]
    inputs: [data/a11y/agencies.json, data/a11y/README.md]
    scope: [data/a11y/enrich/, data/a11y/agencies.json]
    outputs: [data/a11y/enrich/desc-final.json]
    verify:
      - node scripts/build-a11y.mjs
      - "покрытие description >= 90%"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-05 (commit 1985a15): 5 параллельных субагентов исследовали сайты, 45 записей получили описание, 8 исключены (мёртвые/заброшенные сайты, поглощения, нет услуг доступности — та же логика, что the-pixel) и удалены из agencies.json + добавлены в excluded.json. Итог: 253→245 агентств, описания 245/245 (100%, было 79.1%). Порог 90% для снятия noindex закрыт."

  - id: A0-CI-FIX
    title: "Починить CI: package-lock.json (добавлен), lint:upl/lint:minimize/check-links.mjs отсутствуют, хотя ci.yml их вызывает"
    owner: devops-engineer
    status: done
    depends_on: []
    inputs: [.github/workflows/ci.yml, package.json, scripts/]
    scope: [package.json, scripts/, .github/workflows/ci.yml]
    outputs: [scripts/check-links.mjs]
    verify: ["npm ci проходит", "npm run typecheck", "npm run build", "npm run check-links — 0 битых ссылок"]
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-05 (commit 969bdf5): ci.yml на этой ветке был скопирован целиком с main (detnav) при заведении ветки. lint:upl/lint:minimize — линтеры юридического контента detnav (content/** на формулировки вида UPL), у AccessAtlas нет такого контента — шаги удалены, не переписаны заглушками (D-009). check-links.mjs — генерик-полезная идея, реализована заново под dist/ этого сайта: 413 внутренних ссылок, 0 битых. npm ci→typecheck→build→check-links проходят локально."

  - id: A0-OWNER-LEGAL
    title: Реквизиты Imprint + покупка домена
    owner: product-lead
    status: done
    depends_on: []
    inputs: []
    scope: [src/pages/ImprintPage.tsx, src/pages/AccessibilityStatementPage.tsx]
    outputs: [src/pages/ImprintPage.tsx]
    verify:
      - "D-089 (2026-08-08): реквизиты юрлица получены от владельца напрямую в чат, опубликованы на /imprint/ — Murman Express Inc, California corporation, 2699½ N Beachwood Drive STE 4048, Los Angeles CA 90068; представитель Evgenii Skliarov (CEO); email zincroom@gmail.com; телефон +1 213 421 8848. typecheck/build 450 стр./audit-a11y 47 стр. 0 нарушений/check-links 498-0/scripts:test 48/48 — ✅"
      - "AccessibilityStatementPage.tsx обновлён: секция Provider identification перестала быть 'pending', Legal basis и Enforcement body честно называют новую находку — юрлицо НЕ в ЕС/ЕЭЗ (California, USA), какой надзорный орган применим к неевропейскому оператору — не решено с юристом, не выдумано — ✅"
      - "Домен куплен владельцем 2026-08-08: verscala.com (GoDaddy, 2 года, + M365 Email — ящик info@verscala.com владелец создаст позже). Узел закрыт ПОЛНОСТЬЮ (D-089 реквизиты + D-091 домен). Публичный бренд переименован в Verscala (D-091)"
    max_attempts: 1
    attempts: 1
    risk: high
    approval_required: true
    notes: "Юр-реквизиты закрыты (D-089); домен — единственное, что осталось от исходного scope узла. Без домена не снять noindex с /imprint/ (эта правка — задача A0-ORIGIN, не эта) и не открыть A0-DEPLOY."

  - id: A0-ORIGIN
    title: "Ребрендинг AccessAtlas→Verscala + реальный домен verscala.com (куплен владельцем 2026-08-08), снять noindex с Imprint"
    owner: frontend-engineer
    status: done
    depends_on: [A0-OWNER-LEGAL]
    inputs: [src/lib/seo.tsx, scripts/gen-a11y-sitemap.mjs, wrangler.jsonc]
    scope: [src/, scripts/gen-a11y-sitemap.mjs, wrangler.jsonc]
    outputs: []
    verify:
      - "grep -r 'accessatlas.example' по src/scripts/wrangler.jsonc → пусто; grep 'AccessAtlas' по dist/ → пусто"
      - "полный набор гейтов зелёный; dist-дифф до/после объясним только сменой имени/домена/noindex (ничего структурного)"
      - "/imprint/ индексируем (noindex снят), sitemap на https://verscala.com"
      - "воркер редеплой с ALLOWED_ORIGIN=https://verscala.com (одобрено владельцем вместе с моделью Sonnet), CORS-заголовок проверен живьём"
    max_attempts: 2
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Разблокирован 2026-08-08: владелец купил verscala.com на GoDaddy (+M365 Email — ящик info@verscala.com будет создан владельцем позже, Imprint пока остаётся с gmail). Внутреннее имя проекта в docs/project/ НЕ переименовывается (историческая память); ребрендинг касается только user-facing строк (SITE_NAME, JSON-LD, тексты страниц). Готово 2026-08-08 (D-091): 18 файлов, субагент Sonnet, перепроверено родителем независимо — все гейты перезапущены (245/48/20/211, build 451 стр. / sitemap 415 — imprint добавлен, check-links 500-0, audit-a11y 47-0), грепы: 0 «accessatlas.example» в исходниках, 0 «AccessAtlas» в dist, canonical/robots на verscala.com. Воркер редеплой (Version 14ed0644), CORS проверен живьём preflight'ом: access-control-allow-origin: https://verscala.com (после ~30 сек edge-пропагации — тот же класс, что D-020). Оставлены сознательно: имя воркера accessatlas-worker, D1/KV-имена, CUSTOM_FIELD_KEY Stripe (yourslugaccessatlas — обязан совпадать с живым payload), внутренние комментарии scripts/."

  - id: A0-DEPLOY
    title: Cloudflare Pages деплой + автодеплой из CI
    owner: devops-engineer
    status: review
    depends_on: [A0-ORIGIN]
    inputs: [.github/workflows/ci.yml, docs/deepdive-accessibility-directory.md]
    scope: [.github/workflows/, wrangler/pages config]
    outputs: []
    verify:
      - "прод-URL отдаёт 200 — ✅ verscala.pages.dev: главная, /imprint/, /scan/, /guides/rgaa-guide/, /reports/jurisdiction-coverage-gap/, /agencies/deque-systems/, /components/data-table/, /wcag/, /sitemap.xml — все 200"
      - "в живом HTML бренд Verscala, 0 вхождений AccessAtlas, canonical https://verscala.com/ — ✅"
      - "кастомные домены verscala.com + www привязаны к проекту — ✅ (status pending до перевода NS)"
      - "ОСТАЁТСЯ: владелец меняет NS на GoDaddy ns73/ns74.domaincontrol.com → arvind/gwen.ns.cloudflare.com; после этого зона active, домены Pages active, сайт на verscala.com"
      - "НЕ СДЕЛАНО: автодеплой из CI (вторая половина узла) — сейчас деплой ручной (wrangler pages deploy --branch main)"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: true
    notes: "D-092 (2026-08-08): Pages-проект `verscala` создан, 962 файла задеплоены, домены привязаны. ЛОВУШКА, стоившая ложного «готово»: первый деплой ушёл в PREVIEW (проект создан с production-веткой main, а деплой шёл с git-ветки итерации) — команда рапортовала успех, а боевой URL отдавал 404; починка `--branch main`. DNS: 9 почтовых записей переведены в dns-only (проксирование сломало бы DKIM/M365 — см. D-092), парковочные A GoDaddy заменены на CNAME → verscala.pages.dev (proxied). Узел остаётся `review`: NS у регистратора не переключены + автодеплой из CI не настроен."

  - id: A1-SCAN
    title: "Сканер MVP: POST /api/scan (Browser Rendering + axe-core) -> D1, GET /api/scan/:id"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/domains/architecture.md, docs/project/INTERFACES.md]
    scope: [worker/, wrangler.jsonc, migrations/]
    outputs: [worker/, wrangler.jsonc, migrations/0001_init.sql, docs/project/domains/backend.md]
    verify: ["wrangler dev: скан example.com возвращает findings", npm run typecheck, npm run worker:test]
    max_attempts: 4
    attempts: 2
    risk: medium
    approval_required: true
    notes: "Закрыто 2026-08-06 (D-021). Реальный деплой на аккаунт владельца (D1 accessatlas-scans, RATE_LIMIT_KV, EXPLAIN_CACHE — реальные ID в wrangler.jsonc), настоящий скан https://example.com через Browser Rendering: 2 подлинные axe-core находки (landmark-one-main, region — у example.com действительно нет <main>), score 94. Ошибочный путь тоже проверен живьём: заведомо несуществующий домен -> status=error, errorCode=refused (ERR_CONNECTION_RESET), понятный текст. Данные подтверждены прямым SELECT из реальной D1. Транзиентные edge-ошибки Cloudflare (1104/1042) наблюдались в первые ~10с сразу после деплоя на 2 из 8 опросов — не связаны с данными/кодом, самостоятельно прошли. Cron Trigger (A1-RETENTION) подтверждён зарегистрированным на аккаунте (schedule в выводе deploy)."

  - id: A1-REPORT
    title: "UI отчёта /report/:id — находки по WCAG, severity, score"
    owner: frontend-engineer
    status: done
    depends_on: [A1-SCAN]
    inputs: [docs/project/INTERFACES.md, docs/project/VISION.md]
    scope: [src/pages/, src/components/, src/routes.tsx, src/lib/scanner.ts, worker/lib/errors.js]
    outputs: [src/pages/ReportPage.tsx, src/lib/scanner.ts, .env.example]
    verify:
      - npm run build
      - npm run typecheck
      - npm run audit-a11y
      - "async-прогресс скана виден пользователю (VISION.md UX-требование 2)"
      - "своё понятное сообщение на каждое error-состояние, не общее (UX-требование 4)"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06: все 5 UX-требований выполнены, 6 состояний верифицированы через Playwright с подменённым API против реального dev-сервера — 0 нарушений axe-core (см. domains/frontend.md). Найден и исправлен реальный баг контраста (D-014). Закрыто 2026-08-06 (D-021): A1-SCAN теперь done, реальный ScanReport с живого воркера побайтово совпадает с контрактом, который ReportPage.tsx уже потреблял в мокнутых тестах. Один остаточный, честно незакрытый пробел: реальный браузер (Playwright Chromium) в этой песочнице не может достучаться ни до какого внешнего HTTPS вообще (подтверждено отдельно на example.com, не только на нашем воркере) — тот же класс ограничения, что у Browser Rendering (D-010), а не баг фронтенда/бэкенда. Полный browser+live-backend E2E непроверяем именно в этой среде."

  - id: A1-LANDING
    title: "Лендинг сканера /scan — форма URL, Turnstile, честный прогноз времени, ссылка из шапки"
    owner: frontend-engineer
    status: review
    depends_on: []
    inputs: [docs/project/VISION.md, src/lib/scanner.ts]
    scope: [src/pages/ScanPage.tsx, src/components/TurnstileWidget.tsx, src/lib/scanner.ts, src/lib/data.ts, src/components/Layout.tsx]
    outputs: [src/pages/ScanPage.tsx, src/components/TurnstileWidget.tsx]
    verify:
      - npm run build
      - npm run typecheck
      - npm run audit-a11y
      - "клиентская валидация URL не делает лишний сетевой запрос"
      - "успешная отправка ведёт на /report/:id с реальным scanId"
      - "серверная ошибка (напр. rate limit) отображается пользователю"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06 (D-015). Статическая пререндерящаяся страница (не client-only, в отличие от /report/:id) — 385-я страница сборки, добавлена в sitemap. 4 сценария проверены живьём через Playwright с подменённым `page.route()`: idle-рендер, невалидный URL (0 сетевых вызовов), успешный сабмит → /report/abc123/, серверная ошибка 429 → текст виден пользователю. Все состояния axe-чисты. Explicitly НЕ добавлено поле email — POST /api/scan его не использует, обещать в UI значило бы врать (D-015). status=review, не done: реальный Turnstile-виджет (внешний скрипт challenges.cloudflare.com) не тестировался в этой песочнице — тот же сетевой MITM-барьер, что у D-010; путь без VITE_TURNSTILE_SITE_KEY (виджет не рендерится) полностью проверен."

  - id: A1-EXPLAIN
    title: AI-пояснения находок (Claude Haiku + KV-кэш ruleId×locale)
    owner: backend-engineer
    status: done
    depends_on: [A1-SCAN]
    inputs: [docs/project/DECISIONS.md, docs/project/INTERFACES.md]
    scope: [worker/]
    outputs: [worker/lib/explain.js, worker/routes/explain.js, worker/lib/ratelimit.js]
    verify:
      - npm run worker:test
      - "400 на невалидный ruleId, 503 без ANTHROPIC_API_KEY, 502 с понятной причиной при ошибке API"
      - "повторный запрос того же ruleId отдаётся из кэша (KV EXPLAIN_CACHE, ключ ruleId×locale)"
      - "лимит: 30/час на IP (checkExplainRateLimit)"
    max_attempts: 4
    attempts: 2
    risk: medium
    approval_required: true
    notes: "Закрыто 2026-08-06 (D-020, D-021): владелец явно одобрил ANTHROPIC_API_KEY отдельно от CF-одобрения A1-SCAN (правило сработало как задумано). Live-прогон настоящим ключом через `wrangler dev --local --var` (ключ передан только как аргумент процесса, нигде не записан на диск и не закоммичен) вскрыл РЕАЛЬНЫЙ баг: Claude Haiku оборачивает JSON в markdown code fence несмотря на промпт «ONLY a JSON object» — parseExplainResponse падал на JSON.parse, каждый cache-miss отдавал 502. Ни одна прежняя синтетическая фикстура это не покрывала. Исправлено (stripCodeFence), 5 новых фикстур в explain.test.mjs — 39/39 worker-тестов зелёные. Живьём подтверждены все документированные пути: happy path, cache hit, abstention, 400, 503. Отдельно: тот же ключ загружен как настоящий Worker secret (`wrangler secret put`) на реальный деплой A1-SCAN и подтверждён живым запросом — объяснение для `landmark-one-main` (ровно то правило, что реально нашёл живой скан example.com) пришло реальное и по делу. Замечено: сразу после `secret put` часть запросов ещё получала 503 (секрет не пропагировался мгновенно на все edge-узлы Cloudflare) — не баг, обычная эксплуатационная задержка распространения секрета по глобальной сети, самостоятельно прошла за секунды."

  - id: A1-COST
    title: "Оценка стоимости исправлений на /report/:id — эвристика × price bands, всегда вилка"
    owner: frontend-engineer
    status: done
    depends_on: [A1-REPORT]
    inputs: [docs/project/DECISIONS.md, data/a11y/taxonomies.json]
    scope: [src/lib/costEstimate.ts, src/pages/ReportPage.tsx]
    outputs: [src/lib/costEstimate.ts]
    verify:
      - npm run build
      - npm run typecheck
      - "живой прогон 5 сценариев (0 находок/budget/premium/enterprise/дедуп) через Playwright, значения сверены с ручным расчётом по формуле"
      - "каждое состояние axe-чисто"
    max_attempts: 4
    attempts: 2
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06 (D-017, уточняет D-006). Первая версия (интерполяция между двумя соседними price bands) дала неинформативно широкие диапазоны — поймано именно живой проверкой с ручным расчётом ожиданий, не код-ревью. Переделано на один band по effort-score (дубль дедуп-логики worker/lib/score.js). Формат чисел приведён к конвенции taxonomies.json (один символ валюты на диапазон) — тоже найдено верификацией. Дисклеймер и ссылка на каталог обязательны рядом с оценкой (D-006), не факультативны. Закрыто как done 2026-08-06: A1-REPORT, от которого зависел этот узел, теперь тоже done (D-021)."

  - id: A1-MATCH
    title: "Блок «подходящие агентства» под отчётом (страна/услуга/бюджет) + бейджи верификации на AgencyCard"
    owner: frontend-engineer
    status: done
    depends_on: [A1-REPORT]
    inputs: [docs/project/BACKLOG.md, docs/project/VISION.md, src/lib/data.ts, data/a11y/types.ts]
    scope: [src/lib/matchAgencies.ts, src/components/MatchedAgencies.tsx, src/components/AgencyCard.tsx, src/pages/ReportPage.tsx]
    outputs: [src/lib/matchAgencies.ts, src/components/MatchedAgencies.tsx]
    verify:
      - npm run typecheck
      - npm run build
      - npm run audit-a11y
      - npm run check-links
      - "5 сценариев Playwright против реальных agencies.json: any country / country=Germany (сверено с ручным фильтром) / critical finding → remediation-пул / честный empty-state (Australia+remediation, реальный 0-результатный кейс в данных) / верификационный бейдж на AgencyCard"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06 (D-018). matchAgencies.ts переиспользует sortListing/agenciesIn/withService/withStandard из data.ts, не изобретает новый порядок. Бейдж верификации (sourceRefs.length + lastVerified) добавлен в AgencyCard — общий компонент, задел проверен полным audit-a11y (22 страницы, 0 нарушений) и check-links (408 ссылок, 0 битых) заново, не только новых страниц. Закрыто как done 2026-08-06: A1-REPORT, от которого зависел этот узел, теперь тоже done (D-021)."

  - id: A1-PRIVACY
    title: "Обновить Privacy Policy под данные сканера (URL, результаты, IP, Turnstile)"
    owner: product-lead
    status: done
    depends_on: [A1-SCAN]
    inputs: [worker/routes/scan.js, worker/lib/db.js, worker/lib/ratelimit.js, worker/lib/turnstile.js, migrations/0001_init.sql, docs/project/RISKS.md]
    scope: [src/pages/PrivacyPage.tsx, docs/project/RISKS.md]
    outputs: [src/pages/PrivacyPage.tsx]
    verify:
      - npm run typecheck
      - npm run build
      - npm run audit-a11y
      - npm run check-links
      - "текст сверен с реальным кодом worker/, не с предположением о том, что он делает"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06. Прочитан реальный код (scan.js/db.js/ratelimit.js/turnstile.js), не документация о нём: email в БД хранится, если передан через API (хотя UI-форма его не собирает, D-015) — сформулировано честно, не как «мы не собираем email»; на момент написания scans в D1 хранились без TTL — RISKS.md R6 обновлён тогда же, чтобы не переоценивать закрытость риска; IP — только транзитно в KV (TTL 1 час) для rate-limit, не в D1; Turnstile при включении шлёт IP в Cloudflare и может ставить свою куку — прежняя формулировка «no cookies are set» была неточной, уточнена. done, не review: чисто документная страница, ничего не зависит от живого прогона A1-SCAN по существу — зависимость в GRAPH только смысловая (описывает его данные), не техническая блокировка. Текст про retention обновлён тем же днём вслед за A1-RETENTION (см. ниже)."

  - id: A1-RETENTION
    title: "Ежедневное удаление сканов старше 90 дней (Cron Trigger + worker/lib/retention.js)"
    owner: backend-engineer
    status: review
    depends_on: []
    inputs: [docs/project/RISKS.md, migrations/0001_init.sql]
    scope: [worker/lib/retention.js, worker/index.js, wrangler.jsonc, src/pages/PrivacyPage.tsx]
    outputs: [worker/lib/retention.js, worker/lib/retention.test.mjs]
    verify:
      - npm run worker:test
      - npx wrangler deploy --dry-run
      - "граница: скан ровно на cutoff не удаляется (строгое <)"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06 (D-019). DELETE FROM scans WHERE created_at < cutoff, окно 90 дней, ежедневный Cron Trigger 0 3 * * *. Использует существующий idx_scans_created_at — миграция не нужна. 5 юнит-тестов на фейковом D1, 39/39 worker-тестов зелёные. Реальный деплой 2026-08-06 (D-021) подтвердил Cron Trigger зарегистрированным на аккаунте владельца (`schedule: 0 3 * * *` в выводе `wrangler deploy`). status=review, не done: само срабатывание по расписанию (03:00 UTC) ещё не произошло/не пронаблюдано в рамках этой сессии — логика удаления и факт регистрации триггера подтверждены, а не единственно недостающий кусок (живой прогон scheduled()) — иначе была бы та же неполная картина, что у A1-SCAN до его закрытия."

  # ---------------------------------------------------------------------
  # Фаза 2 — Lead Marketplace. Разбито 2026-08-06 (см. HANDOFF.md, DECISIONS.md).
  # Правило: одобрение платных CF-ресурсов на A1-SCAN (2026-08-05) НЕ переносится
  # на новые платные/внешние ресурсы Фазы 2 (Stripe, Resend) — каждый узел,
  # которому нужен НОВЫЙ внешний платный сервис или реальная рассылка живым
  # третьим лицам, помечен approval_required: true отдельно. Узлы, которые только
  # пишут код / используют уже одобренный D1 (accessatlas-scans) / работают
  # локально — approval_required: false, консистентно с тем, как были размечены
  # A1-REPORT/A1-COST/A1-MATCH относительно уже одобренного A1-SCAN.
  # ---------------------------------------------------------------------

  - id: A2-LEAD-SCHEMA
    title: "Схема D1 для лидов (leads table) — миграция, только локально"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/INTERFACES.md, migrations/0001_init.sql, migrations/0002_error_code.sql]
    scope: [migrations/, docs/project/INTERFACES.md]
    outputs: [migrations/0003_leads.sql]
    verify:
      - npm run db:migrate:local   # применяется на локальную D1 без ошибок
      - npm run worker:test
      - "поля таблицы соответствуют черновику Lead/leads из INTERFACES.md §3–4, не придуманы заново"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06: migrations/0003_leads.sql (leads: id PK, scan_id, country, standard, service, budget, deadline, contact_json, matched_json, status DEFAULT 'sent', created_at + idx на created_at и scan_id) — поля 1:1 с черновиком INTERFACES.md §4, ничего не добавлено сверх. `npm run db:migrate:local` применил все 3 миграции на локальную accessatlas-scans без ошибок (подтверждено PRAGMA table_info). `npm run worker:test` 39/39 зелёные (миграция не трогает существующий worker-код, тестов на leads пока нет — они появятся вместе с A2-LEAD-API, который читает/пишет эту таблицу). Реальный remote accessatlas-scans этим узлом не тронут — только --local. Разблокировано: A2-LEAD-API."

  - id: A2-LEAD-FORM
    title: "RFQ-форма на фронтенде — без реальной отправки (submit подключается в A2-LEAD-API)"
    owner: frontend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/INTERFACES.md, docs/project/VISION.md, src/lib/matchAgencies.ts]
    scope: [src/pages/, src/components/, src/lib/leadForm.ts]
    outputs: [src/pages/RequestQuotePage.tsx, src/components/LeadForm.tsx, src/lib/leadForm.ts]
    verify:
      - npm run build
      - npm run typecheck
      - npm run audit-a11y
      - "клиентская валидация полей (страна/стандарт/услуга/бюджет/контакт) по типу Lead из INTERFACES.md §3"
      - "0 сетевых вызовов при submit, пока /api/lead не реализован — явный TODO/дисклеймер в UI, не тихая заглушка"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06: src/lib/leadForm.ts (LeadDraft/validateLeadForm — валидирует country против data.ts::countries, standard/service/budget против таксономий, email regex, deadline не в прошлом; никакого fetch), src/components/LeadForm.tsx (форма + client-only превью подходящих агентств через уже существующий matchAgencies() — читает только забандленные статические данные, сети не трогает), src/pages/RequestQuotePage.tsx. `npm run typecheck`/`npm run build` зелёные. `npm run audit-a11y` зелёный, но НЕ покрывает новую страницу — её нет в захардкоженном SAMPLE_ROUTES (scripts/audit-own-a11y.mjs), это scripts/ — вне scope этого узла (владелец каталога — devops-engineer, INTERFACES.md §6). Поэтому прогнан отдельный одноразовый axe-прогон (Playwright, вне репозитория) по /request-quote/ в 3 состояниях формы (пустая/с ошибками валидации/после успешного client-side submit) — 0 нарушений во всех трёх; тем же прогоном перехвачены все сетевые запросы браузера во время submit — 0, включая внешние. Найден и исправлен реальный baseline-баг: heading-order (h1→h3, пропущен h2) в блоке превью совпадений — добавлен h2 «Agencies that would match». scope официально не включал src/routes.tsx и src/lib/data.ts, но без записи маршрута страница вообще недостижима — по прецеденту A1-LANDING (её scope прямо включал routes.tsx) сделаны два минимальных, явно раскрытых здесь правки: paths.requestQuote() в data.ts и один route-объект в routes.tsx. Оба пробела закрыты той же сессией сразу после: `/request-quote/` добавлена в `SAMPLE_ROUTES` (`scripts/audit-own-a11y.mjs`) и в `scripts/gen-a11y-sitemap.mjs` — постоянный CI-гейт теперь реально видит страницу (23 страницы вместо 22, живой прогон 0 нарушений), sitemap 349 URL вместо 348. Заодно найден и закрыт третий пробел, отдельный от изначально заявленных: страница была `orphan` — ни одна ссылка в UI на неё не вела (только прямой URL), несмотря на то что она в sitemap/индексируется. Добавлена точка входа из воронки: CTA «Request a quote from matching agencies →» в `MatchedAgencies.tsx` (блок на `/report/:id`, рядом с уже существующим «Compare all agencies»), с пробросом `?scanId=` — тот же паттерн deep-link, что `RequestQuotePage.tsx` уже поддерживал, но никто раньше не генерировал. Верифицировано живьём: 2 состояния формы (пустой сабмит с ошибками валидации, заполненная форма → превью совпадений с дисклеймером «Not sent yet») — axe-чисто; CTA на `/report/:id` (мокнутый API) даёт корректный `href=/request-quote/?scanId=<id>`. Полный набор (`typecheck`/`worker:test` 39/39/`build` 386 страниц/`check-links` 410 ссылок 0 битых/`audit-a11y` 23 страницы 0 нарушений) зелёный. done."

  - id: A2-LEAD-API
    title: "POST /api/lead: приём RFQ + матчинг агентств → D1, без отправки email"
    owner: backend-engineer
    status: done
    depends_on: [A2-LEAD-SCHEMA]
    inputs: [docs/project/INTERFACES.md, src/lib/matchAgencies.ts, worker/lib/ratelimit.js, worker/lib/turnstile.js]
    scope: [worker/routes/lead.js, worker/lib/matchAgenciesServer.js, worker/index.js]
    outputs: [worker/routes/lead.js]
    verify:
      - npm run worker:test
      - "400 на невалидный/неполный ввод (страна/стандарт/услуга обязательны)"
      - "запись в D1 leads (локально, wrangler dev --local), matched[] совпадает с логикой matchAgencies.ts / INTERFACES.md §5"
      - "тот же rate-limit/Turnstile паттерн, что у /api/scan — публичный write-эндпоинт не остаётся без защиты от спама"
      - "email агентствам этим узлом НЕ отправляется (лог/TODO вместо реального вызова Resend) — реальная отправка это A2-LEAD-EMAIL"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06. worker/lib/matchAgenciesServer.js — переиспользует ТОТ ЖЕ алгоритм, что src/lib/matchAgencies.ts (agenciesIn/withService/withStandard/sortListing/standardForCountry, INTERFACES.md §5), но как отдельный plain-ESM модуль: worker/ не бандлится Vite, не может импортировать @data/a11y/* алиас или .ts-файлы напрямую, поэтому логика продублирована вручную (JSON-каталог импортируется через `import ... with { type: 'json' }`, поддерживается и Node --test, и wrangler/esbuild — подтверждено обоими). D1-доступ к leads и rate-limiter держим прямо в worker/routes/lead.js, а не в worker/lib/db.js / worker/lib/ratelimit.js — оба файла вне scope этого узла (db.js специализирован под scans, см. его заголовок; ratelimit.js трогать не стали, чтобы не расширять scope молча) — тот же паттерн (KV fixed-window), но отдельная реализация с более низким лимитом (5/ч на IP, RFQ дороже скана). Turnstile переиспользует worker/lib/turnstile.js::verifyTurnstile как есть (файл не менялся, только импортирован), тот же паттерн, что и scan.js: пропускается, если TURNSTILE_SECRET_KEY не задан. Верифицировано: 26 новых юнит-тестов (worker/lib/matchAgenciesServer.test.mjs, worker/routes/lead.test.mjs; в т.ч. с реальным каталогом), 65/65 worker:test зелёные; typecheck и build чистые; живьём через `wrangler dev --local` — валидный лид даёт 201 {leadId, matched[]} с реальными slug'ами из каталога (Германия/audit → 5 реальных агентств), подтверждено прямым `wrangler d1 execute ... SELECT` из локальной D1 (все поля, включая matched_json/contact_json/status='sent'); 400 на пустое тело/неизвестную страну/стандарт/сервис/бюджет/email; rate-limit подтверждён живьём (6-й запрос с одного IP → 429, независимые IP не блокируются); Turnstile skip-путь (без секрета) подтверждён живьём, оба пути с секретом (accept/reject) — юнит-тестом с подменённым fetch (реальный публичный Turnstile-ключ владельца в этой сессии не запрашивался — не требуется для этого узла); email агентствам не отправляется — явный TODO-комментарий + юнит-тест, что fetch не вызывается на happy path; CORS/OPTIONS и регрессия /api/scan, /api/explain, 404 проверены тем же живым прогоном — не сломаны правкой worker/index.js. `npx wrangler deploy --dry-run` бандлится чисто. Единственное намеренно не сделанное: реальный remote-деплой обновлённого воркера на прод — отдельное решение по факту (как при A1-RETENTION/A1-EXPLAIN), не подразумевается автоматически этим узлом."

  - id: A2-LEAD-EMAIL
    title: "Отправка лида 3–5 подходящим агентствам через Resend"
    owner: backend-engineer
    status: done
    depends_on: [A2-LEAD-API]
    inputs: [docs/project/INTERFACES.md]
    scope: [worker/lib/resend.js, worker/routes/lead.js]
    outputs: [worker/lib/resend.js]
    verify:
      - npm run worker:test
      - "503 без RESEND_API_KEY (пересмотрено на graceful degradation, см. A2-CLAIM-EMAIL notes — та же логика)"
      - "живой прогон: минимум 1 реальное письмо агентству получено (после одобрения владельцем ключа)"
    max_attempts: 4
    attempts: 1
    risk: high
    approval_required: true
    notes: "Владелец выбрал вариант из двух предложенных (D-025): уведомлять ТОЛЬКО claimed+verified среди совпавших агентств (claims.email), не добавлять email в agencies.json (отдельный крупный data-проект) и не оставлять узел замороженным. findClaimedEmails() ищет claims по matched-slugs с verified=1 — незаявленные агентства не получают ничего (нет для них проверенного адреса). Best-effort, как A2-CLAIM-EMAIL — сбой Resend не превращает уже успешную запись лида в 5xx. 6 новых тестов (включая: claimed-но-не-verified не уведомляется; claimed-но-не-в-matched-set не уведомляется). 117/117 worker:test. Живьём реальным RESEND_API_KEY: claim + verify реального агентства (marc-haunschild-accessibility-consulting, email владельца), затем лид, реально совпавший с этим агентством по matchAgencies (DE/audit/mid) — уведомление отправлено без ошибок в логе воркера; лид без claimed-совпадений прошёл 0 писем, без сбоя. Регрессия /api/claim/scan/stripe-hook не задета."

  - id: A2-CLAIM-SCHEMA
    title: "Схема D1 для claim-потока (claims table) — миграция, только локально"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/INTERFACES.md, data/a11y/types.ts]
    scope: [migrations/]
    outputs: [migrations/0004_claims.sql]
    verify:
      - npm run db:migrate:local
      - npm run worker:test
      - "поля соответствуют черновику claims из INTERFACES.md §4 (agency_slug, email, verified, patch_json, status)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06. migrations/0004_claims.sql, поля 1:1 с INTERFACES.md §4. Применена локально (npm run db:migrate:local), схема подтверждена живьём (PRAGMA table_info), реальный INSERT+SELECT тестовой записи прошёл и удалён. worker:test 65/65 (не задет — миграция не трогает код), typecheck/wrangler deploy --dry-run чистые. Разблокировало A2-CLAIM-API."

  - id: A2-CLAIM-API
    title: "POST /api/claim: создание claim-записи + verify-токен, БЕЗ отправки письма"
    owner: backend-engineer
    status: done
    depends_on: [A2-CLAIM-SCHEMA]
    inputs: [docs/project/INTERFACES.md, data/a11y/types.ts]
    scope: [worker/routes/claim.js]
    outputs: [worker/routes/claim.js]
    verify:
      - npm run worker:test
      - "400 если agencySlug не существует в agencies.json"
      - "verify-токен генерируется и пишется в D1 (локально), но email этим узлом не отправляется"
      - "домен email не проверяется на совпадение с доменом сайта агентства этим узлом (это часть верификации при переходе по ссылке, не при создании заявки) — если проектное решение иное, зафиксировать в DECISIONS.md"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06 (D-023). worker/routes/claim.js: POST /api/claim {agencySlug, email, turnstileToken?} -> 201 {claimId}, тот же стиль, что lead.js/stripeHook.js (D1-доступ прямо в routes/-файле, не lib/db.js; fixed-window rate-limit 5/ч на IP, отдельная реализация как у lead.js; Turnstile через уже существующий worker/lib/turnstile.js, skip без секрета). agencySlug валидируется против реального agencies.json (переиспользован export agencies из worker/lib/matchAgenciesServer.js, не выдуман список) — неизвестный slug -> 400. Домен email намеренно НЕ сверяется с доменом сайта агентства на этом шаге, как и просил узел — зафиксировано в коде-комментарии, отдельного решения не потребовалось (это и есть принятое поведение). НАЙДЕНО и явно зафиксировано архитектурное решение сверх буквального текста узла (D-023, DECISIONS.md): контракт INTERFACES.md §2 описывает вход как отображающийся в ДВЕ разные вещи — verify-link (email, будущий A2-CLAIM-EMAIL) и claimId (ответ API, сразу). Схема claims (0004_claims.sql) не содержала отдельного token-столбца — если бы verify-токен совпадал с id/claimId, вызывающий получал бы 'доказательство владения почтой' прямо из ответа API, не переходя по ссылке, что ломает саму цель email-верификации (пример атаки: указать чужой email агентства, получить claimId, тут же 'верифицировать' заявку). Добавлена migrations/0006_claim_token.sql (ALTER TABLE claims ADD COLUMN token TEXT + индекс) — выход за буквальный scope узла (worker/routes/claim.js), раскрыто явно по прецеденту A1-LANDING/A2-LEAD-FORM, не тихо; patch_json НЕ переиспользован под token (зарезервирован под будущие правки профиля, A2-CLAIM-REBUILD). INTERFACES.md §4 обновлена тем же коммитом. worker/index.js тоже тронут за пределами буквального scope — маршрут иначе недостижим (тот же прецедент). 14 новых юнит-тестов (worker/routes/claim.test.mjs, включая явный тест 'claimId != token' и 'email domain NOT checked'), 103/103 worker:test зелёные. typecheck и wrangler deploy --dry-run чистые. Живьём через wrangler dev --local + wrangler d1 execute --local на настоящей локальной D1 (не мок): неизвestный slug -> 400 с полем agencySlug в error; валидный claim -> 201 {claimId}, прямой SELECT подтвердил реальную строку (status='pending', verified=0, patch_json=NULL, token — 64-символьный hex, отличный от claimId); rate-limit подтверждён живьём (6-й запрос с одного IP -> 429, независимые IP не блокируются); grep по файлу подтвердил отсутствие fetch()/сетевых вызовов вообще — email не отправляется. Регрессия других маршрутов (/api/lead, /api/scan через index.js, 404, OPTIONS) проверена тем же живым прогоном — не сломаны правкой worker/index.js. Тестовые строки удалены из локальной D1 после проверки. Разблокировало (технически) A2-CLAIM-EMAIL и A2-CLAIM-REBUILD — approval остаётся отдельным гейтом на обоих."

  - id: A2-CLAIM-EMAIL
    title: "Письмо со ссылкой верификации claim через Resend"
    owner: backend-engineer
    status: done
    depends_on: [A2-CLAIM-API]
    inputs: [docs/project/INTERFACES.md]
    scope: [worker/lib/resend.js, worker/routes/claim.js]
    outputs: [worker/lib/resend.js, worker/routes/claim.js]
    verify:
      - npm run worker:test
      - "503 без RESEND_API_KEY (пересмотрено на graceful degradation, см. notes)"
      - "живой прогон: реальное verify-письмо получено на реальный email домена агентства (после одобрения владельцем)"
    max_attempts: 4
    attempts: 1
    risk: high
    approval_required: true
    notes: "Resend одобрен владельцем явно для этого узла 2026-08-06 (D-024, зафиксировано, не предположено). Реализовано: worker/lib/resend.js (тонкая обёртка над Resend API) + worker/routes/claim.js (verify-ссылка несёт claims.token, не claimId — D-023, идёт на GET /api/claim/verify?token=..., НОВЫЙ эндпоинт, добавлен тем же узлом — иначе ссылка в письме вела бы в никуда, тот же принцип, что D-015). Отклонение от исходного verify-критерия «503 без RESEND_API_KEY»: реализовано как best-effort (запись claim в D1 не зависит от Resend, отправка — отдельный некритичный шаг, ошибка/отсутствие ключа логируется, не превращает уже успешную запись в 5xx) — иначе создание заявки сломалось бы для уже работающего A2-CLAIM-API без всякой связи с email. Живьём подтверждено через wrangler dev --local с реальным RESEND_API_KEY: реальное письмо отправлено на email владельца (200 от Resend, настоящий message id), реальный переход по verify-ссылке (GET .../verify?token=...) реально перевёл claims.verified 0→1 в D1. НАЙДЕНО (D-024): sandbox-домен Resend (onboarding@resend.dev, используется пока нет верифицированного домена) может слать ТОЛЬКО на email владельца аккаунта Resend — на произвольного стороннего получателя (реального claim-заявителя) отдаёт 422, подтверждено безопасным пробником на example.com, не на живом человеке. Реальная доставка третьим лицам не заработает, пока не верифицирован домен сайта (A0-ORIGIN). Секрет НЕ загружен в прод (в отличие от ANTHROPIC_API_KEY после D-020) — с текущим sandbox-ограничением это была бы частично рабочая прод-функция, вводящая в заблуждение; ждёт домена."

  - id: A2-CLAIM-REBUILD
    title: "Ежедневный ребилд: D1-оверлей claims/featured наложить на agencies.json перед сборкой"
    owner: devops-engineer
    status: review
    depends_on: [A2-CLAIM-API]
    inputs: [docs/project/INTERFACES.md, scripts/build-a11y.mjs]
    scope: [scripts/, .github/workflows/]
    outputs: [scripts/apply-d1-overlay.mjs]
    verify:
      - node scripts/build-a11y.mjs
      - "оверлей применяется идемпотентно: повторный прогон не меняет результат"
      - "прод-сайт не читает D1 в рантайме (правило INTERFACES.md §4) — оверлей только на этапе сборки"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: true
    notes: "Владелец явно одобрил узел «в проде» 2026-08-06 (D-026) — approval снят. scripts/apply-d1-overlay.mjs написан и живьём проверен против реальной локальной D1: патчит claimed/featured, снимает истёкший featured (проверено — удалил D1-запись, поле реально исчезло из agencies.json), идемпотентность подтверждена побайтовым сравнением второго прогона с первым, не только тестом. patch_json намеренно не применяется (см. комментарий в скрипте — ни один узел ещё не собирает такие патчи). 7 юнит-тестов на чистых функциях computeOverlay/applyOverlay. status=review, не done: реальное включение в прод-CI (cron + деплой) невозможно прямо сейчас — A0-DEPLOY (цель деплоя) сам ещё заблокирован A0-OWNER-LEGAL, добавлять cron сейчас означало бы либо падать на отсутствующем секрете, либо собирать в пустоту. Approval сохраняется на будущее, повторно спрашивать не нужно, когда A0-DEPLOY решится. Побочно: worker:test (117)/scripts:test (7) добавлены в ci.yml — не запускались в CI вообще до этого прохода."

  - id: A2-STRIPE-SCHEMA
    title: "Схема D1 для featured (featured table) — миграция, только локально"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/INTERFACES.md, data/a11y/types.ts]
    scope: [migrations/]
    outputs: [migrations/0005_featured.sql]
    verify:
      - npm run db:migrate:local
      - npm run worker:test
      - "поля соответствуют черновику featured из INTERFACES.md §4 (agency_slug PK, until, stripe_ref)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06. migrations/0005_featured.sql, поля 1:1 с INTERFACES.md §4. agency_slug PK — намеренно (одно активное featured-размещение на агентство, продление через UPDATE, не новую строку); подтверждено живьём: второй INSERT с тем же agency_slug реально упал на UNIQUE constraint (SQLITE_CONSTRAINT_PRIMARYKEY), не просто предположение по DDL. Применена локально, INSERT+SELECT тестовой записи прошёл и удалён. worker:test 65/65, typecheck/wrangler deploy --dry-run чистые. Разблокировало A2-STRIPE-WEBHOOK-CODE."

  - id: A2-STRIPE-WEBHOOK-CODE
    title: "Обработчик POST /api/stripe-hook: проверка подписи + обновление featured, тесты на синтетических фикстурах Stripe-событий"
    owner: backend-engineer
    status: done
    depends_on: [A2-STRIPE-SCHEMA]
    inputs: [docs/project/INTERFACES.md]
    scope: [worker/routes/stripeHook.js, worker/lib/stripeSig.js]
    outputs: [worker/routes/stripeHook.js, worker/routes/stripeHook.test.mjs]
    verify:
      - npm run worker:test
      - "проверка подписи webhook отклоняет поддельное событие (тест на синтетическом секрете, НЕ реальном)"
      - "checkout.session.completed → featured.until обновляется в D1 (локально)"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06. worker/lib/stripeSig.js::verifyStripeSignature — реальный алгоритм Stripe (Stripe-Signature: t=…,v1=…, HMAC-SHA256 через Web Crypto secret над \"{t}.{raw_body}\", константное по времени сравнение, поддержка нескольких v1 при ротации секрета, 5-минутный tolerance на timestamp против replay). worker/routes/stripeHook.js: секрет — env.STRIPE_WEBHOOK_SECRET (НЕ STRIPE_SECRET_KEY, тот не нужен этому узлу вовсе), нет секрета → 503 (тот же паттерн, что ANTHROPIC_API_KEY в A1-EXPLAIN); подпись считается по СЫРОМУ телу (request.text(), не request.json()); agency_slug/until извлекаются из checkout.session.completed → session.metadata (единственный канал протащить нашу бизнес-данные через Stripe Checkout — реальную настройку Payment Link с этими полями делает A2-STRIPE-LIVE); апсерт в featured через `INSERT ... ON CONFLICT(agency_slug) DO UPDATE` (agency_slug PK из A2-STRIPE-SCHEMA — продление, не новая строка); любое подтверждённое-по-подписи событие (в т.ч. необрабатываемый тип, в т.ч. валидное событие с некорректной/пустой metadata) отвечает 200, чтобы Stripe не ретраил то, что мы осознанно не обрабатываем — retry имеет смысл только для реальных сбоев доставки, не для 'мы это не парсим'. 24 новых юнит-теста (worker/lib/stripeSig.test.mjs — 11, worker/routes/stripeHook.test.mjs — 13), 89/89 worker:test зелёные, все на синтетическом секрете whsec_test_..., подписи для тестов строятся через node:crypto createHmac НЕЗАВИСИМО от crypto.subtle, которым пользуется сам модуль — проверяет совместимость с реальным алгоритмом Stripe, не то, что код согласен сам с собой. typecheck и `wrangler deploy --dry-run` чистые. Живой прогон через `wrangler dev --local --var STRIPE_WEBHOOK_SECRET:whsec_local_synthetic_dev_secret` + `wrangler d1 execute --local` на реальной локальной D1 (не мок): поддельная подпись (чужой secret) → 400, D1 не тронута; отсутствующий заголовок Stripe-Signature → 400; валидное событие → 200, реальная строка появилась в featured (agency_slug/until/stripe_ref все верные); второе checkout.session.completed для того же agency_slug (продление, новый until) → тот же agency_slug, until обновился на новый, вторая строка НЕ появилась (ON CONFLICT реально сработал в настоящей SQLite/D1, не только по тексту SQL) — тестовая строка удалена после проверки. Разблокировало A2-STRIPE-LIVE (approval_required остаётся true — не снимается этим узлом)."

  - id: A2-STRIPE-LIVE
    title: "Payment Link вручную в Stripe Dashboard (featured €590/год, разовый платёж) + custom field 'Your agency name' (slug ИЛИ название агентства) + живой webhook secret на деплое"
    owner: devops-engineer
    status: in_progress
    depends_on: [A2-STRIPE-WEBHOOK-CODE]
    inputs: [docs/project/DECISIONS.md]
    scope: [worker/routes/stripeHook.js, worker/routes/stripeHook.test.mjs, wrangler.jsonc]
    outputs: []
    verify:
      - "живой тестовый платёж на НОВОЙ ссылке (после пересоздания под D-029) доходит до webhook и обновляет featured в реальной D1"
      - "npx wrangler deploy --dry-run"
    max_attempts: 3
    attempts: 3
    risk: high
    approval_required: true
    notes: "approval: владелец явно одобрил (2026-08-06) — Payment Link вручную в Dashboard, НЕ через API (STRIPE_SECRET_KEY не выдан и не нужен), scope сужен владельцем до одного продукта: featured €590/год, разовый платёж (не месячная подписка, не lead-пакет — оба explicitly deferred). D-027: Dashboard-ссылка не умеет нести динамическую metadata → agency приходит через Stripe custom field в session.custom_fields, не session.metadata. D-028: первая версия Payment Link подтверждена настоящим оплаченным платежом ($10, Apple Pay) — реальный key custom field не совпал с видимым label (`yourslugaccessatlas`, не `agency_slug`), код исправлен под подтверждённое значение. D-029: владелец переименовал label на 'Your agency name' (понятнее не-технической аудитории, включая себя самого) — код теперь принимает и точный slug, и название агентства (`agencies[].name`, case-insensitive), не только slug, иначе люди печатали бы название и молча получали отказ; 245/245 имён в каталоге уникальны, дубликат намеренно не резолвился бы (см. D-029). 126/126 worker:test, typecheck и wrangler deploy --dry-run чистые, живьём подтверждено (значение 'Deque Systems' → верная строка featured). Владелец пересоздаёт саму Payment Link под новый label — ОСТАЁТСЯ: подтвердить key НОВОГО custom field тем же способом (один реальный тестовый платёж → прислать JSON события), настроить Stripe webhook endpoint (checkout.session.completed -> /api/stripe-hook) в Dashboard, передать реальный STRIPE_WEBHOOK_SECRET для `wrangler secret put`, и по желанию задать Statement descriptor на уровне Price, чтобы в выписке было не 'Y2KGLOBAL.COM'."

  - id: A2-OUTREACH-PREP
    title: "Список 99 агентств из гос-деклараций + черновик письма «заберите профиль» — без отправки"
    owner: growth-strategist
    status: done
    depends_on: []
    inputs: [research/STATE.md, data/a11y/agencies.json]
    scope: [research/, docs/project/domains/growth.md]
    outputs: [research/outreach-99.json, research/outreach-letter-draft.md]
    verify:
      - "99 записей: slug, email (публичный, из декларации/сайта), ссылка-первоисточник (декларация)"
      - "email не выдуман — только те, что реально указаны в публичном источнике; неизвестное поле остаётся пустым"
      - "черновик письма на review, ничего не отправлено"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06. Реальное число — 96, не 99 (96 записей в agencies.json с сертификатом gov-declared-auditor; «99» в research/STATE.md — округление из более ранней стадии исследования, расхождение зафиксировано, не скрыто). 5 параллельных субагентов с WebFetch по ~19-20 агентств каждый (fan-out, как A0-DESC-REST) — 82/96 (85%) реальных публичных email найдено с указанием источника и URL, 14/96 честно null с объяснением (403 на все пути, только форма/телефон, необработанная Cloudflare-обфускация, мёртвый домен). 2 явных несовпадения домен-сайта/домен-email помечены, не тихо нормализованы. Письмо ведёт с доказательства (конкретная декларация), не с продажи; featured не упоминается в первом письме; явный opt-out. Ничего не отправлено — только research/outreach-99.json + research/outreach-letter-draft.md. Найден дополнительный, не самоочевидный блокер для A2-OUTREACH-SEND: письмо ссылается на реальный домен (которого пока нет, A0-ORIGIN) и упирается в то же требование Imprint-идентичности отправителя, что R2, но применительно к email, не только к сайту — зафиксировано в domains/growth.md и RISKS.md R2, а не только в Resend-approval."

  - id: A2-OUTREACH-SEND
    title: "Рассылка 99 агентствам через Resend"
    owner: growth-strategist
    status: todo
    depends_on: [A2-OUTREACH-PREP, A2-CLAIM-API]
    inputs: [docs/project/DECISIONS.md]
    scope: [worker/lib/resend.js]
    outputs: []
    verify:
      - "живой прогон на малой пилотной группе (напр. 5 из 99) перед полной рассылкой, по решению владельца"
      - "ссылка «заберите профиль» в письме реально ведёт на рабочий claim-поток (A2-CLAIM-API готов), не на заглушку"
    max_attempts: 2
    attempts: 0
    risk: high
    approval_required: true
    notes: "Resend одобрен владельцем 2026-08-06 для этого узла (D-024) — approval снят, но узел остаётся заблокирован ДВУМЯ независимыми от Resend причинами: (1) реальный домен ещё не куплен (A0-ORIGIN ждёт A0-OWNER-LEGAL) — письмо не может честно ссылаться на несуществующий https://accessatlas.example; (2) подтверждено живым API-пробником (безопасно, на example.com, не на живом человеке, D-024): sandbox-домен Resend в принципе не может доставить письмо стороннему получателю без верифицированного собственного домена — 422 на любой адрес кроме email владельца аккаунта Resend. A2-CLAIM-API готов (done), ссылка «заберите профиль» вела бы на рабочий claim-поток технически, но физически ни одно из 96 писем сейчас доставить нельзя. status=todo сохранён (не blocked, как A2-LEAD-EMAIL) — здесь единственный путь вперёд один и понятен (домен), а не открытый вопрос о модели данных."

  # --- Сканер: разрыв с тем, что реально проверяют регуляторы (BACKLOG.md, 2026-08-06) ---
  # Владелец: "Начинай все по порядку" — все 9 узлов сделаны последовательно в одной
  # итерации (сверх обычного max_nodes_per_iteration:8 — прямое указание владельца).

  - id: A3-STATEMENT
    title: "Детект заявления о доступности (Anlage 3 / EU-модель) на главной + оценка полноты 4 пунктов"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [worker/lib/textUtils.js]
    scope: [worker/lib/statement.js, worker/lib/statement.test.mjs, worker/lib/__fixtures__/statement-*.html]
    outputs: [worker/lib/statement.js]
    verify:
      - "node --test worker/lib/statement.test.mjs — 7/7"
      - "живая проверка на реальных страницах bundesregierung.de и impots.gouv.fr"
    max_attempts: 4
    attempts: 2
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06. findStatementLink() ищет ссылку по тексту (не любое упоминание слова на странице — false positive иначе); evaluateStatementContent() — 4 пункта Anlage 3 по ключевым фразам. Живая проверка на реальных фикстурах (bundesregierung.de/breg-de/barrierefreiheit, impots.gouv.fr/accessibilite) нашла и исправила 2 реальных false negative первой версии: немецкий текст вставляет слова между «Erklärung» и «gilt für» (паттерны сокращены до коротких подстрок), методология называется поимённо («BITV-Test», «audit de conformité»), не словом «geprüft». normalize() также приводит типографский апостроф (’) к обычному — французское «s’applique» не матчилось без этого. Оба реальных гос. заявления теперь корректно detected complete:true."

  - id: A3-FEEDBACK
    title: "Детект доступного канала обратной связи (email/форма/телефон) по официальному EU-заголовку раздела"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [worker/lib/textUtils.js]
    scope: [worker/lib/feedback.js, worker/lib/feedback.test.mjs, worker/lib/__fixtures__/feedback-*.html]
    outputs: [worker/lib/feedback.js]
    verify:
      - "node --test worker/lib/feedback.test.mjs — 5/5"
      - "живая проверка на реальной странице bundesregierung.de/.../barrierefreiheit-feedback"
    max_attempts: 4
    attempts: 2
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06. Первая идея («искать <form> тег») провалилась на живой проверке: единственный <form> на реальной странице bundesregierung.de принадлежал cookie-баннеру, не форме обратной связи. Переключились на поиск официального заголовка EU-шаблона заявления («Feedback and contact information» / «Retour d'information et contact» и переводы, Commission Implementing Decision (EU) 2018/1523) — надёжный сигнал, подтверждённый на двух реальных гос. сайтах (DE, FR). Заодно нашли, что impots.gouv.fr публикует email как голый текст без mailto: — добавлен regex на bare email в окне ±400 символов вокруг найденной фразы."

  - id: A3-PDF
    title: "Детект ссылок на PDF-документы на обойдённых страницах (axe их не видит вообще — не HTML)"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: []
    scope: [worker/lib/pdf.js, worker/lib/pdf.test.mjs, worker/lib/__fixtures__/pdf-*.html]
    outputs: [worker/lib/pdf.js]
    verify:
      - "node --test worker/lib/pdf.test.mjs — 6/6"
      - "живая проверка: 43 уникальных реальных PDF-ссылки на bundesregierung.de/.../publikationen"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06. detectPdfLinks() — href=\"...pdf(?query)\", резолв относительных URL, дедуп. Живая проверка нашла реальный немецкий паттерн '?__blob=publicationFile&v=4' (query после расширения) — regex должен матчить '.pdf' ДО '?', не в конце строки. 44 сырых совпадения на реальной странице, 43 уникальных (одна PDF реально линкуется дважды — не баг дедупа, настоящий дубль на сайте)."

  - id: A3-REFLOW
    title: "Reflow на 320px (WCAG 1.4.10) — viewport + горизонтальный скролл"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: []
    scope: [worker/lib/domChecks.js, worker/lib/domChecks.test.mjs]
    outputs: [worker/lib/domChecks.js]
    verify:
      - "node --test worker/lib/domChecks.test.mjs — 11/11 (реальный Chromium через Playwright)"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06 (вместе с A3-KEYBOARD/A3-MEDIA/A3-RESIZE/A3-COOKIEBANNER — общий модуль domChecks.js, общая живая проверка). checkReflow320() ставит viewport 320×800, ждёт кадр после resize (requestAnimationFrame×2 — без этого некоторые страницы ещё не успели перестроить layout), сравнивает scrollWidth/clientWidth, восстанавливает исходный viewport. Живая проверка реальным Chromium (Playwright, локальный HTML — внешний HTTPS браузеру в песочнице недоступен, тот же барьер, что у A1-LANDING/D-010): фиксированная 900px раскладка корректно даёт overflow, адаптивная — нет."

  - id: A3-KEYBOARD
    title: "Клавиатурная навигация: обход Tab, focus trap, невидимый фокус"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: []
    scope: [worker/lib/domChecks.js, worker/lib/domChecks.test.mjs]
    outputs: [worker/lib/domChecks.js]
    verify:
      - "node --test worker/lib/domChecks.test.mjs — 11/11 (реальный Chromium через Playwright)"
    max_attempts: 4
    attempts: 2
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06. checkKeyboardTraversal() жмёт Tab до 40 раз, ловит trap (один и тот же activeElement 4 раза подряд) и невидимый фокус (computed outline/box-shadow отсутствуют). Живая проверка реальным Chromium нашла и исправила реальный баг первой версии: проверка невидимости фокуса читала document.activeElement ОТДЕЛЬНЫМ evaluate() ПОСЛЕ выхода из цикла Tab — к этому моменту фокус на короткой странице уже мог уйти на <body> (после последней ссылки), и страница с явным `a:focus{outline:3px solid blue}` ложно помечалась «невидимый фокус». Исправлено: селектор и видимость читаются за один evaluate, пока элемент точно ещё в фокусе. Регрессионный тест добавлен."

  - id: A3-MEDIA
    title: "Автовоспроизведение без mute (1.4.2) и видео без <track kind=captions> (1.2.2)"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: []
    scope: [worker/lib/domChecks.js, worker/lib/domChecks.test.mjs]
    outputs: [worker/lib/domChecks.js]
    verify:
      - "node --test worker/lib/domChecks.test.mjs — 11/11 (реальный Chromium через Playwright)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06. checkMedia() — прямой DOM-запрос video/audio, без эвристик текста (в отличие от statement/feedback тут не нужно — свойства autoplay/muted и наличие <track> детерминированы). Живая проверка реальным Chromium: video[autoplay] без muted и без captions-track корректно даёт 2 находки, muted+captions видео — 0."

  - id: A3-RESIZE
    title: "Увеличение текста 200% (WCAG 1.4.4) — обрезка/наложение контента"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: []
    scope: [worker/lib/domChecks.js, worker/lib/domChecks.test.mjs]
    outputs: [worker/lib/domChecks.js]
    verify:
      - "node --test worker/lib/domChecks.test.mjs — 11/11 (реальный Chromium через Playwright)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06. checkResize200() приближает браузерный zoom через CSS `zoom:200%` на documentElement (Chromium-специфично, но Browser Rendering — это Chromium) — честно задокументированное приближение, не идентичное Ctrl+ (тот также масштабирует UI окна браузера, вне DOM). Живая проверка: фиксированная nowrap-раскладка 1200px корректно клипается при 200%, обычный текстовый параграф — нет."

  - id: A3-PAGESELECT
    title: "Приоритизация транзакционных/формовых страниц (корзина, вход, контакт) при выборе, какие ≤6 страниц обойти"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: []
    scope: [worker/lib/links.js, worker/lib/links.test.mjs, worker/lib/__fixtures__/links-*.html]
    outputs: [worker/lib/links.js]
    verify:
      - "node --test worker/lib/links.test.mjs — 9/9"
      - "живая проверка приоритизации на реальном HTML manufactum.de и impots.gouv.fr"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06. pickPriorityLinks() матчит ключевые слова и по пути URL, и по видимому тексту ссылки — живая проверка на manufactum.de нашла, что реальная ссылка на корзину (/warenkorb) имеет видимый текст «0,00 €» (просто сумма, БЕЗ слова «корзина») — версия, искавшая только по тексту, эту страницу вообще не подняла бы. НЕ входило в буквальный scope узла, но найдено тем же живым прогоном и исправлено тем же коммитом: старая sameOriginLinks() матчила href=\"...\" ЛЮБОГО тега, не только <a> — на реальном сайте это ловило <link rel=preload href=...woff2> (шрифты!) как «страницы» для скана, тратя весь бюджет MAX_PAGES на нескачиваемые-как-страница ресурсы. Исправлено в обеих функциях модуля (extractAnchors вынесен в textUtils.js). Регрессионный тест добавлен."

  - id: A3-COOKIEBANNER
    title: "Детект и снятие cookie-баннера ДО axe.run() — иначе axe видит перекрытый DOM"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: []
    scope: [worker/lib/domChecks.js, worker/lib/domChecks.test.mjs]
    outputs: [worker/lib/domChecks.js]
    verify:
      - "node --test worker/lib/domChecks.test.mjs — 11/11 (реальный Chromium через Playwright)"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06. detectAndDismissCookieBanner() — быстрый путь по известным вендорским селекторам (#onetrust-banner-sdk, #CybotCookiebotDialog и т.п.) + основной путь по computed style (position:fixed/sticky + z-index≥100 + покрытие ≥10% viewport + cookie-лексика в тексте), затем клик по кнопке с текстом из списка (accept/akzeptieren/j'accepte/zaakceptuj/aceptar). Живая проверка реальным Chromium: известный вендор и кастомный баннер оба детектятся и снимаются; маленький несвязанный fixed-виджет (кнопка «наверх») корректно НЕ распознаётся банннером (порог покрытия площади спас от false positive). Не находка против сайта — прозрачная scan-meta находка impact:minor, что DOM был скорректирован перед проверкой."

  - id: A3-JURISDICTION-OVERRIDE
    title: "Явный выбор страны пользователем поверх TLD-эвристики + показ jurisdictionNote в отчёте (был невидим с D-030)"
    owner: backend-engineer
    status: done
    depends_on: [A3-JURISDICTION]
    inputs: [docs/project/DECISIONS.md]
    scope: [worker/lib/jurisdiction.js, worker/routes/scan.js, src/lib/scanner.ts, src/lib/jurisdictions.ts, src/pages/ScanPage.tsx, src/pages/ReportPage.tsx, .github/workflows/ci.yml, package.json]
    outputs: [src/lib/jurisdictions.ts, src/lib/jurisdictions.test.mjs]
    verify:
      - "npm run worker:test — 175/175"
      - "npm run src:test — 3/3 (зеркало UI-списка сверено с реальным worker-модулем)"
      - "npm run audit-a11y — /scan/ с новым <select> без нарушений"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06 (D-032). resolveJurisdiction(url, countryCode) — явный код страны ПЕРЕБИВАЕТ TLD (сайт на .com, обслуживающий Германию, по домену не определялся вовсе -> находка 'нет заявления' не получала вес). Невалидный/пустой/не-строковый код молча откатывается к TLD, скан не падает. КЛЮЧЕВАЯ ПОПУТНАЯ НАХОДКА: jurisdictionNote вычислялся с D-030, писался в D1, возвращался API — но фронтенд-тип ScanFinding про него не знал и ReportPage не рендерил, т.е. весь A3-JURISDICTION (две итерации) пользователь не видел ни разу; 'реализовано' и 'видно пользователю' — разные проверки, зелёные worker-тесты вторую не проверяли. Исправлено тем же проходом. Список стран для селектора — вынужденное зеркало (worker plain ESM, D-010), расхождение ловит src/lib/jurisdictions.test.mjs; проверено что тест реально падает (временно убрал Норвегию — упал, вернул — зелёный). Попутно: тесты в src/** не запускались в CI вообще, добавлен npm run src:test + шаг в ci.yml."

  - id: A3-JURISDICTION
    title: "Взвешивание severity по юрисдикции сайта (TLD), не только по axe impact — отсутствие заявления в 13 юрисдикциях ЕС/ЕЭЗ становится critical"
    owner: backend-engineer
    status: done
    depends_on: [A3-STATEMENT]
    inputs: [docs/project/BACKLOG.md]
    scope: [worker/lib/jurisdiction.js, worker/lib/jurisdiction.test.mjs, worker/routes/scan.js]
    outputs: [worker/lib/jurisdiction.js]
    verify:
      - "node --test worker/lib/jurisdiction.test.mjs — 10/10"
      - "npm run worker:test — 170/170 (после интеграции в scan.js)"
    max_attempts: 3
    attempts: 2
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06, расширено тем же днём (D-031) — владелец выбрал 'юрисдикции' вторым пунктом из предложенного меню. jurisdictionForUrl() — TLD→юрисдикция, изначально de/fr/es/nl/pl, расширено до 13 стран (+it/ie/at/be/se/dk/fi/no) — выбраны по реальному покрытию каталога (agencies.json: у всех 13 есть реальные агентства в данных), не наугад. Каждая новая страна — закон транспозиции EAA подтверждён по официальному правовому порталу страны (RIS Austria, Finlex, Lovdata Норвегия, retsinformation.dk, riksdagen.se, ejustice.just.fgov.be, irishstatutebook.ie), не по агрегатору/блогу. ТОЛЬКO DE помечена verified:true с суммой штрафа (§37 BFSG) — все остальные 12, включая новые 8, получают закон+обязательность заявления БЕЗ суммы (verified:false), тот же стандарт, что был у FR/ES/NL/PL с первого прохода. GB/US/CA/AU/CH/IN сознательно НЕ включены — материально другая правовая база (не транспозиция EAA), задокументировано в коде явно, не молчаливый пробел. applyJurisdictionWeight()/интеграция в scan.js не менялись — только таблица данных."

  - id: DE-JURIS-NOTE-FIX
    title: "Правовая пометка не доходила до САМОЙ решающей находки (ранний выход на already-critical)"
    owner: backend-engineer
    status: done
    depends_on: [A3-JURISDICTION-OVERRIDE]
    inputs: [worker/lib/axe.js, worker/lib/jurisdiction.js]
    scope: [worker/lib/jurisdiction.js, worker/lib/jurisdiction.test.mjs, src/lib/scanner.ts]
    outputs: [worker/lib/jurisdiction.js]
    verify:
      - "node --test worker/lib/jurisdiction.test.mjs — 19/19 (было 16)"
      - "npm run worker:test — 202/202"
    max_attempts: 3
    attempts: 1
    risk: high
    approval_required: false
    notes: "Готово 2026-08-06 (D-040). applyJurisdictionWeight() выходила рано на f.impact==='critical', а axe.js отдаёт a11y-statement-missing именно critical — значит правовое основание (Anlage 3 zu §14 BFSG) не получала ровно та находка, ради которой модуль написан; его видел только более слабый -incomplete (serious). 200 зелёных тестов пропустили это, т.к. фикстуры подавали impact:'serious' — форму, которой в проде нет (fixture drift, третий случай в проекте). Теперь пометка ставится ВСЕГДА, бамп impact — только если ниже critical. Добавлен анти-drift тест, читающий реальный axe.js и проверяющий, что там действительно 'critical'. Прежний тест 'does not double-bump' удалён — он фиксировал баг как ожидаемое поведение. Попутно добавлено поле jurisdictionCountry (INTERFACES.md §3), чтобы UI ветвился по коду страны, а не по разбору текста заметки."

  - id: DE-ENTRY
    title: "Связный входной путь для немецкого рынка: /bfsg-check/ (скан → §14 BFSG → названные в декларациях прюферы)"
    owner: frontend-engineer
    status: done
    depends_on: [DE-JURIS-NOTE-FIX]
    inputs: [data/a11y/agencies.json, data/a11y/guides/bfsg-pflichten-guide.md, data/a11y/guides/bitv-test-kosten-ablauf.md, data/a11y/en301549-coverage.json]
    scope: [src/pages/BfsgCheckPage.tsx, src/routes.tsx, src/lib/data.ts, src/pages/ScanPage.tsx, src/pages/ReportPage.tsx, src/pages/CountryPage.tsx, src/components/FilterableList.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs, data/a11y/guides/]
    outputs: [src/pages/BfsgCheckPage.tsx]
    verify:
      - "npm run build — 388 страниц, dist/bfsg-check/index.html существует"
      - "npm run check-links — 417 ссылок, битых нет"
      - "npm run audit-a11y — 25 страниц, /bfsg-check/ чисто (0 нарушений)"
      - "sitemap 351 URL (было 350), /bfsg-check/ внутри"
      - "npm run typecheck — чисто"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06 (D-041). Нового контента не производилось — связаны существующие активы: 18 прюферов отобраны по cert.country==='DE' (не по стране HQ: доказательство привязано к документу), каждый со ссылкой на саму декларацию; цифры покрытия берутся из coverageSummary (50/31/19), а не вписаны; два немецких гайда ведут CTA сюда. CTA → /scan/?country=DE (ScanPage читает параметр в useEffect, не в инициализаторе useState — иначе расхождение при гидратации, страница пререндерится SSG без query). Полной локализации интерфейса НЕ делалось (G-I18N вне scope): оболочка английская, контент немецкий — тот же компромисс, что у гайдов. priceBand не заполнялся: вместо выдуманных цен назван опубликованный ориентир BIK со ссылкой + прямо сказано, почему цен по агентствам нет."

  - id: DE-PAGE-GATE
    title: "Постоянный гейт: новая страница не может выпасть из sitemap и a11y-аудита молча"
    owner: qa-engineer
    status: done
    depends_on: []
    inputs: [src/routes.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs]
    scope: [scripts/page-lists.test.mjs]
    outputs: [scripts/page-lists.test.mjs]
    verify:
      - "npm run scripts:test — 10/10 (было 7)"
      - "негативная проверка: временно убран /bfsg-check/ из audit-списка → тест упал; возвращён → зелёный"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06 (D-041). Проект дважды наступал на одни грабли (D-037 и /request-quote): у gen-a11y-sitemap.mjs и audit-own-a11y.mjs СВОИ захардкоженные списки, и новая страница выпадает из обоих при полностью зелёных прогонах — оба скрипта проверяют список, а не сайт. Тест читает реальный src/routes.tsx и требует каждый статический маршрут в обоих списках; исключения для sitemap (/imprint/, /404/) заданы с причиной, а не молчанием. Третий тест страхует сам парсер: если routes.tsx сменит форму записи, гейт упадёт громко, а не станет тихо пустым."

  - id: G-CERT-EVIDENCE
    title: "Перепроверить все 96 доказательств бейджа доверия и развести декларантов (public-body / private)"
    owner: data-engineer
    status: done
    depends_on: [DE-ENTRY]
    inputs: [data/a11y/agencies.json, data/a11y/types.ts, src/lib/data.ts]
    scope: [data/a11y/types.ts, data/a11y/agencies.json, data/a11y/collect/, data/a11y/README.md, scripts/build-a11y.mjs, src/lib/data.ts, src/components/FilterableList.tsx, src/components/AgencyCard.tsx, src/pages/AgencyPage.tsx, src/pages/BfsgCheckPage.tsx]
    outputs: [data/a11y/types.ts, data/a11y/agencies.json]
    verify:
      - "node scripts/build-a11y.mjs — 245 агентств, 0 ошибок"
      - "негативная проверка гейта: три поломанных cert → ровно 3 ошибки сборки; откат → зелёная"
      - "npm run typecheck / worker:test 202 / src:test 3 / scripts:test 10 — чисто"
      - "npm run build — 388 страниц, sitemap 351, check-links 417, audit-a11y 25 страниц 0 нарушений"
    max_attempts: 3
    attempts: 1
    risk: high
    approval_required: false
    notes: "Готово 2026-08-06 (D-042). 96/96 evidenceUrl открыты вживую; ни одна запись бейдж не потеряла. kind переименован в statement-named-auditor, добавлено обязательное declarant (81 public-body / 13 private / 2 unknown). Главная находка: у 9 записей (8 NL + 200-ok, чей портал toegankelijkheidsonderzoek.nl механическая проверка по домену не ловила) доказательством была собственная страница агентства — заменено записью декларации заказчика в госреестре toegankelijkheidsverklaring.nl, отчёт ушёл в sourceRefs. Инвариант «evidenceUrl не на своём домене» + перечни kind/declarant стали ошибкой сборки в build-a11y.mjs (раньше certs не валидировались вообще). Детали и таблица проверки — domains/data.md, гейт — domains/qa.md."

  - id: G-PRICE
    title: "Заполнить priceBand по реально опубликованным ценам (сплошная проверка 245 агентств)"
    owner: data-engineer
    status: done
    depends_on: [G-CERT-EVIDENCE]
    inputs: [data/a11y/agencies.json, data/a11y/taxonomies.json, src/lib/costEstimate.ts, src/lib/matchAgencies.ts, src/components/FilterableList.tsx]
    scope: [data/a11y/agencies.json, scripts/build-a11y.mjs, scripts/apply-priceband.mjs, scripts/priceband.test.mjs]
    outputs: [data/a11y/agencies.json, scripts/priceband.test.mjs]
    verify:
      - "node scripts/build-a11y.mjs — 245 агентств, 0 ошибок, priceBand 17/245"
      - "негативная проверка гейта: priceBand без цитаты цены в sourceRefs → ошибка сборки; откат → зелёная"
      - "npm run scripts:test — 15/15 (было 10); негативная проверка: снят sourceRef с ценой у access42 → тест упал"
      - "npm run typecheck / worker:test 202 / src:test 3 — чисто"
      - "npm run build 388 страниц, sitemap 351, check-links 417, audit-a11y 25 страниц 0 нарушений"
      - "живая проверка фильтра (Playwright, собранный dist): FR mid → 3 из 28, PL budget → 3 из 29, DE — чипов нет, снятие фильтра восстанавливает список; профиль без цены не показывает строку цены"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06 (D-045). 245/245 проверены обходом сайтов самих агентств; 23 сайта с пустым HTML (JS/анти-бот) добраны отдельно (поиск по домену, sitemap.xml, архивная копия). Заполнено 17 (budget 8 / mid 9), у каждого — URL прайса и дословная цитата суммы; 31 агентство публикует цену не за аудит (обучение, SaaS, ставки за час/страницу, мини-скан, чужие услуги, рыночные ориентиры в собственном блоге), 197 не публикует вовсе. Все 4 ранее заполненных значения подтвердились, но были БЕЗ источника цены — исправлено. Две записи (akse, access-first) оставлены пустыми осознанно: опубликованная вилка не определяет один бэнд. Германия 0/40 подтверждена сплошь, текст /bfsg-check/ не менялся. Главный побочный результат — инвариант в build-a11y.mjs: band без цитаты цены = ошибка сборки."

  - id: G-FOUNDED
    title: "Заполнить founded по реально опубликованным годам основания (сплошная проверка 245 агентств)"
    owner: data-engineer
    status: done
    depends_on: [G-PRICE]
    inputs: [data/a11y/agencies.json, data/a11y/types.ts, scripts/apply-priceband.mjs, src/pages/AgencyPage.tsx]
    scope: [data/a11y/agencies.json, scripts/build-a11y.mjs, scripts/apply-founded.mjs, scripts/founded.test.mjs, src/pages/AgencyPage.tsx]
    outputs: [data/a11y/agencies.json, scripts/apply-founded.mjs, scripts/founded.test.mjs]
    verify:
      - "node scripts/build-a11y.mjs — 245 агентств, 0 ошибок, founded 65/245"
      - "негативная проверка гейта: 4 нарушения (год без цитаты, год не тот, год только в ISO-дате, год 1200) → 4 ошибки, exit 1; откат → зелёная"
      - "npm run scripts:test — 20/20 (было 15); негативная проверка: снят sourceRef с годом у temesis → тест упал, откат → 20/20"
      - "npm run typecheck / worker:test 202 / src:test 10 — чисто"
      - "npm run build 388 страниц, sitemap 351, check-links 417, audit-a11y 25 страниц 0 нарушений"
      - "строка «In business since» присутствует в 65 профилях собранного dist/ и отсутствует у профилей без года"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-06 (D-047). 245/245 проверены обходом сайтов (homepage → about-ссылки → sitemap.xml → LinkedIn, если на него ссылается сам сайт). Заполнено 65 (было 31): 43 новых, 1 исправлено (knowbility 1998→1999), 9 снято. Как и в G-PRICE, ни у одного из 31 старого значения не было источника года. Главное решение: LinkedIn ОТКЛОНЁН как самостоятельный источник по измерению — из 24 записей, где год есть и на сайте, и в LinkedIn, они расходятся в 6 (25%); засчитывается только как подтверждение совпадающего года с собственного сайта (правило D, 15 записей), 72 LinkedIn-only отклонены. Побочно: founded не рендерился НИГДЕ — добавлена строка «In business since» в профиль. Журнал отклонений (8 категорий) — domains/data.md."

  - id: G-GUIDES-BFSG-STATEMENT
    title: "Первый гайд BFSG-кластера про Anlage 3 (Barrierefreiheitserklärung: пять пунктов, где публиковать)"
    owner: product-lead
    status: done
    depends_on: []
    inputs: [worker/lib/statement.js, data/a11y/guides/bfsg-pflichten-guide.md, docs/project/DECISIONS.md]
    scope: [data/a11y/guides/barrierefreiheitserklaerung-bfsg-anlage3.md, data/a11y/guides/bfsg-pflichten-guide.md]
    outputs: [data/a11y/guides/barrierefreiheitserklaerung-bfsg-anlage3.md]
    verify:
      - npm run build   # 389 страниц, dist/guides/barrierefreiheitserklaerung-bfsg-anlage3/index.html существует
      - npm run typecheck
      - npm run check-links   # 418 ссылок, 0 битых
      - npm run audit-a11y    # 25 страниц, 0 нарушений (постоянный сэмпл)
      - "разовый axe-прогон на /guides/barrierefreiheitserklaerung-bfsg-anlage3/ — 0 нарушений, порядок заголовков без пропусков"
      - "sitemap 352 URL (было 351), новый гайд внутри"
      - "нет сумм штрафов в тексте (grep на €/EUR/Bußgeld-цифры) — инвариант D-035"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-07 (D-056). product-lead выбрал G-GUIDES вместо G-I18N для этой итерации (см. D-056 за полным обоснованием — hreflang из G-I18N упирается в тот же A0-ORIGIN блокер, что и весь деплой; масштаб G-I18N не оценён; трафик до деплоя может расти только через контент). Пять обязательных пунктов Anlage 3 сверены внешним поиском (gesetze-im-internet.de отдавал 503 в моменте, вторичный источник bfsg-gesetz.de использован как временная замена, независимо согласуется с уже закодированной эвристикой worker/lib/statement.js). relatedAgencies переиспользует уже проверенный набор из bfsg-pflichten-guide.md, не выдуман заново. Интеграция полностью через существующие динамические механизмы (getStaticPaths, sitemap-генератор по каталогу, BfsgCheckPage-фильтр по countryCode) — ни один захардкоженный список руками не трогали, кроме одной обратной ссылки в bfsg-pflichten-guide.md. Остаток G-GUIDES (EAA-кластер для 8 новых юрисдикций D-031, VPAT-кластер, ещё BFSG-контент) НЕ начат — заведётся отдельными узлами по мере выбора следующего куска."

  - id: G-GUIDES-EAA-8
    title: "EAA-кластер: по одному посвящённому гайду для 8 юрисдикций без текста (IT/IE/AT/BE/SE/DK/FI/NO, D-031)"
    owner: product-lead
    status: done
    depends_on: []
    inputs: [worker/lib/jurisdiction.js, docs/project/DECISIONS.md (D-031/D-035), data/a11y/agencies.json, data/a11y/taxonomies.json]
    scope:
      - data/a11y/guides/eaa-italy-guide.md
      - data/a11y/guides/eaa-ireland-guide.md
      - data/a11y/guides/eaa-austria-guide.md
      - data/a11y/guides/eaa-belgium-guide.md
      - data/a11y/guides/eaa-sweden-guide.md
      - data/a11y/guides/eaa-denmark-guide.md
      - data/a11y/guides/eaa-finland-guide.md
      - data/a11y/guides/eaa-norway-guide.md
      - data/a11y/guides/european-accessibility-act-guide.md
    outputs:
      - "8 новых гайдов + расширенная country-by-country таблица в european-accessibility-act-guide.md (добавлены BE/SE/DK/FI/NO строки + ссылки-'full guide' на все 8)"
    verify:
      - "npm run build-a11y — 245 агентств, 0 ошибок"
      - "npm run typecheck — чисто"
      - "npm run worker:test — 202/202, npm run src:test — 10/10, npm run scripts:test — 20/20 (все без изменений — задача чисто контентная)"
      - "npm run build — 397 страниц (было 388, +9: 8 гайдов + не считая изменение хаба), sitemap 360 URL (было 351)"
      - "npm run check-links — 434 уникальных ссылки, 0 битых (было 417)"
      - "npm run audit-a11y — 25 страниц постоянного сэмпла, 0 нарушений"
      - "разовый axe-прогон на всех 8 новых страницах + обновлённом хабе (не входит в постоянный сэмпл) — 9/9 чисто, 0 нарушений"
      - "grep на €/EUR/Bußgeld-цифры по всем 8 новым файлам — только порог микропредприятия (EUR 2 млн, не штраф), сумм санкций нет нигде — инвариант D-035"
      - "все 8 юридических цитат перепроверены живьём в этой итерации (не только по коду): 6/8 подтверждены WebFetch напрямую (IT/BE/SE/FI/NO + повторно), 2/8 (IE/DK) WebFetch словил антибот 403 у самого портала — перепроверены curl с браузерным User-Agent, оба вернули 200 и совпали текстом; AT — URL из комментария jurisdiction.js (Gesetzesnummer=20012484) оказался мёртвым (404), найден и подтверждён верный (Gesetzesnummer=20012316, содержит 'Barrierefreiheitsgesetz – BaFG' + 'BGBl. I Nr. 76/2023')"
      - "все relatedAgencies (26 слотов, 2-4 на страну) сверены скриптом против data/a11y/agencies.json по полю hq.countryCode — все существуют, ни один не выдуман"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-07. Объём (8 юрисдикций × факт-чек + контент) оценён как потенциально параллелизуемый, но в этой сессии не было доступного инструмента для in-process сабагентов (только тяжеловесные remote CCR-сессии через create_session, непригодные для тесно связанного контента с общими docs/project/*.md — риск git-конфликтов перевесил выигрыш по времени) — задача выполнена последовательно одним агентом с батчевой верификацией фактов (все 8 юридических ссылок проверены параллельными WebFetch/curl-вызовами перед написанием текста, не после). Локали: IT/IE/BE/SE/DK/FI/NO — 'en' (национальные языки этих стран не входят в A11Y_LOCALES=[en,de,fr,pl,es|]; английский — рабочий язык бизнес-комплаенса для этой группы, как уже прецедентно у european-accessibility-act-guide.md). AT — 'de' (немецкий поддерживается платформой и является естественным языком аудитории). CTA у всех восьми — `/scan/?country=XX` (не bfsg-check-стиль отдельная страница: уже есть рабочий генерик-сканер с юрисдикцией из query, JURISDICTION_OPTIONS во фронте покрывает все 8 кодов). Норвегия — особый случай, обработан честно: forskrift 2013 года НЕ 'транспозиция EAA' (Норвегия не член ЕС, forskrift старше директивы) — гайд прямо это объясняет и отдельно описывает, где EAA всё же достаёт норвежский бизнес (через реальную продажу услуг в ЕС, не через транспозицию). Финляндия — второй особый случай: закон 306/2019 сам по себе покрывает только госсектор, EAA-расширение на частный сектор — отдельный акт 2023/179 — оба процитированы, не смешаны в один. Ни одной суммы штрафа не добавлено (инвариант D-035 проверен grep). relatedAgencies отобраны предпочтением standard:'eaa' в agencies.json, с добором audit-способных агентств там, где eaa-тегированных <4 (SE/FI/NO имеют только 2-3 агентства в каталоге вообще — использованы все реальные, не растянуто до 4 выдумкой). Побочная находка вне scope (не исправлялась): audit-rgaa-guide.md и european-accessibility-act-guide.md (последний — country-by-country таблица, не тронутые мной DE/FR строки) содержат суммы штрафов (DE €100k, FR €50k/€25k) — эти файлы написаны ДО D-035 (2026-08-04/05, решение D-035 от 2026-08-06) и, в отличие от bfsg-pflichten-guide.md/anlage3, не были ретроактивно вычищены; зафиксировано как находка для будущей задачи, не исправлено самостоятельно (вне заявленного объёма этой итерации — риск нежелательного расширения scope)."

  - id: G-GUIDES-FINE-SWEEP
    title: "Зачистка сумм штрафов вопреки D-035 (audit-rgaa-guide.md, строка Германии в хабе) + постоянный гейт"
    owner: qa-engineer
    status: done
    depends_on: []
    inputs: [docs/project/DECISIONS.md (D-035), data/a11y/guides/audit-rgaa-guide.md, data/a11y/guides/european-accessibility-act-guide.md]
    scope:
      - data/a11y/guides/audit-rgaa-guide.md
      - data/a11y/guides/european-accessibility-act-guide.md
      - scripts/no-fine-amounts.test.mjs
    outputs: [scripts/no-fine-amounts.test.mjs]
    verify:
      - "npm run scripts:test — 21/21 (было 20)"
      - "негативная проверка: временно возвращена сумма в audit-rgaa-guide.md → тест упал с указанием файла и строки → откат → снова зелёный"
      - "npm run build-a11y/typecheck/worker:test/src:test/build/check-links/audit-a11y — все зелёные"
      - "живая проверка: суммы не попали в собранный dist/"
    max_attempts: 2
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-07 (D-058). Побочная находка из notes узла G-GUIDES-EAA-8, закрыта отдельной итерацией сразу после обнаружения, не отложена. Оба места переписаны без цифр (Arcom-процедура/§37 BFSG-Ordnungswidrigkeit + MLBF, с явным объяснением почему цифра не приводится), ссылка на блог юрфирмы taylorwessing.com заменена внутренней ссылкой на bfsg-pflichten-guide.md. Гейт scripts/no-fine-amounts.test.mjs — эвристика 'сумма + слово о санкции в одной строке', не ловит легитимный контент (порог микропредприятий, цены аудита в bitv-test-kosten-ablauf.md). Третий подряд случай того же дрейфа (D-035 сам, затем D-041) — постоянный гейт добавлен вместо очередного разового фикса."

  - id: G-GUIDES-VPAT-CLUSTER
    title: "VPAT-кластер: гайд связывает VPAT/ACR с EN 301 549 и WCAG 2.1 AA, объясняет когда VPAT нужен (US 508 + EU-контекст), отличает от Barrierefreiheitserklärung/RGAA déclaration"
    owner: data-engineer
    status: done
    depends_on: []
    inputs: [data/a11y/guides/vpat-acr-guide.md, data/a11y/guides/section-508-compliance-guide.md, data/a11y/guides/en-301-549-explained.md, data/a11y/guides/barrierefreiheitserklaerung-bfsg-anlage3.md, data/a11y/guides/audit-rgaa-guide.md, data/a11y/agencies.json]
    scope:
      - data/a11y/guides/vpat-en-301-549-mapping-guide.md
      - data/a11y/guides/vpat-acr-guide.md
      - data/a11y/guides/section-508-compliance-guide.md
      - data/a11y/guides/en-301-549-explained.md
    outputs:
      - "новый гайд vpat-en-301-549-mapping-guide.md + встречные ссылки из 3 существующих гайдов (анти-orphan) + исправленная дата черновика EN 301 549 V4 в en-301-549-explained.md"
    verify:
      - "npm run build-a11y — 245 агентств, 0 ошибок"
      - "npm run typecheck — чисто"
      - "npm run worker:test — 202/202, npm run src:test — 10/10, npm run scripts:test — 21/21 (без изменений в тестах)"
      - "npm run build — 398 страниц (было 397), sitemap 361 (было 360)"
      - "npm run check-links — 435 уникальных ссылок, 0 битых (было 434)"
      - "npm run audit-a11y — 25 страниц постоянного сэмпла, 0 нарушений"
      - "разовый axe-прогон новой страницы + 3 изменённых (не входят в постоянный сэмпл) — нашёл 1 нарушение (empty-table-header, пустой заголовок первой колонки в сравнительной таблице), исправлено, повторный прогон 4/4 чисто"
      - "живая перепроверка (не по коду, не по памяти): скачаны и распакованы сами .docx-шаблоны ITI (EU- и 508-редакция VPAT 2.5Rev) — структура Clause 4-13 EN 301 549, cross-reference на WCAG для Web/части Non-web Documents/Software подтверждена текстом самого шаблона; первичный PDF ETSI (не вторичные блоги) для статуса черновика EN 301 549 V4 — нашёл более новую версию (V4.1.0 2026-06 в фазе Vote), чем указанная в существующем en-301-549-explained.md (V4.1.0 ноябрь 2025) — исправлено как побочная находка в этой же итерации"
      - "relatedAgencies (tpgi/level-access/qualitylogic/usablenet) сверены скриптом против agencies.json — все существуют и реально покрывают оба standards: section-508 И en-301-549"
      - "grep + explicit no-fine-amounts.test.mjs прогон по новому файлу — сумм и слов о штрафе нет"
    max_attempts: 2
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-07 (D-059). Третий кусок G-GUIDES из меню, оставленного в notes G-GUIDES-EAA-8. Единственная связка (ACR-секция ↔ EN 301 549 clause ↔ WCAG) не существовала ни в одном из трёх смежных гайдов (vpat-acr-guide/section-508-compliance-guide/en-301-549-explained — все из базового импорта 2026-08-06, факты не проверялись живьём в этом проекте ни разу). Раздел про отличие от Barrierefreiheitserklärung/RGAA déclaration построен только на перекрёстных ссылках на уже существующие гайды (D-033/D-056/D-057), факты BFSG/RGAA не передублированы. standard:'section-508' — гайд встал на /standards/section-508/ рядом с vpat-acr-guide/section-508-compliance-guide через существующий guidesFor()-механизм StandardPage.tsx, без единой ручной правки списка."

  - id: A0-A11Y-STATEMENT
    title: "Собственное заявление о доступности AccessAtlas (/accessibility-statement/) — пробел найден: платное Featured через Stripe поднимает вопрос e-commerce-service по EAA для самого сайта"
    owner: product-lead
    status: done
    depends_on: []
    inputs:
      - worker/lib/statement.js
      - data/a11y/guides/barrierefreiheitserklaerung-bfsg-anlage3.md
      - docs/project/EN301549-COVERAGE.md
      - scripts/en301549-coverage.mjs
      - scripts/audit-own-a11y.mjs
      - src/pages/ImprintPage.tsx
    scope:
      - src/pages/AccessibilityStatementPage.tsx
      - src/routes.tsx
      - src/lib/data.ts
      - src/components/Layout.tsx
      - scripts/gen-a11y-sitemap.mjs
      - scripts/audit-own-a11y.mjs
    outputs: [src/pages/AccessibilityStatementPage.tsx]
    verify:
      - "npm run build-a11y — 245 агентств, 0 ошибок"
      - "npm run typecheck — чисто"
      - "npm run worker:test — 202/202, npm run src:test — 10/10, npm run scripts:test — 21/21 (page-lists.test.mjs ловит пропуск страницы в sitemap/audit-own-a11y — прошёл без изменений в самом тесте)"
      - "npm run build — 399 страниц (было 398), sitemap 362 URL (было 361)"
      - "npm run check-links — 437 уникальных ссылок, 0 битых (было 435)"
      - "npm run audit-a11y — 26 страниц (было 25, новая страница внутри постоянного сэмпла с первого дня, не разово), 0 нарушений"
      - "npm run en301549:coverage — запущен живьём непосредственно перед написанием текста статуса соответствия (31/50, 62%), число взято из coverageSummary (src/lib/coverage.ts), не вписано руками — не может разойтись с /methodology/"
      - "живая проверка встроенного HTML dist/accessibility-statement/index.html: числа 50/31/62% и текст 'Self-assessed'/'25 sampled pages' реально присутствуют в собранной странице, не только в JSX-исходнике"
      - "grep на €/EUR/Bußgeld/fine/penalty по новому файлу — нет ни одного (инвариант D-035); единственное совпадение '§' — параграф закона, не сумма"
      - "фактчек вживую (не по памяти прошлого чата): Art. 3(30) EAA и § 2 Nr. 26 BFSG — оба подтверждены WebFetch/WebSearch на первичный/вторичный текст, оба определяют e-commerce service через 'Verbrauchervertrag'/consumer contract"
    max_attempts: 2
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-07 (D-060). Пробел найден пользователем, не в BACKLOG/GRAPH ранее. Живой фактчек (WebFetch на eur-lex.europa.eu текст Art. 3(30)/3(22) EAA + WebSearch/WebFetch на §2 Nr.26 BFSG, gesetze-im-internet.de) скорректировал исходную посылку задачи: платные Featured-размещения продаются агентствам (бизнесам, действующим в рамках своей профессии) — это B2B, а не 'Verbrauchervertrag' с потребителем, который требует и Art. 3(30) EAA, и §2 Nr.26 BFSG для статуса 'e-commerce service'. Сама по себе продажа Featured эту классификацию строго не включает. Бесплатный сканер (/scan) — более сильный кандидат: доступен 'по индивидуальному запросу' любому посетителю без требования торговой цели. Страница не утверждает разрешённую классификацию как факт — честно называет обе стороны неопределённости и публикуется добровольно независимо от исхода (сама компания работает в сфере доступности — репутационный довод сильнее спорного юридического триггера). 3 из 5 пунктов Anlage 3 закрыты живыми фактами без выдумывания (описание услуги, правовая основа с оговоркой о юрисдикции, статус соответствия — из свежего audit-a11y/en301549-coverage). Оставшиеся 2 пункта (идентификация Anbieter по Art. 246 EGBGB, надзорный орган) ЗАВИСЯТ ОТ ОДНОГО И ТОГО ЖЕ БЛОКЕРА — A0-OWNER-LEGAL (какая страна регистрации, та и юрисдикция/орган) — не только идентификация, как в исходной формулировке задачи; орган MLBF назван как иллюстрация (используется в собственных гайдах проекта), явно не заявлен как 'наш'. Канал обратной связи переиспользует /contact/ (уже существующий реальный email), не изобретён новый. Опубликовано как done + индексируемо (не noindex, как /imprint/) — обоснование в DECISIONS.md D-060: страница сама по себе фактически полна и честна о своём единственном пробеле (тот же паттерн, что уже использует /privacy/, которая тоже ссылается на неполный /imprint/, но сама остаётся индексируемой)."

  - id: G-GUIDES-BFSG-MUSTER
    title: "Четвёртый (последний заявленный) кусок G-GUIDES BFSG-кластера: практическая Muster-Vorlage/чеклист публикации Barrierefreiheitserklärung"
    owner: product-lead
    status: done
    depends_on: []
    inputs:
      - data/a11y/guides/barrierefreiheitserklaerung-bfsg-anlage3.md
      - data/a11y/guides/bfsg-pflichten-guide.md
      - docs/project/DECISIONS.md
    scope:
      - data/a11y/guides/
    outputs:
      - "новый гайд-чеклист (Muster/Vorlage + практический checklist публикации) + встречные ссылки из barrierefreiheitserklaerung-bfsg-anlage3.md и bfsg-pflichten-guide.md (анти-orphan)"
    verify:
      - "node scripts/build-a11y.mjs — 245 агентств, 0 ошибок (baseline подтверждён 2026-08-07 перед стартом узла)"
      - "npm run typecheck — чисто"
      - "npm run worker:test — 202/202, npm run src:test — 10/10, npm run scripts:test — 21/21 (без изменений в тестах — задача контентная; baseline подтверждён 2026-08-07)"
      - "npm run build — 400 страниц (было 399, baseline подтверждён 2026-08-07), новый файл в dist/guides/"
      - "npm run check-links — >437 уникальных ссылок (было 437, baseline подтверждён 2026-08-07), 0 битых"
      - "npm run audit-a11y — 26 страниц постоянного сэмпла, 0 нарушений"
      - "разовый axe-прогон новой страницы + обеих изменённых существующих (не входят в постоянный сэмпл) — 0 нарушений"
      - "sitemap 363 URL (было 362, baseline подтверждён 2026-08-07), новый гайд внутри"
      - "grep + npm run scripts:test (no-fine-amounts.test.mjs) по новому файлу — сумм штрафов нет (инвариант D-035)"
      - "не дублировать пять пунктов Anlage 3 из barrierefreiheitserklaerung-bfsg-anlage3.md дословно — только ссылка + новый практический слой (образец формулировок / чек-лист публикации)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Заведён 2026-08-07 при отсутствии других незаблокированных узлов графа (все прочие: done, blocked на A0-OWNER-LEGAL, review в ожидании пассивного наблюдения, или todo но функционально заблокированы доменом — A2-OUTREACH-SEND). Прямое продолжение G-GUIDES-BFSG-STATEMENT/EAA-8/VPAT-CLUSTER (D-056/D-057/D-059) — единственный явно названный в HANDOFF.md/BACKLOG.md остаток G-GUIDES ('ещё BFSG-контент: Muster-Vorlage/чеклист'). Baseline-числа в verify (245/0, 399→400 стр., 362→363 sitemap, 437 ссылок, 21/21 scripts:test) сняты живым прогоном непосредственно перед постановкой узла, не предположены. Готово 2026-08-07: новый гайд `barrierefreiheitserklaerung-muster-checkliste.md` — Platzhalter-Muster для всех пяти пунктов Anlage 3 + 7-пунктовая Veröffentlichungs-Checkliste; пять пунктов не пересказаны дословно, только ссылка + практический слой (проверено вручную). Встречные ссылки добавлены в обе стороны (barrierefreiheitserklaerung-bfsg-anlage3.md, bfsg-pflichten-guide.md) — анти-orphan. Интеграция полностью автоматическая (src/lib/guides.ts глобит директорию, routes.tsx/gen-a11y-sitemap.mjs не тронуты). Все verify прошли: build-a11y 245/0, typecheck чисто, worker:test 202/202, src:test 10/10, scripts:test 21/21 (no-fine-amounts.test.mjs включая новый файл, без правок теста), build 400 страниц (было 399), sitemap 363 (было 362), check-links 438/0 битых (было 437), audit-a11y 26 страниц постоянного сэмпла 0 нарушений (не изменялся), отдельный разовый axe-прогон 3 страниц (новая + 2 изменённые, временный скрипт-копия harness'а audit-own-a11y.mjs, удалён после прогона) — 0 нарушений. grep €/EUR/Bußgeld/fine/penalty по новому файлу — 0 совпадений. Живой WebFetch-фактчек не проводился и не требовался verify этого узла — новых юридических фактов не добавлено, только практический слой поверх уже перепроверенных в D-056/D-060 фактов (Anlage 3, §16/17 BFSG, MLBF, Art. 246 EGBGB)."

  - id: G-GUIDES-RGAA-FR
    title: "FR-гайд под голое `rgaa` (8100/мес по DataForSEO 2026-08-08): что такое RGAA, 106 критериев, связь WCAG/EN 301 549, кому обязателен — информационный интент, без дублирования audit-rgaa-guide"
    owner: product-lead
    status: done
    depends_on: []
    inputs:
      - data/a11y/guides/audit-rgaa-guide.md
      - docs/project/domains/growth.md (keyword-рефреш 2026-08-08)
      - docs/project/DECISIONS.md (D-035 no-fines, D-058 прецедент RGAA-контента)
    scope:
      - data/a11y/guides/
    outputs:
      - "новый fr-гайд информационного интента (что такое RGAA) + встречные ссылки с audit-rgaa-guide.md (анти-orphan в обе стороны)"
    verify:
      - "юридические/технические факты (версия RGAA, число критериев, орган, связь с WCAG) сверены живьём с первоисточником (accessibilite.numerique.gouv.fr / DINUM), не по памяти модели"
      - "полный набор гейтов: build-a11y 245 / typecheck / worker:test / src:test / scripts:test (вкл. no-fine-amounts) / build / check-links / audit-a11y — зелёные"
      - "разовый axe-прогон новой страницы — 0 нарушений"
      - "sitemap: +1 URL, новый гайд внутри; <html lang=fr> в собранном dist, chrome честно английский (D-087)"
      - "не дублирует транзакционный audit-rgaa-guide.md — информационный интент, взаимные ссылки"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Заведён 2026-08-08 по данным keyword-рефреша DataForSEO (владелец дал API in-memory): голое `rgaa` 8100/мес @ $3.63 — largest gap спрос↔контент; существующий audit-rgaa-guide метит только в `audit rgaa` 390/мес. Модель узла — Sonnet (выбор владельца через AskUserQuestion, совет хука haiku отклонён). Готово 2026-08-08 (D-090): data/a11y/guides/rgaa-guide.md, реализован субагентом, перепроверен родительской сессией НЕЗАВИСИМО — гайд прочитан построчно, диф audit-rgaa-guide.md (1 строка, взаимная ссылка) прочитан, ВСЕ гейты перезапущены родителем (build-a11y 245 / typecheck / scripts:test 48/48 вкл. no-fine-amounts / worker:test 211 / src:test 20 / build 451 стр. sitemap 414 / check-links 500-0 / audit-a11y 47-0), разовый axe новой страницы (свой скрипт, не субагента) 0 нарушений, <html lang=fr> + английский chrome (aria-label=Main) в dist подтверждены. Фактчек-находка родителя: «18 mois на обновление декларации» отсутствовал в списке проверенного субагентом — подтверждён родителем на официальной странице DINUM déclaration-accessibilité дословно, факт верен. RGAA 4.1.2 и «RGAA 5 fin 2026» перепроверены родителем на homepage DINUM дословно."

  - id: CN-HERO
    title: "Scanner-first главная (конституция §7-8, §59): функциональный hero с URL-вводом, каталог ниже"
    owner: frontend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/DESIGN_CONSTITUTION.md (§7-8, §37, §59), src/pages/HomePage.tsx, src/pages/ScanPage.tsx, src/lib/scanner.ts]
    scope: [src/pages/HomePage.tsx, src/components/, src/lib/data.ts]
    outputs: [src/pages/HomePage.tsx]
    verify:
      - "npm run build / typecheck / audit-a11y (главная в постоянном сэмпле) — зелёные"
      - "hero: URL-ввод + CTA 'Scan website' + микрокопия 'Free instant scan. No signup required.' (§59); отправка ведёт на существующий scan-поток, НЕ дублирует его логику"
      - "каталог и доверие (245 записей, evidence) остаются на главной ниже hero — §8 порядок секций"
      - "без fear-копирайтинга (D-035), без выдуманных счётчиков — 'sites scanned today' только если реально считается из D1, иначе не показывать"
      - "живой прогон Playwright: ввод URL на главной реально доводит до /report/:id (мокнутый API), состояние ошибки валидации видно"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Заведён 2026-08-07 (D-062) как первая и самая рычажная дельта конституции: сканер уже реально задеплоен, домена не требует, меняет подачу продукта с 'каталог с кнопкой скана' на 'платформа со сканером на входе'. Готово 2026-08-07 (D-063): hero дословно по §7 ('Check your website accessibility' / 'Scan website' / 'Free instant scan. No signup required.'), логика отправки НЕ продублирована — общий useScanForm (src/components/ScanForm.tsx) для hero и /scan/, валидация тот же isValidScanUrl. Fear-заголовок '…before the deadline finds you' снят (D-035/§57). 'Sites scanned today' не показывается — реального агрегата из D1 нет; stat-ряд только из чисел сборки (245/19/121/31из50). Каталог и оба trust-блока ниже hero (§8). Все verify прошли: build 400 стр., typecheck чисто, audit-a11y 26 стр. 0 нарушений, живой Playwright — невалидный URL даёт видимый role=alert, валидный доводит до /report/demo123 на мокнутом API, фокус-кольцо 2px видно при Tab. Скриншоты before/after — scratchpad design/."

  - id: CN-NAV
    title: "IA-переименования (конституция §6): 'Guides' → 'Knowledge', 'All agencies' → 'Experts' — только UI-ярлыки, не URL"
    owner: frontend-engineer
    status: done
    depends_on: [CN-HERO]
    inputs: [docs/project/DESIGN_CONSTITUTION.md (§6), src/components/Layout.tsx, src/pages/GuidesIndexPage.tsx, src/pages/TierPages.tsx]
    scope: [src/components/Layout.tsx, src/pages/]
    outputs: [src/components/Layout.tsx, src/pages/GuidesIndexPage.tsx, src/pages/GuidePage.tsx, src/pages/AgencyPage.tsx, src/pages/TierPages.tsx]
    verify:
      - "npm run build / typecheck / check-links / audit-a11y — зелёные; ярлыки видны в собранном dist/"
      - "URL /guides/* и /agencies/* НЕ изменены; модель данных agencies не тронута (D-062)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Первая часть (Scan — первый акцентный пункт шапки) сделана в D-063; остаток закрыт 2026-08-07 (D-065): ярлыки Knowledge/Experts в шапке, h1 обеих индекс-страниц и breadcrumbs. Смена URL на /knowledge/ и /experts/ — сознательно НЕ сделана, отдельное будущее решение владельца (если делать — только 301 + sitemap/page-lists). Прозаические упоминания guides/agencies в контенте оставлены — тип контента, не пункт навигации."

  - id: CN-TOKENS
    title: "Дизайн-токены/типографика (конституция §24-29): нейтральная база, один акцент, семантические severity-цвета, фокус, tabular-nums"
    owner: frontend-engineer
    status: done
    depends_on: [CN-HERO]
    inputs: [docs/project/DESIGN_CONSTITUTION.md (§24-29, §37-38, §57), src/styles.css, src/pages/ReportPage.tsx]
    scope: [src/styles.css, src/pages/, src/components/, docs/project/domains/design.md]
    outputs: [src/styles.css, docs/project/domains/design.md]
    verify:
      - "npm run build / typecheck / worker:test / src:test / scripts:test — зелёные"
      - "npm run audit-a11y — 26 страниц 0 нарушений: контраст новой палитры проверен axe живьём (главный риск итерации)"
      - "severity-палитра согласована со шкалой impact axe-core (scanner.ts), все пары text-on-soft ≥4.5:1 (посчитано скриптом)"
      - "фокус-кольцо видимо всегда: последние focus:outline-none сняты (LeadForm, ScanPage), Playwright подтвердил outline 2px при Tab"
      - "шрифты self-hosted (Inter Variable уже в бандле) или system-stack — CDN нет; tabular-nums (.num) на числах-метриках"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Заведён и закрыт 2026-08-07 одной итерацией с CN-HERO (D-063, первая дизайн-итерация по запросу владельца 'нужно дополнить UX и дизайн'). Решения и обоснования: DECISIONS.md D-063; источник правды системы — docs/project/domains/design.md (палитра hex+назначение, типографика, spacing, фокус, severity-семантика, анти-паттерны §57). База — slate как есть, акцент один #4338ca, severity: critical #b91c1c / serious #9a3412 / moderate #854d0e / minor slate + success/info. Кастомный моно-шрифт сознательно не введён (system-stack), tabular-nums через .num. ReportPage severity-чипы переведены с chip-accent на chip-{impact}."

  - id: CN-BRANDBOOK
    title: "Применить brand book + Stitch-макеты владельца (D-072): токены обеих тем, JetBrains Mono, pill/компакт-форма, dark полноценно"
    owner: frontend-engineer
    status: done
    depends_on: [CN-TOKENS]
    inputs: [scratchpad stitch/stitch_premium_web_studio_design (31 макет, code.html+screen.png), docs/project/BRAND_BOOK.md, docs/project/DESIGN_CONSTITUTION.md (§24-29, §28), docs/project/domains/design.md]
    scope: [src/styles.css, src/main.tsx, src/pages/, src/components/, scripts/audit-own-a11y.mjs, docs/project/BRAND_BOOK.md, docs/project/domains/design.md, package.json]
    outputs: [src/styles.css, docs/project/BRAND_BOOK.md, docs/project/domains/design.md, scripts/audit-own-a11y.mjs]
    verify:
      - "токены обеих тем — точные значения из code.html макетов (не выдуманы); severity-пары подобраны и посчитаны скриптом ≥4.5:1 на background/surface/soft в СВОЕЙ теме — ✅ contrast-d072.mjs, все ok"
      - "ни один цвет не живёт только в dark-ветке: dark-блок лишь переопределяет @theme; sweep slate/amber/indigo из src/ полный (0 остатков) — ✅"
      - "audit-own-a11y гоняет ВСЕ шаблоны в ОБЕИХ темах; 36 стр. × 2 = 0 нарушений; dark-проход доказан провал-способным (канарейка → 8 [dark]-маршрутов красные, light нетронут) — ✅"
      - "Playwright обе темы: hero-сабмит → отчёт (мок форм воркера), фокус-кольцо = primary своей темы, модалка/табы/аккордеон/тост, scan-error — ✅ 16/16"
      - "полный набор: build-a11y 245 / typecheck / worker:test 211 / src:test 20 / scripts:test 37 / build / check-links 487-0 — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Заведён и закрыт 2026-08-08 (D-072) по прямому указанию владельца «Примени brand book и дизайн из этих примеров». Палитра D-063 superseded, принципы сохранены. Спецификация извлечена в BRAND_BOOK.md (сами 8.7MB макетов в репо не тащим). Сознательные отклонения от макетов перечислены в D-072 (Material Symbols/CDN, dark class-toggle, выдуманная телеметрия, фиктивные числа). pitch_deck_* — отдельный артефакт, узел не заводился, todo-строка G-PITCH-DECK в BACKLOG.md ждёт решения владельца о формате."

  - id: CN-COMPONENTS
    title: "Публичная библиотека доступных компонентов (§22) — ПЕРВЫЙ КУСОК: каркас + 3 из 13"
    owner: frontend-engineer
    status: done
    depends_on: [CN-TOKENS]
    inputs: [docs/project/DESIGN_CONSTITUTION.md (§22, §24-29, §57), docs/project/domains/design.md, src/pages/GuidePage.tsx, src/pages/WcagCriterionPage.tsx, src/lib/guides.ts, src/lib/wcag.ts]
    scope: [data/a11y/components.json, src/components/library/, src/lib/componentsLib.tsx, src/pages/ComponentsIndexPage.tsx, src/pages/ComponentPage.tsx, src/routes.tsx, src/lib/data.ts, src/components/Layout.tsx, src/pages/GuidesIndexPage.tsx, src/styles.css, scripts/audit-own-a11y.mjs, scripts/gen-a11y-sitemap.mjs, scripts/components.test.mjs]
    outputs: [data/a11y/components.json, src/components/library/{Accordion,Tabs,Modal,CopyButton,CodeBlock}.tsx, src/lib/componentsLib.tsx, src/pages/ComponentsIndexPage.tsx, src/pages/ComponentPage.tsx, scripts/components.test.mjs]
    verify:
      - "build:data / typecheck / worker:test 211 / src:test 20 / scripts:test 32 / build 436 / check-links 481-0 — зелёные"
      - "audit-a11y 33 стр. 0 нарушений: индекс + все 3 страницы компонентов в ПОСТОЯННОМ сэмпле; открытая модалка аудитируется отдельно (INTERACT-хук кликает [data-a11y-demo-open] и гоняет axe по role=dialog)"
      - "живой Playwright: accordion Enter-toggle + панель в/из DOM; tabs Arrow/Home/End двигают выбор И фокус (roving); modal focus trap (6× Tab внутри) + Esc + возврат фокуса на триггер — 20/20"
      - "каждый живой пример реально работает клавиатурой и мышью (не картинка); код в примерах — реальный исходник примитива (?raw), кнопка Copy реально копирует"
      - "prefers-reduced-motion: анимация rise модалки и поворот chevron сняты под reduce; фокус-кольца видимы (D-063)"
      - "порог честности: 10 незаделанных — явный Planned без страницы/ссылки; components.test.mjs гейтит инварианты и согласованность с sitemap/аудитом"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Заведён и закрыт 2026-08-08 (D-068, четвёртая дизайн-итерация). ПЕРВЫЙ КУСОК по явному требованию владельца: образцовый каркас + 3 фундаментальных компонента (Accordion/aria-expanded, Tabs/roving-tabindex+arrows, Modal/focus-trap+Esc+возврат), а не 13 наспех. Конвенция как guides/wcag: данные в data/a11y/components.json → страницы; готовую страницу имеют только ready-компоненты; код показывается через ?raw-исходник примитива (не дрейфует). Гейт audit-a11y СРАЗУ поймал 3 реальных нарушения (slate-400 2.63:1, scrollable pre без фокуса, opacity-анимация модалки → транзиентный контраст 1.1) — починены (см. D-068). Иконки: введена одна stroke-SVG система (§29). Остаток (узел CN-COMPONENTS-REST) закрыт 2026-08-08 (D-084, Data table, последний из 10) — вся инициатива §22 завершена, библиотека 13/13 готовых компонентов."

  - id: CN-COMPONENTS-REST
    title: "Остаток библиотеки §22: 10 компонентов, по одному образцовому за раз"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, src/components/library/, docs/project/DESIGN_CONSTITUTION.md (§22)]
    scope: [data/a11y/components.json, src/components/library/, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs]
    outputs: [data/a11y/components.json, src/components/library/]
    verify:
      - "каждый новый компонент: живой пример проходит axe в постоянном audit-a11y + Playwright на его клавиатуру; components.test.mjs зелёный (ready → полный набор §22 + файл реализации) — ✅ все 10/10"
      - "страница появляется ТОЛЬКО когда пример реален (планка та же); planned-запись до тех пор без impl/страницы (R1) — соблюдено на всех 10"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-08. Заведён (D-068) как явный остаток CN-COMPONENTS, было 10 planned. Закрыт последовательно, по одному за раз, каждый — выбор владельца через AskUserQuestion (узел + модель): Toast (D-070) → Tooltip (D-074) → Breadcrumbs (D-077) → Combobox (D-078, Opus, самый рискованный композит) → Menu button (D-079, Sonnet) → Select/listbox (D-080, Sonnet) → Form field (D-081, Sonnet, единственный извлечённый из уже работающего LeadForm.tsx, не написанный с нуля) → Switch (D-082, Sonnet, простейший — ловушка закрыта по конструкции) → Pagination (D-083, Sonnet, не именованный APG-паттерн) → **Data table (D-084, Sonnet, последний — сортировка + aria-sort + live-объявления)**. Библиотека §22 — **13/13 готовых компонентов** (3 исходных Accordion/Tabs/Modal + эти 10). Каждый компонент перепроверен родительской сессией живьём (не по заявлению субагента): диф построчно + полный набор гейтов + независимый Playwright-скрипт, написанный заново, не переиспользованный от субагента. Четыре раза за серию независимая проверка ловила баг в СВОЁМ скрипте, не в компоненте — см. D-079/D-080/D-082/D-083."

  - id: CN-RESEARCH
    title: "Честные data-продукты (§23/§45) на уникальных данных — из самого каталога, не бенчмарк чужих сайтов"
    owner: growth-strategist
    status: in_progress
    depends_on: []
    inputs: [data/a11y/agencies.json, data/a11y/taxonomies.json, docs/project/DESIGN_CONSTITUTION.md (§23,§43,§45)]
    scope: [scripts/reports-data.mjs, scripts/reports-data.test.mjs, data/a11y/reports.json, src/lib/reports.ts, src/pages/ReportsIndexPage.tsx, src/pages/ReportDocPage.tsx, src/routes.tsx, src/lib/data.ts, src/components/Layout.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs]
    outputs: [data/a11y/reports.json, src/pages/ReportDocPage.tsx]
    verify:
      - "все числа отчёта считаются скриптом из agencies.json (не руками); reports-data.test.mjs пересчитывает и требует совпадения со снапшотом — ✅ доказан негативно (правка total→красный, откат→зелёный)"
      - "отчёт назван честно как разбор НАШЕГО каталога, не бенчмарк чужих сайтов; никаких выдуманных site-scores (D-045/D-047) — ✅ блок What this is / is not"
      - "индекс + отчёт в audit-own-a11y (36-0) и sitemap; JSON-LD Dataset+Report; CTA; анти-orphan (Reports в навигации) — ✅"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Заведён и первый кусок закрыт 2026-08-08 (D-071): /reports/verified-audit-market + индекс /reports/. Раньше бэклог помечал CN-RESEARCH как depends_on A0-DEPLOY — снято: отчёт на СВОИХ данных не требует деплоя, работа разблокирована независимо от блокера владельца (как G-GUIDES, D-056). НЕЛЬЗЯ делать бенчмарк топ-100 чужих сайтов / accessibility index — корпуса сканов нет (D-010), это потребовало бы выдуманных цифр (D-045/D-047). Реальный уникальный датасет — сам каталог (245 записей): страны/стандарты/типы декларантов/доля с проверяемым доказательством. **Второй кусок закрыт 2026-08-08 (D-085, узел CN-RESEARCH-EN301549-AUTOMATION)**: /reports/en301549-automation-coverage/ на данных en301549-coverage.json (не agencies.json — свежий источник), потребовал рефакторинга reports.ts/ReportDocPage.tsx на диспетчер по slug, первый отчёт остался байт-в-байт тем же (проверено родительской сессией независимо). Узел CN-RESEARCH остаётся открытым — следующий data-продукт по новому решению направления, не автопродолжение."

  - id: CN-RESEARCH-EN301549-AUTOMATION
    title: "Второй data-продукт: покрытие EN 301 549 автоматизацией — что проверка может и не может доказать"
    owner: growth-strategist
    status: done
    depends_on: []
    inputs: [data/a11y/en301549-coverage.json, src/lib/wcag.ts, scripts/reports-data.mjs, scripts/reports-data.test.mjs, src/lib/reports.ts, src/pages/ReportDocPage.tsx, src/pages/ReportsIndexPage.tsx]
    scope: [scripts/en301549-report-data.mjs, scripts/en301549-report-data.test.mjs, data/a11y/en301549-report.json, src/lib/reports.ts, src/pages/ReportDocPage.tsx, src/pages/ReportsIndexPage.tsx, scripts/audit-own-a11y.mjs]
    outputs: [data/a11y/en301549-report.json, src/pages/ReportDocPage.tsx]
    verify:
      - "новый аггрегатор (отдельный файл scripts/en301549-report-data.mjs, scripts/reports-data.mjs/.test.mjs/reports.json НЕ тронуты — подтверждено git diff --stat = пусто) считает: 20 axe / 8 ours / 3 both / 19 none из 50, группировка по WCAG POUR-принципу (Perceivable 20/Operable 17/Understandable 10/Robust 3, сумма = 50); отдельный тест-гейт 4/4, доказан негативно РОДИТЕЛЬСКОЙ сессией самостоятельно (испорчен byStatus.axe в снапшоте → красный с точным диффом → восстановлено побайтово → снова зелёный) — ✅"
      - "существующий отчёт verified-audit-market НЕ изменился — родительская сессия собрала dist/ ДО и ПОСЛЕ изменений независимо и сравнила /reports/verified-audit-market/index.html побайтово (нормализуя только хэши JS/CSS-бандлов и SSG build-hash) — содержательно идентичен; reports-data.test.mjs 4/4 зелёный без изменений в самом файле — ✅"
      - "новая страница отчёта /reports/en301549-automation-coverage/: честная рамка 'what this is/is not', JSON-LD Dataset+Report, ссылки на /methodology/ и /wcag/, заявленное совпадение цифры 31/50 с /methodology/ и /accessibility-statement/ подтверждено чтением src/lib/coverage.ts — та же формула (status !== 'none'), не совпадение по написанному тексту; индекс /reports/ показывает оба отчёта — ✅"
      - "npm run audit-a11y 46 стр. 0 нарушений / typecheck чисто / build 449 стр. sitemap 412 / check-links 497-0 / scripts:test 41/41 / worker:test 211/211 / src:test 20/20 — все зелёные, перепроверено родительской сессией заново — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-08 (D-085). Заведён по выбору владельца через AskUserQuestion (после закрытия CN-COMPONENTS-REST 10/10) — второй data-продукт, НЕ пересчёт того же agencies.json другими словами: свежий источник (en301549-coverage.json, уже питает /wcag/[criterion], D-066), никогда не собирался в отдельный отчёт-нарратив. Архитектурный вызов решён явно: src/lib/reports.ts/ReportDocPage.tsx были жёстко завязаны на ОДИН отчёт — рефакторены на диспетчер по slug (REPORT_BODIES, тот же приём, что DEMOS в componentsLib.tsx), каждый отчёт несёт свой типизированный stats-экспорт и свой аггрегатор/гейт. Единственная тонкость, которую субагент сам поймал и задокументировал: дедлайн первого отчёта («computed from N catalog records») оставлен статичным JSX-текстом, не интерполяцией через общий пропс — интерполированная версия добавила бы лишний React hydration comment-маркер и сломала бы побайтовую идентичность SSR-вывода. Позиционирование: подтверждает уже принятую честность проекта про границы автоматизации (D-035/D-045) — первый раз собранная в отдельный публикуемый data-продукт, с той же цифрой 31/50, что уже называют /methodology/ и /accessibility-statement/ (не более дружелюбной)."

  - id: CN-RESEARCH-JURISDICTION-COVERAGE
    title: "Третий data-продукт: где закон требует специалиста, а в каталоге его нет — синтез agencies.json + worker/lib/jurisdiction.js"
    owner: growth-strategist
    status: done
    depends_on: []
    inputs: [data/a11y/agencies.json, worker/lib/jurisdiction.js, scripts/reports-data.mjs, src/lib/reports.ts, src/pages/ReportDocPage.tsx]
    scope: [scripts/jurisdiction-report-data.mjs, scripts/jurisdiction-report-data.test.mjs, data/a11y/jurisdiction-report.json, src/lib/reports.ts, src/pages/ReportDocPage.tsx, src/pages/ReportsIndexPage.tsx, scripts/audit-own-a11y.mjs]
    outputs: [data/a11y/jurisdiction-report.json, src/pages/ReportDocPage.tsx]
    verify:
      - "новый аггрегатор ИМПОРТИРУЕТ worker/lib/jurisdiction.js напрямую через supportedJurisdictions()+jurisdictionForUrl() (публичный TLD-lookup, не новый экспорт) — родительская сессия независимо подтвердила все 13 TLD==код страны через отдельный живой прогон; НЕ дублирует список законов вручную — ✅"
      - "для каждой из 13 юрисдикций — сколько агентств каталога реально обслуживают эту страну (countriesServed, исключая remote-*); честная находка: **0 из 13 юрисдикций с нулевым покрытием** (DE максимум — 42, FI/NO минимум — по 2) — заголовок скорректирован субагентом под реальный результат вместо предполагавшегося 'has none yet' — ✅"
      - "НИ ОДНОЙ суммы штрафа — подтверждено родительской сессией отдельным grep (все совпадения ложные: fileURLToPath/European/названия законов/сам текст об отсутствии сумм); постоянный автоматический тест на это же (word-boundary regex, не путает 'fine' с 'Finland') — ✅"
      - "существующие ДВА отчёта не изменились — родительская сессия независимо пересобрала dist/ до/после (git stash -u) и сравнила ОБЕ страницы побайтово — содержательно идентичны; npm run audit-a11y 47 стр. 0 нарушений / typecheck / build 450 стр. sitemap 413 / check-links 498-0 / scripts:test 48/48 (было 41) / worker:test 211/211 / src:test 20/20 — все перепроверены заново, зелёные — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-08 (D-086). Заведён сразу после D-085 (второй отчёт), по выбору владельца через AskUserQuestion: свежих источников почти не осталось, поэтому владелец выбрал НОВЫЙ УГОЛ — синтез agencies.json + worker/lib/jurisdiction.js (юр-факты уже перепроверены по первоисточникам, D-030…D-034, НЕ новое юридическое исследование этим узлом). Технический приём: jurisdiction.js — plain ESM JS без браузерных API, build-time Node-скрипт импортирует его напрямую (в отличие от src/lib/jurisdictions.ts — та копия для клиентского бандла /scan, другая причина дублирования). Гейт `jurisdiction-report-data.test.mjs` (7 тестов) — самый строгий из трёх аггрегаторов серии: помимо снапшота проверяет независимый пересчёт agencyCount прямо из agencies.json (не доверяя даже своему же аггрегатору) и что ровно DE verified:true. Урок для LEARNING_LOG: заголовок отчёта писался ПОСЛЕ подсчёта, не до — гипотеза 'юрисдикции без специалистов' не подтвердилась (0 из 13), и заголовок честно заменён на то, что показали данные, а не подогнан под предположение."

  - id: G-I18N-CHROME-DE
    title: "G-I18N первый безопасный кусок: немецкий chrome (шапка/футер) на уже немецких страницах, без новых URL"
    owner: frontend-engineer
    status: done
    depends_on: []
    inputs: [src/components/Layout.tsx, src/pages/BfsgCheckPage.tsx, src/pages/GuidePage.tsx, src/lib/guides.ts, data/a11y/types.ts]
    scope: [src/lib/i18n.ts, src/components/Layout.tsx, src/pages/BfsgCheckPage.tsx, src/pages/GuidePage.tsx]
    outputs: [src/lib/i18n.ts, src/components/Layout.tsx]
    verify:
      - "полный сайт (450 страниц) до и после — ПОЛНОЕ дерево dist/ сравнено родительской сессией независимо (пересборка через git stash -u, diff -rq всего dist/): ровно 6 HTML-файлов реально отличаются — /bfsg-check/ + 5 немецких гайдов (barrierefreiheitserklaerung-bfsg-anlage3, barrierefreiheitserklaerung-muster-checkliste, bfsg-pflichten-guide, bitv-test-kosten-ablauf, eaa-austria-guide), совпадает с независимым grep по locale:de; ВСЕ ~440 остальных страниц побайтово идентичны — ✅"
      - "на /bfsg-check/ и немецких гайдах: реально прочитан собранный HTML родительской сессией — <html lang=\"de\">, aria-label Hauptnavigation/Brotkrümelnavigation/Rechtliches, nav Scan/Länder/Leistungen/Normen/Wissen/Komponenten/Berichte/Experten, футер на немецком; на fr-гайде отдельно проверено — <html lang=\"fr\"> сохранён, а chrome остался английским (aria-label=Main, honest fallback, не притворяется переводом) — ✅"
      - "sitemap.xml — 413 URL, не изменилось; audit-a11y 47 стр. 0 нарушений вкл. /bfsg-check/ — ✅"
      - "npm run typecheck / build / check-links / scripts:test 48/48 / worker:test 211/211 / src:test 20/20 — все зелёные, числа как до узла — ✅"
    max_attempts: 3
    attempts: 1
    risk: high
    approval_required: false
    notes: "Готово 2026-08-08 (D-087). Заведён по прямому запросу владельца «возьми G-I18N» — полный масштаб (UI на 4 языках + роутинг + hreflang + перевод сотен описаний/гайдов) оценён и честно назван неподъёмным для одной итерации (удвоение сайта до ~900 страниц при роутинге /de/). Владелец сузил до безопасного первого куска через AskUserQuestion дважды. Реализовано субагентом на Sonnet, результат перепроверен родительской сессией живьём. Субагент сам нашёл и починил тонкий баг ДО того, как показать результат: наивная реализация (Layout ставит <html lang> через собственный НОВЫЙ <Head>-блок как сиблинг) сдвигала бы позиционные индексы всех детей общего Fragment'а, а с ними — React.useId() у ЛЮБОГО интерактивного компонента на ЛЮБОЙ странице (Accordion/Combobox/формы), меняя DOM id при каждой пересборке без функциональной причины. Починка: <html lang> рендерится ВНУТРИ уже существующего <Head> компонента Meta (src/lib/seo.tsx), не как новый сиблинг в Layout — один источник правды, ноль сдвига позиций. Также разведены locale (язык ШАПКИ/футера, en/de) и htmlLang (язык ДОКУМЕНТА, полный набор существующих локалей) — иначе fr/pl-гайды тихо потеряли бы верный <html lang>, откатившись на 'en' вслед за отсутствующим chrome-словарём. aria-label ориентиров (Main/Breadcrumb/Legal) тоже переведены — по духу задачи «полноценный немецкий chrome» касается не только видимого текста, но и того, что слышит пользователь скринридера. risk:high оправдан — Layout.tsx рендерится на каждой из 450 страниц. Остаток G-I18N (роутинг /de/, hreflang, перевод контента на fr/pl/es) — отдельные будущие решения, каждое требует нового scoping-разговора с владельцем, не автопродолжение."

  - id: CN-WCAG22
    title: "Самопроверка сайта до WCAG 2.2 AA (конституция §40): target-size в audit-own-a11y"
    owner: qa-engineer
    status: done
    depends_on: []
    inputs: [scripts/audit-own-a11y.mjs, node_modules/axe-core (4.13), src/pages/AccessibilityStatementPage.tsx]
    scope: [scripts/audit-own-a11y.mjs, src/pages/AccessibilityStatementPage.tsx, docs/project/domains/design.md]
    outputs: [scripts/audit-own-a11y.mjs]
    verify:
      - "канарейка: посаженное нарушение target-size (16x16px кнопка) ловится новым конфигом и НЕ ловится старым — гейт умеет провалиться"
      - "npm run build && npm run audit-a11y — 26 страниц 0 нарушений с включённым target-size"
      - "npm run typecheck / check-links — зелёные"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-07 (D-064). Находка: дефолтный axe.run() НЕ гонял target-size — единственное wcag22aa-правило axe-core 4.13 поставляется с enabled:false; самопроверка молча была 2.0/2.1 AA + best practices. Включено явно через rules:{'target-size':{enabled:true}}; runOnly:{tags} сознательно не использован (снял бы best-practice-правила гейта). Канарейка подтвердила силу проверки (старый конфиг молчит, новый ловит), 26 страниц — 0 нарушений, чинить не пришлось: интерактивные цели сайта уже >=24px. Обновлены /accessibility-statement/ (rule set назван явно) и domains/design.md §7."

  - id: CN-SCAN-STREAM
    title: "Deploy-подобный поток состояний скана (конституция §10) — только реальные состояния API"
    owner: frontend-engineer
    status: done
    depends_on: []
    inputs: [worker/routes/scan.js, worker/lib/db.js, src/pages/ReportPage.tsx, src/lib/scanner.ts, docs/project/INTERFACES.md]
    scope: [src/lib/scanStream.ts, src/components/ScanStream.tsx, src/pages/ReportPage.tsx, src/styles.css]
    outputs: [src/lib/scanStream.ts, src/components/ScanStream.tsx]
    verify:
      - "шаги строятся ТОЛЬКО из полей GET /api/scan/:id (running|done|error, createdAt, completedAt, pages, findings) — проверено чтением worker/lib/db.js, тест src/lib/scanStream.test.mjs фиксирует ровно 3 шага"
      - "живой Playwright с мокнутым API: очередь -> running (elapsed тикает) -> done; error; not-found — 17 проверок зелёные"
      - "prefers-reduced-motion: reduce -> animation none (плюс контроль, что без reduce анимация живая); axe на running/error состояниях 0 нарушений"
      - "npm run src:test / typecheck / build — зелёные"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-07 (D-064). Сначала проверен реальный контракт: воркер пишет только running->done|error одним UPDATE, пофазного прогресса нет — поэтому 3 честных шага (requested/scanning/report), а не фейковые фазовые таймеры (тот же класс запрета, что D-035/D-045, только в UI). Побочно снята существовавшая ложь: 'Scanned N pages so far' всегда показывал 0. Elapsed — от серверного createdAt (переживает перезагрузку), длительность done-скана — только серверные метки. Доступность: статус шага формой+текстом, не цветом; тикающий elapsed вне live-региона; единый sr-only live-регион в ReportPage; ошибка не тупик (Run a new scan, §38). Скриншоты: scratchpad design/scan-stream-{running,done,error}.png."

  - id: CN-WCAG-PAGES
    title: "SEO-поверхность /wcag/[criterion] (конституция §20/§43) из реальных данных en301549-coverage.json"
    owner: frontend-engineer
    status: done
    depends_on: []
    inputs: [data/a11y/en301549-coverage.json, src/lib/coverage.ts, docs/project/DESIGN_CONSTITUTION.md (§20, §43-44)]
    scope: [src/lib/, src/pages/, src/routes.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs, scripts/]
    outputs: [src/lib/wcag.ts, src/pages/WcagIndexPage.tsx, src/pages/WcagCriterionPage.tsx, scripts/wcag-pages.test.mjs]
    verify:
      - "полный набор зелёный; audit-a11y включает /wcag/ + 2 представителя шаблона"
      - "порог thin-content (status !== 'none') реализован в страницах И sitemap; гейт wcag-pages.test.mjs доказан негативно"
      - "вся фактура из данных/кода воркера — ничего не выдумано (уровень A/AA не показывается: его нет в данных)"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Закрыт 2026-08-07 (D-066): 31 страница + индекс, 433 файла сборки (было 401), sitemap 395 (было 363), check-links 474-0, audit-a11y 29 стр. 0 нарушений. Скриншоты: scratchpad design/wcag-{index,1-3-1-axe,1-4-10-ours}.png."

  - id: CN-SCAN-PHASES
    title: "НАСТОЯЩИЙ пофазный стрим скана (discovering/statement/axe/dom-checks/aggregating) — расширение контракта API"
    owner: backend-engineer
    status: done
    depends_on: [CN-SCAN-STREAM]
    inputs: [worker/lib/axe.js, worker/routes/scan.js, worker/lib/db.js, docs/project/INTERFACES.md, src/lib/scanStream.ts]
    scope: [worker/, migrations/, src/lib/, src/components/ScanStream.tsx, docs/project/INTERFACES.md]
    outputs: [migrations/0007_scan_progress.sql, worker/lib/progress.js, worker/lib/db.js, worker/lib/axe.js, src/lib/scanner.ts, src/lib/scanStream.ts, docs/project/INTERFACES.md]
    verify:
      - "воркер пишет промежуточный прогресс в D1 по ходу scanSite, GET /api/scan/:id отдаёт его — ✅ живой прогон wrangler dev --local: 8 прямых sqlite-чтений с непустым progress_json в середине скана, финал перезаписал в NULL"
      - "изменение контракта записано в DECISIONS.md (D-067) и INTERFACES.md §3/§4 — ✅"
      - "UI-шаги в ScanStream только из реальных полей, ни один не выдуман; fallback на трёхшаговый поток без progress — ✅ тесты"
      - "worker:test 211/211 (+db.test.mjs, progress.test.mjs), src:test 20/20 — ✅"
      - "D-088 (2026-08-08): реально задеплоено на прод — 5 миграций (0003-0007) применены на реальной D1 accessatlas-scans, worker deploy успешен (Version ID bf8087e6). Живой прогон против https://www.w3.org/WAI/ с countryCode=DE поймал реальные фазы discovering(0)→statement(6)→axe(1..5/6)→dom-checks в GET /api/scan/:id, финал сбросил progress в null. Попутно подтверждён живьём D-040 (juridiction-note reaching a11y-statement-incomplete): impact критический, jurisdictionCountry:DE, jurisdictionNote с точной статьёй BFSG — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: true
    notes: "Заведён 2026-08-07 (D-064), код полностью готов 2026-08-07 (D-067). **Причина блокировки уточнена 2026-08-08 (D-069):** разрешение владельца на деплой ЕСТЬ (D-022, «делай всё»); блок был ТЕХНИЧЕСКИЙ и локальный для среды — из тогдашнего окружения `worker:deploy` был невозможен (нет CLOUDFLARE_API_TOKEN). **Закрыт 2026-08-08 (D-088):** владелец выдал CLOUDFLARE_API_TOKEN in-memory прямо в чат (никогда не записан на диск/в коммит), деплой и миграции выполнены реально, проверено живым сканом на проде. ALLOWED_ORIGIN в wrangler.jsonc остаётся плейсхолдером — деплой не чинит CORS каталога (нужен A0-ORIGIN, отдельный блокер). Локальный Browser Rendering в песочнице: CI=1 (miniflare добавляет --no-sandbox), axe-source через тестовый шов env.AXE_SOURCE_URL (в prod не задан)."

  - id: CN-LIGHT-ONLY
    title: "Убрать тёмную тему — светлый фон везде (прямое указание владельца, реверс части D-072)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-BRANDBOOK]
    inputs: [src/styles.css, scripts/audit-own-a11y.mjs, docs/project/BRAND_BOOK.md, docs/project/domains/design.md]
    scope: [src/styles.css, scripts/audit-own-a11y.mjs, docs/project/BRAND_BOOK.md, docs/project/domains/design.md]
    outputs: []
    verify:
      - "npm run audit-a11y — ОДИН проход (light), 0 нарушений"
      - "живая проверка: Playwright colorScheme:'dark' против dist/ — фон/текст/--color-primary совпадают со СВЕТЛЫМИ токенами (доказательство, что реверс реальный: при оставшемся dark-блоке тест обязан провалиться)"
      - "grep prefers-color-scheme по src/ — только комментарий-запрет, ни одного переопределения"
    max_attempts: 2
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-08 (D-073). Блок @media (prefers-color-scheme: dark) с 23 токенами удалён целиком, не закомментирован и не скрыт переключателем — надёжнее, чем JS-класс, который можно забыть подключить на новой странице. color-scheme в проекте не выставлялся ни разу (проверено grep до и после). Конституция §28 сама формулирует «dark mode is optional» — реверс применяет её же принцип. Обратимо: dark-значения остались в BRAND_BOOK.md §2 как исторические, но возврат — новое решение владельца, не автоматический откат."

  - id: CN-COMPONENTS-BREADCRUMBS
    title: "Breadcrumbs — 6-й готовый компонент библиотеки (CN-COMPONENTS-REST 3/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, src/components/Layout.tsx, scripts/audit-own-a11y.mjs]
    scope: [src/components/library/Breadcrumbs.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/Breadcrumbs.tsx]
    verify:
      - "npm run audit-a11y — /components/breadcrumbs/ в постоянном сэмпле; 38 страниц 0 нарушений (INTERACT не нужен: пример статичен)"
      - "npm run scripts:test — components.test проходит (ready → полный набор §22 + файл реализации + сэмпл аудита)"
      - "собранный dist: на странице два nav-ориентира с РАЗНЫМИ именами (Breadcrumb / Breadcrumb example), aria-current=page присутствует, разделитель '/' в разметке отсутствует"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-08 (D-077). Выбран вместо Combobox сознательно: контекст сессии был на 77%, Combobox — самый тяжёлый оставшийся композит. Паттерн извлечён из работающего Layout.tsx, а не написан заново. Гейт audit-a11y поймал реальное нарушение в первой версии — landmark-unique: зашитый aria-label=\"Breadcrumb\" столкнулся с трейлом самого сайта на той же странице. Починка архитектурная, не косметическая: label стал пропом, потому что уникальность имени ориентира — свойство страницы, а не компонента."

  - id: CN-COMPONENTS-COMBOBOX
    title: "Combobox — 7-й компонент библиотеки (CN-COMPONENTS-REST 4/10), самый тяжёлый оставшийся композит"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs, scripts/components.test.mjs]
    scope: [src/components/library/Combobox.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/Combobox.tsx]
    verify:
      - "npm run audit-a11y — /components/combobox/ в постоянном сэмпле, INTERACT открывает popup и аудитирует РАСКРЫТОЕ состояние (role=listbox+role=option); 39 стр. 0 нарушений — ✅"
      - "живой Playwright (независимый прогон родительской сессии, не только заявление субагента): ArrowDown/Up двигают aria-activedescendant БЕЗ увода DOM-фокуса с input, фильтрация по вводу сужает список без авто-активации, Enter выбирает и закрывает popup с фокусом на input, Escape закрывает без выбора и без потери фокуса/значения, клик по опции выбирает, пустой фильтр — role=status, не пустой listbox — 18/18 проверок ✅"
      - "npm run scripts:test — components.test проходит без изменений в самом тесте (37/37) — ✅"
      - "npm run typecheck / npm run build / npm run check-links / npm run worker:test / npm run src:test — все зелёные (typecheck чисто, build 442 стр. sitemap 405, check-links 490-0, worker:test 211/211, src:test 20/20) — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-08 (D-078). Реализовано субагентом (Opus, по явному выбору владельца через AskUserQuestion — самый рискованный оставшийся композит), результат перепроверен родительской сессией живьём (не по заявлению): диф прочитан построчно, все 5 гейтов перезапущены, отдельный независимый Playwright-скрипт (18 проверок) написан и прогнан заново против собранного dist/, не переиспользуя скрипт субагента. Ключевая архитектурная особенность паттерна: DOM-фокус НИКОГДА не покидает input — активная опция помечается только aria-activedescendant (виртуальный фокус), это ARIA 1.2 combobox, не roving tabindex (тот — паттерн Tabs). Демо — реальные данные каталога (19 стран + реальные счётчики агентств из data/a11y/taxonomies.json, те же, что на /countries/), не выдуманный список. Гейт a11y прошёл с первого прогона (ловушки закрыты по конструкции: mousedown preventDefault против blur-до-click, пустой результат как role=status вместо пустого/фейкового listbox-option, ручной scrollIntoView). Осознанные отклонения: Home/End двигают активную опцию только при открытом попапе (иначе — родная каретка); ввод текста не авто-активирует первую опцию (Enter не должен коммитить то, что пользователь не смотрел); Escape не очищает поле; анимации открытия нет (нечего отключать под prefers-reduced-motion)."

  - id: CN-COMPONENTS-MENU-BUTTON
    title: "Menu button — 8-й компонент библиотеки (CN-COMPONENTS-REST 5/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs, scripts/components.test.mjs]
    scope: [src/components/library/MenuButton.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/MenuButton.tsx]
    verify:
      - "npm run audit-a11y — /components/menu-button/ в постоянном сэмпле, INTERACT открывает меню и аудитирует РАСКРЫТОЕ состояние (role=menu + role=menuitem); 40 стр. 0 нарушений — ✅"
      - "живой Playwright (независимый прогон родительской сессии): ArrowDown/Up двигают реальный DOM-фокус между пунктами меню (подтверждено — aria-activedescendant на странице не встречается вовсе) с оборачиванием, Home/End на первый/последний пункт, type-ahead прыгает к пункту по первой букве, Enter активирует пункт (реальная навигация на /contact/ подтверждена по URL) и закрывает меню с возвратом фокуса на кнопку, Escape закрывает без активации с возвратом фокуса, Tab закрывает БЕЗ возврата фокуса на кнопку, клик по пункту активирует и закрывает, клик вне меню закрывает без активации — 16/16 проверок ✅"
      - "npm run scripts:test — components.test проходит без изменений в самом тесте (37/37) — ✅"
      - "npm run typecheck / npm run build / npm run check-links / npm run worker:test / npm run src:test — все зелёные (build 443 стр. sitemap 406, check-links 491-0, worker:test 211/211, src:test 20/20) — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-08 (D-079). Заведён сразу после Combobox (D-078), по выбору владельца через AskUserQuestion (компонент Menu button, модель Sonnet — стандартный выбор проекта). Реализовано субагентом на Sonnet, результат перепроверен родительской сессией живьём (диф прочитан построчно, все гейты перезапущены, независимый Playwright-скрипт написан заново — 16/16). Архитектурная суть: НАСТОЯЩИЙ roving tabindex (реальные .focus()-вызовы через refs, tabIndex 0/-1) — противоположность виртуальному фокусу Combobox (aria-activedescendant), тот же механизм, что у Tabs. Демо — «Page actions» (Copy link/Print this page/Report an issue), последний пункт реально ведёт на существующий /contact/, не выдуманная фича. Tab закрывает меню и НЕ возвращает фокус на кнопку (в отличие от Escape/Enter) — APG трактует Tab как переход к следующему элементу, не как dismiss-клавишу."

  - id: CN-COMPONENTS-LISTBOX-SELECT
    title: "Select (listbox) — 9-й компонент библиотеки (CN-COMPONENTS-REST 6/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs, scripts/components.test.mjs]
    scope: [src/components/library/ListboxSelect.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/ListboxSelect.tsx]
    verify:
      - "npm run audit-a11y — /components/listbox-select/ в постоянном сэмпле, INTERACT открывает popup и аудитирует РАСКРЫТОЕ состояние (role=listbox+role=option); 41 стр. 0 нарушений — ✅"
      - "живой Playwright (независимый прогон родительской сессии): реальный DOM-фокус (roving tabindex, aria-activedescendant НИГДЕ на странице не встречается) двигается ArrowDown/Up с оборачиванием, повторное открытие возвращает фокус на уже выбранную опцию (не на первую), Enter выбирает и закрывает popup с фокусом на кнопке, Escape закрывает БЕЗ изменения выбора, typeahead по первой букве, клик по опции выбирает и закрывает, клик вне — закрывает без изменения выбора — 15/15 проверок ✅ (первый прогон дал 1 ложный fail — ошибка в самом проверочном скрипте: сравнивал textContent опции с hint против текста кнопки без hint; точечно подтверждено — совпадают)"
      - "npm run scripts:test — components.test проходит без изменений в самом тесте (37/37) — ✅"
      - "npm run typecheck / npm run build / npm run check-links / npm run worker:test / npm run src:test — все зелёные (build 444 стр. sitemap 407, check-links 492-0, worker:test 211/211, src:test 20/20) — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-08 (D-080). Заведён сразу после Menu button (D-079), по выбору владельца через AskUserQuestion (компонент Select/listbox, модель Sonnet). Реализовано субагентом, результат перепроверен родительской сессией живьём. Архитектурное решение задокументировано явно в шапке файла: НАСТОЯЩИЙ roving tabindex (не aria-activedescendant) — обоснование сравнивает оба существующих прецедента библиотеки (Combobox: virtual focus вынужден editable-полем; MenuButton: real focus, т.к. ничего не редактируется) и относит Select к случаю MenuButton, т.к. триггер — кнопка, а не текстовое поле. Второе архитектурное решение: selection НЕ следует за focus — active (курсор по списку) и selected (закоммиченное значение) разведены как отдельное состояние, поэтому Escape — отдельный путь «ничего не менять», а не откат уже перезаписанного значения (в отличие от нативного <select>, где выбор совпадает с фокусом). Демо — реальные 7 стандартов каталога (WCAG/EN 301 549/Section 508/…) из taxonomies.json, ведёт на существующие /standards/[slug]/. Повторное открытие возвращает фокус на уже выбранную опцию, не на первую — предотвращает потерю места в длинном списке."

  - id: CN-COMPONENTS-FORM-FIELD
    title: "Form field — 10-й компонент библиотеки (CN-COMPONENTS-REST 7/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs, scripts/components.test.mjs, src/components/LeadForm.tsx, src/pages/RequestQuotePage.tsx]
    scope: [src/components/library/FormField.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/FormField.tsx]
    verify:
      - "npm run audit-a11y — /components/form-field/ в постоянном сэмпле; демо смонтировано touched=true с невалидным значением, ошибка видна статично (INTERACT не потребовался); 42 стр. 0 нарушений — ✅"
      - "живой Playwright (независимый прогон родительской сессии): label[for] совпадает с id input, клик по label фокусирует поле; ДО ошибки aria-describedby указывает только на hint; ПОСЛЕ появления ошибки aria-describedby содержит ОБА id (hint+error, оба реально существуют в DOM), не заменяет один другим; aria-invalid='true' только при реальной ошибке, после исправления атрибут отсутствует полностью (null, не строка 'false'); ошибка внутри role=alert; фокус НЕ уводится с поля при появлении ошибки — 11/11 проверок ✅"
      - "npm run scripts:test — components.test проходит без изменений в самом тесте (37/37) — ✅"
      - "npm run typecheck / npm run build / npm run check-links / npm run worker:test / npm run src:test — все зелёные (build 445 стр. sitemap 408, check-links 493-0, worker:test 211/211, src:test 20/20) — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-08 (D-081). CN-COMPONENTS-REST — 7/10 (10 готовых компонентов библиотеки §22 всего: 3 исходных Accordion/Tabs/Modal + 7 из REST). Остаток REST — 3: Pagination, Switch, Data table. Заведён сразу после Select/listbox (D-080), по выбору владельца через AskUserQuestion (компонент Form field, модель Sonnet). Реализовано субагентом, результат перепроверен родительской сессией живьём (диф прочитан построчно, все гейты перезапущены, независимый Playwright — 11/11). Ключевое решение — обобщение уже РАБОТАЮЩЕГО в проде паттерна `Field` из `src/components/LeadForm.tsx` (`/request-quote/`), а не написание с нуля из спецификации (тот же прецедент, что Breadcrumbs, D-077). Два реальных расширения сверх LeadForm: (1) hint и error объединяются в aria-describedby одновременно, не один вместо другого — сам `Field` из LeadForm hint вообще не поддерживал; (2) автоматическое связывание id/aria-invalid/aria-describedby через `cloneElement` на переданный control (тот же приём, что уже в Tooltip.tsx) — LeadForm сейчас повторяет эту проводку вручную на всех 6 полях, FormField закрывает этот пробел по конструкции. LeadForm.tsx НЕ рефакторен на новый компонент — вне scope узла, кандидат для отдельной задачи."

  - id: CN-COMPONENTS-SWITCH
    title: "Switch — 11-й компонент библиотеки (CN-COMPONENTS-REST 8/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs, scripts/components.test.mjs]
    scope: [src/components/library/Switch.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/Switch.tsx]
    verify:
      - "npm run audit-a11y — /components/switch/ в постоянном сэмпле; демо статично показывает оба состояния (on+off) без клика, INTERACT не потребовался; 43 стр. 0 нарушений — ✅"
      - "живой Playwright (независимый прогон родительской сессии): role=switch на обоих элементах (не checkbox, на странице 0 role=checkbox/голых input[type=checkbox]), два переключателя стартуют в РАЗНЫХ состояниях, элемент — реальный <button>, клик/Space/Enter каждый по отдельности переключает aria-checked синхронно с визуальным положением ползунка, aria-labelledby резолвится в реальный видимый текст, aria-hidden On/Off-подпись буквально совпадает с aria-checked — 9/9 проверок ✅ (один ложный fail в первом прогоне — CSS-селектор моего скрипта задел вложенный span ползунка вместо span-подписи; исправлено через lastElementChild, подтверждено)"
      - "npm run scripts:test — components.test проходит без изменений в самом тесте (37/37) — ✅"
      - "npm run typecheck / npm run build / npm run check-links / npm run worker:test / npm run src:test — все зелёные (build 446 стр. sitemap 409, check-links 494-0, worker:test 211/211, src:test 20/20) — ✅"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-08 (D-082). Заведён сразу после Form field (D-081), по выбору владельца через AskUserQuestion (компонент Switch, модель Sonnet). Реализовано субагентом, результат перепроверен родительской сессией живьём. Самый простой компонент библиотеки — ловушку паттерна (role=checkbox вместо role=switch) закрыли по конструкции, а не поймали гейтом: примитив — нативный <button role=\"switch\">, не <input type=\"checkbox\">, поэтому checkbox-семантике просто неоткуда взяться, а Enter/Space работают бесплатно (нативная кнопка). Единственный источник состояния — одна переменная checked, управляющая и aria-checked, и позицией ползунка (тот же принцип не-рассинхрона, что уже был предупреждением у Toast/MenuButton). Label — aria-labelledby на соседний span, не <label>-обёртка: осознанно, чтобы не расширять кликабельную область на текст лейбла незаметно для потребителя компонента. Демо — два независимых переключателя в разных стартовых состояниях (Email on, SMS off)."

  - id: CN-COMPONENTS-PAGINATION
    title: "Pagination — 12-й компонент библиотеки (CN-COMPONENTS-REST 9/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs, scripts/components.test.mjs]
    scope: [src/components/library/Pagination.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/Pagination.tsx]
    verify:
      - "npm run audit-a11y — /components/pagination/ в постоянном сэмпле; демо — ДВА инстанса, каждый статично в своём граничном состоянии (первый на стр.1 Prev-disabled, второй на последней Next-disabled), INTERACT не понадобился; 44 стр. 0 нарушений — ✅"
      - "живой Playwright (независимый прогон родительской сессии): 2 <nav> с РАЗНЫМИ непустыми aria-label (реальный axe landmark-unique прогон 0 нарушений — на странице всего 6 nav[aria-label], важно фильтровать по <ul>-потомку, не индексировать вслепую), aria-current='page' (точная строка) ровно на одном элементе на инстанс, текущая страница — <span>, не <a>/<button>, клик по номеру двигает aria-current, Prev на стр.1 — aria-disabled='true' И Playwright реально отказывается кликнуть (actionability-проверка), accessible name Prev/Next через getByRole совпадает, эллипсис aria-hidden присутствует — 17/17 проверок ✅ (два ложных fail в первом прогоне — оба в собственном скрипте: несуществующий button:has-text-селектор, затем индексация nav[0] без фильтра по <ul> взяла не тот landmark, оба исправлены и подтверждены)"
      - "npm run scripts:test — components.test проходит без изменений в самом тесте (37/37) — ✅"
      - "npm run typecheck / npm run build / npm run check-links / npm run worker:test / npm run src:test — все зелёные (build 447 стр. sitemap 410, check-links 495-0, worker:test 211/211, src:test 20/20) — ✅"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-08 (D-083). Заведён сразу после Switch (D-082), по выбору владельца через AskUserQuestion (компонент Pagination, модель Sonnet). Реализовано субагентом, результат перепроверен родительской сессией живьём. Не именованный APG-паттерн (в отличие от Combobox/Menu/Listbox) — общие принципы навигационной доступности применены к конкретной задаче, поэтому легко выглядеть правильно, будучи практически бесполезным; ловушки закрыты явно: <nav aria-label> (label — проп, не константа, тот же урок Breadcrumbs D-077 — на сайте уже 6 nav-ориентиров на странице компонентов), текущая страница — <span> с aria-current='page' (не 'true', не кликабельная ссылка на саму себя), Prev/Next с видимым текстом 'Previous'/'Next' (не голые иконки), эллипсис aria-hidden декоративен (не притворяется управляющим элементом). Решение по disabled: aria-disabled='true' на реально фокусируемой <button> (не нативный disabled) — сохраняет форму ряда кнопок в таб-порядке независимо от текущей страницы; подтверждено живьём двойным способом (сам атрибут + отказ Playwright's actionability engine кликнуть). На сайте НЕТ существующей пагинации ни на одной странице — в отличие от Breadcrumbs/FormField, здесь не было прецедента 'извлечь из работающего кода', компонент написан с нуля по общим принципам. Первый прогон audit-a11y и живой проверки — оба чистые сразу (target-size ≥36px кнопки выбраны заранее)."

  - id: CN-COMPONENTS-DATA-TABLE
    title: "Data table — 13-й и ПОСЛЕДНИЙ компонент библиотеки (CN-COMPONENTS-REST 10/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs, scripts/components.test.mjs, src/pages/ReportDocPage.tsx]
    scope: [src/components/library/DataTable.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/DataTable.tsx]
    verify:
      - "npm run audit-a11y — /components/data-table/ в постоянном сэмпле; демо смонтировано уже отсортированным (initialSort), INTERACT не понадобился (сортировка не открывает попап, только переупорядочивает уже присутствующие строки); 45 стр. 0 нарушений — ✅"
      - "живой Playwright (независимый прогон родительской сессии): 4 th[scope=col], сортирующие кнопки ВНУТРИ th (не сам th кликабелен, 0 onclick на th), реальный порядок строк меняется после клика (сравнение первой ячейки до/после), aria-sort точно 'ascending' затем 'descending' на том же столбце, ровно один th с aria-sort одновременно (сброшен на предыдущем при переключении столбца), live-область содержит непустой текст с названием столбца, фокус остаётся на кликнутой кнопке сортировки — 14/14 проверок ✅, ни одного бага в собственном скрипте на этот раз (впервые в серии)"
      - "npm run scripts:test — components.test проходит без изменений в самом тесте (37/37) — ✅"
      - "npm run typecheck / npm run build / npm run check-links / npm run worker:test / npm run src:test — все зелёные (build 448 стр. sitemap 411, check-links 496-0, worker:test 211/211, src:test 20/20) — ✅"
    max_attempts: 3
    attempts: 1
    risk: medium
    approval_required: false
    notes: "Готово 2026-08-08 (D-084). Заведён сразу после Pagination (D-083), по выбору владельца через AskUserQuestion (компонент Data table, модель Sonnet). Реализовано субагентом, результат перепроверен родительской сессией живьём. Ключевые решения задокументированы в шапке файла: кнопка ВНУТРИ th, не сам th кликабелен (th не имеет интерактивной роли — bare onClick невидим для клавиатуры/скринридера); aria-sort на th, не на кнопке (описывает свойство столбца, не элемента управления); aria-sort НИКОГДА не пишется явной строкой 'none' — ни для никогда-не-сортированных столбцов, ни для столбца, потерявшего статус сортировки при переключении на другой (единое состояние {key,direction}|undefined, без памяти по столбцам — 'было отсортировано, но сортировка ушла' не отличается от 'сейчас не отсортировано'); реальный порядок строк меняется (сравнение через sortValue, не чтение текста уже отрисованных ячеек); фокус остаётся на кнопке — DOM-элемент не удаляется/не пересоздаётся при сортировке, никакого специального управления фокусом не потребовалось. Данные демо — реальный срез каталога (7 агентств, по одной на страну HQ, agencies.json), не выдумка (D-045/D-047), та же конвенция, что у Combobox/ListboxSelect. **Это последний узел CN-COMPONENTS-REST — 10/10 закрыт, вся библиотека §22 — 13/13 готовых компонентов.**"

  - id: CN-COMPONENTS-TOOLTIP
    title: "Tooltip — 5-й готовый компонент библиотеки (CN-COMPONENTS-REST 2/10)"
    owner: frontend-engineer
    status: done
    depends_on: [CN-COMPONENTS]
    inputs: [data/a11y/components.json, src/lib/componentsLib.tsx, scripts/audit-own-a11y.mjs]
    scope: [src/components/library/Tooltip.tsx, src/lib/componentsLib.tsx, data/a11y/components.json, scripts/audit-own-a11y.mjs]
    outputs: [src/components/library/Tooltip.tsx]
    verify:
      - "npm run audit-a11y — /components/tooltip/ в постоянном сэмпле, INTERACT фокусирует триггер и аудитирует РАСКРЫТОЕ состояние; 37 страниц 0 нарушений"
      - "живой Playwright: фокус показывает подсказку (не только hover), aria-describedby указывает на role=tooltip, Escape скрывает И фокус остаётся на триггере, hover показывает, подсказка переживает наведение курсора на себя (WCAG 1.4.13 hoverable)"
      - "npm run scripts:test — components.test проходит (реестр/данные/сэмпл в согласии)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-08 (D-074). Выбран из-за самой частой ошибки паттерна: показ только по mouseover делает подсказку невидимой для клавиатуры — здесь фокус первичен, hover дополнителен. Escape скрывает, НЕ уводя фокус (WCAG 1.4.13 dismissible): пользователь только читал подпись, отнимать позицию нельзя. Hoverable — grace-период 150мс + mouseenter на пузырьке. Проектное ограничение в типе: content — string, не слот для детей; ARIA-роль tooltip не поддерживает интерактивное содержимое, для такого нужен popover — тип делает нарушение невозможным, а не отговаривает в документации."

  # ---------------------------------------------------------------------
  # Фаза 3 — A3-CRON (мониторинг как подписка). Разбито 2026-08-11 (D-135) по
  # образцу Фазы 2 (A2-LEAD/A2-CLAIM/A2-STRIPE, D-022): SCHEMA → API → EMAIL,
  # approval_required только на новых внешних/платных/DNS-действиях. Три
  # решения владельца зафиксированы в D-135: (1) подписчик = email+URL без
  # аккаунта, A3-AUTH НЕ зависимость; (2) подписка бесплатна на старте,
  # recurring Stripe billing НЕ в этом узле; (3) порядок — RESEND-DOMAIN
  # раньше CONFIRM-EMAIL/DIGEST-EMAIL, закодировано ниже как depends_on, не
  # только текстом. Модель делегированных субагентов — Opus (решение
  # владельца, D-135, цена ошибки — новая таблица D1 в проде + DNS живого
  # почтового домена). Переиспользовать: Cron Trigger 0 3 * * * (уже вызывает
  # deleteExpiredScans), очередь accessatlas-scan-queue/handleScanQueueBatch
  # (не создавать новый браузерный путь для re-scan), worker/lib/resend.js
  # (уже используется A2-CLAIM-EMAIL, тот же best-effort паттерн), схема
  # double opt-in claims (id публичный ≠ token секрет — D-023, обязательна
  # к повторению, не к переизобретению).
  # ---------------------------------------------------------------------

  - id: A3-CRON-RESEND-DOMAIN
    title: "Верификация домена verscala.com в Resend (DNS: SPF/DKIM/DMARC) — снимает sandbox-ограничение D-024, разблокирует реальную отправку сторонним получателям"
    owner: devops-engineer
    status: done
    depends_on: []
    inputs: [docs/project/DECISIONS.md, worker/lib/resend.js]
    scope: [DNS verscala.com (Cloudflare), Resend dashboard]
    outputs: []
    node_kind: agent
    on_failure: escalate
    verify:
      - "домен verscala.com в Resend dashboard в статусе Verified (не Pending) — ПОДТВЕРЖДЕНО живьём, GET /domains/:id -> status: verified, все 3 записи (DKIM/SPF-MX/SPF-TXT) verified"
      - "живой пробный email с домена verscala.com (не onboarding@resend.dev) на безопасный адрес (не живой третьей стороне) доставлен без 422 — ПОДТВЕРЖДЕНО, from notify@verscala.com на владельца проекта (не третья сторона), POST /emails -> 200 {id}"
      - "SPF/DKIM/DMARC записи видны в Cloudflare DNS (dns-only, НЕ проксировать — тот же урок D-092 про почтовые записи, проксирование ломает DKIM) — ПОДТВЕРЖДЕНО, все 3 записи TXT/MX (непроксируемые типы по природе), резолв через DoH сразу после записи"
    max_attempts: 2
    attempts: 1
    risk: high
    approval_required: true
    notes: "DONE 2026-08-11: владелец одобрил живьём через AskUserQuestion (не текстом узла), выдал CLOUDFLARE_API_TOKEN (Zone→DNS→Edit, verscala.com) и RESEND_API_KEY in-memory в чат, ни разу не записаны на диск. Перед записью прочитан весь текущий DNS зоны — конфликтов с живой почтой (MX Outlook, DKIM Microsoft, root SPF -all от GoDaddy) нет, новые записи под отдельным поддоменом send.*/resend._domainkey.*. Разблокирует A3-CRON-CONFIRM-EMAIL и A3-CRON-DIGEST-EMAIL по depends_on — оба остаются blocked до закрытия своих остальных зависимостей (SUBSCRIBE-API/RESCAN-DELTA), но sandbox-барьер D-024 для них снят."

  - id: A3-CRON-SCHEMA
    title: "Схема D1 для подписок на мониторинг (subscriptions table) — миграция, только локально"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/INTERFACES.md, migrations/0004_claims.sql, migrations/0001_init.sql]
    scope: [migrations/, docs/project/INTERFACES.md]
    outputs: [migrations/0010_subscriptions.sql]
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run db:migrate:local
      - npm run worker:test
      - "поля: id PK (публичный), email, url, token (секрет verify-ссылки, отдельный от id — D-023), verified INT DEFAULT 0, status (pending|active|unsubscribed), last_scan_id, cadence (фикс. значение на MVP, напр. weekly), created_at, unsubscribed_at NULL"
      - "id != token тем же способом, что claims (migrations/0006_claim_token.sql) — не переизобретать, скопировать паттерн"
    max_attempts: 3
    attempts: 0
    risk: low
    approval_required: false
    notes: "Не заблокирован — не требует ни домена, ни approval, копирует уже проверенный паттерн claims (D-023). Первый узел, готовый к /task-loop без ожидания владельца."

  - id: A3-CRON-SUBSCRIBE-API
    title: "POST /api/subscribe (создание pending-подписки) + GET /api/subscribe/verify (пометка verified) + unsubscribe-эндпоинт — БЕЗ отправки писем"
    owner: backend-engineer
    status: done
    depends_on: [A3-CRON-SCHEMA]
    inputs: [docs/project/INTERFACES.md, worker/routes/claim.js, worker/lib/turnstile.js, worker/lib/ratelimit.js]
    scope: [worker/routes/subscribe.js, worker/index.js]
    outputs: [worker/routes/subscribe.js]
    node_kind: agent
    on_failure: escalate
    verify:
      - "npm run worker:test — 418/418 (было 386, +32), подтверждено независимо родительской сессией"
      - "400 на невалидный email/url"
      - "201 {subscriptionId} — token НЕ в ответе: тест грепает СЫРОЕ тело ответа и все заголовки на значение токена, не просто typeof-проверку"
      - "GET verify?token=... помечает verified=1, status=active; неизвестный токен -> 404"
      - "unsubscribe (GET или POST, токен в ссылке) переводит status=unsubscribed, идемпотентно (повтор не ошибка)"
      - "тот же rate-limit/Turnstile паттерн, что /api/lead и /api/claim"
      - "email этим узлом НЕ отправляется — тест ловит отсутствие fetch даже при заданном RESEND_API_KEY, реальная отправка это A3-CRON-CONFIRM-EMAIL"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "DONE 2026-08-11. Контракт добавлен в INTERFACES.md §2 (три строки таблицы Worker API). Второй файл тестов на настоящем node:sqlite (не только заглушка D1) — заглушка слепа к невалидному SQL/переставленным bind-параметрам, EXPLAIN QUERY PLAN подтвердил verify-lookup идёт по idx_subscriptions_token. Дедупликации (email,url) намеренно нет — иначе эндпоинт стал бы оракулом «следит ли X за Y», см. комментарий в коде. Разблокировал A3-CRON-SUBSCRIBE-FORM (уже ready по SCHEMA) и один из двух depends_on A3-CRON-CONFIRM-EMAIL."

  - id: A3-CRON-SUBSCRIBE-FORM
    title: "Форма подписки на мониторинг на /report/:id (email+URL, submit подключён к реальному /api/subscribe)"
    owner: frontend-engineer
    status: done
    depends_on: [A3-CRON-SCHEMA]
    inputs: [docs/project/VISION.md, src/components/library/, src/pages/ReportPage.tsx, src/lib/leadForm.ts]
    scope: [src/components/, src/lib/subscribeForm.ts, src/pages/ReportPage.tsx]
    outputs: [src/components/SubscribeForm.tsx, src/lib/subscribeForm.ts]
    node_kind: agent
    on_failure: escalate
    verify:
      - "npm run build — 453 HTML, sitemap 415, U+FFFD нет (подтверждено независимо родительской сессией)"
      - "npm run typecheck — чист"
      - "npm run audit-a11y — 54 стр., 0 нарушений (0 serious/critical), включая 3 новых состояния /report/:id (subscription sent)"
      - "клиентская валидация email/url по тому же паттерну, что leadForm.ts/воркер"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "DONE 2026-08-11. ОТКЛОНЕНИЕ от буквы узла (осознанное): исходный verify-критерий «0 сетевых вызовов + TODO-заглушка» ОТМЕНЁН — узел писался до существования /api/subscribe, но зависимость (SUBSCRIBE-API + CONFIRM-EMAIL) закрылась раньше старта, поэтому submit подключён к настоящему POST /api/subscribe (заглушка была бы враньём в обратную сторону). Форма — последним блоком ReportBody (после MatchedAgencies): отсрочка «просто последим» не должна опережать саму починку; единственный шов, не рвущий соседства D-130. Success-панель честно про double opt-in («check your inbox», подписка не активна до клика). Turnstile — виджет формы скана (не лид-формы: /api/subscribe проверяет токен как /api/scan, без виджета прод давал бы 403). src:test 60→67, независимо перепроверено родительской сессией (67/67). В UI осталась честная оговорка «дайджест ещё включается» — снять одной строкой теперь, когда DIGEST-EMAIL done (см. STATUS)."

  - id: A3-CRON-CONFIRM-EMAIL
    title: "Письмо double opt-in (verify-ссылка подписки) через Resend"
    owner: backend-engineer
    status: done
    depends_on: [A3-CRON-SUBSCRIBE-API, A3-CRON-RESEND-DOMAIN]
    inputs: [docs/project/INTERFACES.md, worker/lib/resend.js, worker/routes/claim.js]
    scope: [worker/lib/resend.js, worker/routes/subscribe.js]
    outputs: []
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run worker:test
      - "best-effort graceful degradation — отсутствие RESEND_API_KEY не превращает успешную запись подписки в 5xx (паттерн D-024, НЕ 503-if-missing)"
      - "живой прогон: реальное verify-письмо доставлено на реальный сторонний email (не на email владельца аккаунта Resend) — доказывает, что A3-CRON-RESEND-DOMAIN реально снял sandbox-ограничение"
    max_attempts: 4
    attempts: 1
    risk: high
    approval_required: true
    notes: "DONE 2026-08-11 (D-136). Владелец дал approval именно на этот узел и сузил живой прогон до СВОЕГО адреса (email владельца проекта, тот же, что в git author) вместо стороннего получателя — verify-критерий про «реального стороннего email» исполнен в ослабленном виде осознанно: доказательство снятия sandbox-барьера всё равно получено (from: notify@verscala.com, а не onboarding@resend.dev, HTTP 200 без 422, GET /emails/{id} last_event=delivered, msg c200c33c-f42d-48ea-b726-1c1a6d492012), а рассылка незнакомым людям ради проверки инфраструктуры — цена, которую платить не надо. worker/lib/resend.js: добавлена VERIFIED_FROM, SANDBOX_FROM оставлена claim.js/lead.js (вне scope, см. D-136). worker/routes/subscribe.js: buildConfirmEmail (text+HTML, URL подписчика экранируется — он попадает в href) + sendConfirmEmailBestEffort после INSERT. worker:test 457/457 (в дереве были незакоммиченные файлы параллельного A3-CRON-RESCAN-DELTA; вклад этого узла — subscribe.test.mjs 25 -> 31). Ссылка в живом письме вела на https://verscala.com/api/subscribe/verify?token=... — маршрут закоммичен, но ещё НЕ задеплоен, поэтому до ближайшего деплоя воркера этот конкретный клик отдаст 404; токен из прогона в D1 не писался (заглушка в скрипте), подписка мёртвая."

  - id: A3-CRON-RESCAN-DELTA
    title: "Еженедельный re-scan подписанных URL (переиспользует SCAN_QUEUE) + дельта относительно предыдущего скана (новые/исчезнувшие находки, изменение score)"
    owner: backend-engineer
    status: done
    depends_on: [A3-CRON-SUBSCRIBE-API]
    inputs: [worker/index.js, worker/lib/scanJob.js, worker/lib/retention.js, wrangler.jsonc, docs/project/INTERFACES.md]
    scope: [worker/lib/subscriptionCron.js, worker/lib/scanDelta.js, worker/index.js, wrangler.jsonc]
    outputs: [worker/lib/subscriptionCron.js, worker/lib/scanDelta.js]
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run worker:test
      - npx wrangler deploy --dry-run
      - "только verified/active подписки re-scan'ятся (unverified/unsubscribed пропущены)"
      - "re-scan кладётся в СУЩЕСТВУЮЩУЮ SCAN_QUEUE через тот же producer-путь, что POST /api/scan — не новый браузерный путь"
      - "scanDelta.js — чистая функция (findings A, findings B) -> {new, resolved, scoreChange}, юнит-тесты на синтетических находках"
      - "cadence не чаще заданного в схеме (weekly на MVP) — подписка, re-scan'нутая вчера, сегодня не re-scan'ится повторно"
    max_attempts: 4
    attempts: 0
    risk: medium
    approval_required: false
    notes: "DONE 2026-08-11. Не approval — Browser Rendering уже одобрен ресурс (A1-SCAN), re-scan запускается только для URL, которые сам подписчик добровольно указал. Решение по расписанию: НЕ новый triggers.crons-элемент — ежедневный тик существующего `0 3 * * *`, недельность на уровне строки (cutoff `sc.created_at < now-7d`, LEFT JOIN scans переживает retention-удаление старого скана), иначе `thundering herd` всех подписок в одну ночь при `max_concurrency: 2`. `last_scan_id` обновляется сразу после постановки в очередь (не после completion — иначе ежедневный повтор при падении скана). Дельта — по стабильному ключу `(ruleId, page, selector)`, не по порядку массива, проверено на живых страницах (bundesregierung.de/europarl.europa.eu/gov.uk), не только фикстурами. `worker:test` 418→472, `wrangler deploy --dry-run` чист — оба подтверждены независимо родительской сессией. АРХИТЕКТУРНЫЙ ДОЛГ для A3-CRON-DIGEST-EMAIL: `last_scan_id` перезаписывается сразу, старый scan_id нигде не хранится персистентно после этого тика — пары {previousScanId, scanId} собраны в момент ре-скана (см. TODO в subscriptionCron.js), но digest-узлу либо нужен отдельный проход в ТОМ ЖЕ тике, либо новая колонка `prev_scan_id` (миграция 0011) — выбор оставлен исполнителю DIGEST-EMAIL."

  - id: A3-CRON-DIGEST-EMAIL
    title: "Письмо-дайджест подписчику с дельтой (новые находки/score) через Resend, с рабочей unsubscribe-ссылкой"
    owner: backend-engineer
    status: done
    depends_on: [A3-CRON-RESCAN-DELTA, A3-CRON-CONFIRM-EMAIL, A3-CRON-RESEND-DOMAIN]
    inputs: [docs/project/INTERFACES.md, worker/lib/resend.js, worker/lib/scanDelta.js]
    scope: [worker/lib/resend.js, worker/lib/subscriptionCron.js]
    outputs: []
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run worker:test
      - "пустая дельта (0 новых/0 исчезнувших находок) НЕ шлёт письмо — не спамить подписчика нулевым апдейтом"
      - "письмо содержит рабочую unsubscribe-ссылку (сгенерированную из уже реализованного A3-CRON-SUBSCRIBE-API unsubscribe-эндпоинта)"
      - "best-effort graceful degradation, тот же паттерн, что A3-CRON-CONFIRM-EMAIL/A2-CLAIM-EMAIL"
      - "живой прогон: реальный дайджест с реальной дельтой доставлен реальному стороннему подписчику"
    max_attempts: 4
    attempts: 1
    risk: high
    approval_required: true
    notes: "DONE 2026-08-11 (D-137). Approval на живую отправку получен от владельца строго на безопасный адрес zincroom@gmail.com. Архитектурный долг решён ОДНОЙ колонкой last_digest_scan_id (migrations/0011), а НЕ prev_scan_id: она и «предыдущий скан» для дельты, и маркер идемпотентности; enqueue-путь RESCAN-DELTA не тронут. runSubscriptionDigests — третий потребитель тика 0 3 * * * (отдельный ctx.waitUntil). Пустая дельта письма не шлёт. Письмо: VERIFIED_FROM, /report/:scanId, unsubscribe-ссылка на handleUnsubscribe + RFC 8058 List-Unsubscribe/-Post (sendEmail расширен headers). Best-effort D-024. Живой прогон: Resend id b86e6afb-d730-4be7-9687-6bd81a8a051f, GET /emails → last_event=delivered на zincroom@gmail.com, unsubscribe-ссылка и List-Unsubscribe в письме реальны. worker:test 472→487 (+15), wrangler deploy --dry-run чист. Ослабление: дельта живого письма синтетическая-но-реалистичная (органическая из двух живых сканов в песочнице непрактична) — ослаблен только ВХОД, путь отправки/доставки/заголовков реален. Не задеплоено (деплой — отдельное одобрение)."

  - id: A3-CRON-PRIVACY
    title: "Обновить Privacy Policy под данные подписки (email retention, cadence, unsubscribe)"
    owner: product-lead
    status: done
    depends_on: [A3-CRON-SCHEMA]
    inputs: [worker/routes/subscribe.js, migrations/0010_subscriptions.sql, src/pages/PrivacyPage.tsx, docs/project/RISKS.md]
    scope: [src/pages/PrivacyPage.tsx, docs/project/RISKS.md]
    outputs: [src/pages/PrivacyPage.tsx]
    node_kind: agent
    on_failure: escalate
    verify:
      - "npm run typecheck — чист (прогнан родительской сессией: product-lead-агент без shell-инструмента, гейты и коммит выполнены родителем)"
      - "npm run build — 453 HTML"
      - "npm run audit-a11y — 54 стр., 0 нарушений"
      - "npm run check-links — 501 ссылка, битых нет"
      - "текст сверен с реальным кодом worker/routes/subscribe.js/retention.js/migrations, не с предположением — перечень сверок в отчёте агента (прецедент A1-PRIVACY)"
      - "явно назван unsubscribe-путь и то, что подписка ОСТАНАВЛИВАЕТСЯ по запросу (soft-delete: status='unsubscribed', строка хранится; полное удаление — по запросу через Contact вручную)"
    max_attempts: 3
    attempts: 1
    risk: low
    approval_required: false
    notes: "DONE 2026-08-11. Новая секция «If you subscribe to accessibility monitoring» на /privacy/: email+url, weekly cadence с подавлением пустой дельты, double opt-in, unsubscribe=немедленный стоп, Resend/notify@verscala.com как процессор, ЯВНО названо отсутствие 90-дневной авто-ретенции для подписок (в отличие от сканов). Все факты сверены с кодом построчно (не по предположению — прецедент A1-PRIVACY). Расхождение soft-delete vs GDPR-erasure записано как RISKS.md R14 (право на удаление Art. 17 — пока ручной запрос, авто-ретенция подписок = будущий узел, не выдумывать что код уже удаляет). product-lead-агент не имеет Bash — правки сделал он, 4 гейта и коммит выполнены родительской сессией."

  - id: G-TOOL-CONTRAST
    title: "Бесплатный чекер контраста /tools/contrast-checker/ как страница-магнит (SEO-кластер contrast ≈27k/мес US, LOW-конкуренция)"
    owner: frontend-engineer
    status: done
    depends_on: []
    inputs: [docs/project/domains/growth.md, src/lib/jurisdictions.ts, src/components/ScanForm.tsx, src/styles.css]
    scope: [src/lib/contrast.ts, src/lib/contrast.test.mjs, src/components/ContrastChecker.tsx, src/pages/ContrastCheckerPage.tsx, src/routes.tsx, src/lib/data.ts, src/lib/i18n.ts, src/components/Layout.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs]
    outputs: [src/pages/ContrastCheckerPage.tsx]
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run typecheck
      - npx tsx --test src/lib/contrast.test.mjs   # математика WCAG сверена с WebAIM/axe
      - node --test scripts/page-lists.test.mjs     # маршрут в sitemap И audit
      - VITE_SCANNER_API=https://audit-fixture.invalid npm run build
      - npm run audit-a11y                          # /tools/contrast-checker/ — 0 нарушений (WCAG 2.2 AA)
      - npm run check-links
      - "живая проверка: Playwright против dist/ — гидрация без mismatch, живой пересчёт, swap, permalink в обе стороны"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "DONE 2026-08-13 (D-144). Выбран владельцем после замера DataForSEO. Модель узла — Opus (AskUserQuestion, D-075). Чистый клиент: математика WCAG в src/lib/contrast.ts (порог 0.03928 = совпадение с axe-core/WebAIM; red-on-black=5.25:1 подтверждено живьём), интерактив с дисциплиной SSG-гидрации (permalink/пипетка в useEffect, не в рендере; URL-sync только в обработчиках). Уникальный слой — 13 юрисдикций EN 301 549→WCAG AA из общего JURISDICTION_OPTIONS (verified только DE, сумм штрафов нет, D-035) + мост на /scan/. Ссылка Tools в футер-nav (site-wide, узел после children → useId не сдвигается, D-087). Гейты: src:test 105/105, scripts:test 48/48, worker:test 488/488, audit-a11y 62 стр. 0 нарушений, check-links 539. approval_required:false — платного API/воркера нет; но push в accessatlas (автодеплой сайта) — отдельное разрешение владельца (D-022), пока НЕ задеплоено. Сегмент /tools/ — задел под следующие магниты (growth.md)."

  - id: G-TOOL-READABILITY
    title: "Бесплатный readability checker /tools/readability-checker/ + индекс /tools/ (SEO-кластер readability 7200/мес LOW)"
    owner: frontend-engineer
    status: done
    depends_on: [G-TOOL-CONTRAST]
    inputs: [docs/project/domains/growth.md, src/lib/contrast.ts, src/components/ContrastChecker.tsx, src/pages/ContrastCheckerPage.tsx]
    scope: [src/lib/readability.ts, src/lib/readability.test.mjs, src/components/ReadabilityChecker.tsx, src/pages/ReadabilityCheckerPage.tsx, src/pages/ToolsIndexPage.tsx, src/routes.tsx, src/lib/data.ts, src/components/Layout.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs]
    outputs: [src/pages/ReadabilityCheckerPage.tsx, src/pages/ToolsIndexPage.tsx]
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run typecheck
      - npx tsx --test src/lib/readability.test.mjs
      - node --test scripts/page-lists.test.mjs
      - VITE_SCANNER_API=https://audit-fixture.invalid npm run build
      - npm run audit-a11y
      - npm run check-links
      - "живая проверка: Playwright против dist/ — гидрация без mismatch, живой пересчёт формул"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "DONE 2026-08-13 (D-145). Модель Sonnet (выбор владельца, шаблон contrast уже есть). Чистые формулы (Flesch/FK/Gunning Fog/SMOG/Coleman-Liau/ARI) в readability.ts, 16 тестов. Честность про стандарт: SC 3.1.5 Reading Level = AAA, НЕ базовый AA (сказано прямо), формулы англоязычные, сумм штрафов нет, мост на /scan/. Появился 2-й инструмент → создан индекс /tools/, футер-Tools ведёт на него. Родительская Opus-проверка живьём: Playwright против dist — гидрация чистая, пересчёт верного направления. Гейты: src:test 121/121, scripts:test 48/48, audit-a11y 64 стр. 0 нарушений, check-links 563. НЕ задеплоено (push в accessatlas)."

  - id: A5-ABUSE-LIMITS
    title: "Anti-abuse: суточный лимит скана 10/IP + защита подписки от email-бомбинга (per-email лимит + дедуп)"
    owner: backend-engineer
    status: done
    depends_on: []
    inputs: [worker/lib/ratelimit.js, worker/routes/scan.js, worker/routes/subscribe.js, migrations/0010_subscriptions.sql]
    scope: [worker/lib/ratelimit.js, worker/lib/ratelimit.test.mjs, worker/routes/scan.test.mjs, worker/routes/subscribe.js, worker/routes/subscribe.test.mjs, worker/routes/subscribe.sql.test.mjs]
    outputs: []
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run worker:test
      - "npx wrangler deploy --dry-run — бандл собирается, биндинги без изменений"
      - "порядок dedup ПЕРЕД per-email лимитом (регресс-тест): честный повтор одной формы не жжёт квоту и не 429-ит"
      - "неотличимость dedup-хита от свежей подписки (тот же 201, тот же набор полей — не оракул)"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: true
    notes: "DONE в коде 2026-08-13 (D-147), воркер НЕ задеплоен. Запрос владельца. Модель Sonnet (выбор владельца; совет хука haiku отклонён — security). Скан +10/IP/день (отдельный KV-ключ, не коллизирует с часовым на границе суток). Подписка +3 письма/email/сутки (норм. email) + дедуп lower(email)+url среди pending|active. Родительская поправка: dedup ПЕРЕД per-email лимитом (честный повтор не ломается, атака через разные url режется так же) + регресс-тест. worker:test 488→503, wrangler dry-run чист. approval_required:true — worker deploy только с CF-токеном владельца (D-022)."

  - id: G-CHECKERS-BATCH-1
    title: "3 клиентских чекера-магнита под /checkers/: color blindness simulator, color converter (hex↔rgb↔hsl), text-to-speech"
    owner: frontend-engineer
    status: done
    depends_on: [G-TOOL-CONTRAST, G-TOOL-READABILITY]
    inputs: [src/lib/contrast.ts, src/pages/ContrastCheckerPage.tsx, docs/project/domains/growth.md]
    scope: [src/lib/cvd.ts, src/lib/cvd.test.mjs, src/components/ColorBlindnessSimulator.tsx, src/pages/ColorBlindnessPage.tsx, src/components/ColorConverter.tsx, src/pages/ColorConverterPage.tsx, src/components/TextToSpeech.tsx, src/pages/TextToSpeechPage.tsx, src/routes.tsx, src/lib/data.ts, src/pages/ToolsIndexPage.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs]
    outputs: [src/pages/ColorBlindnessPage.tsx, src/pages/ColorConverterPage.tsx, src/pages/TextToSpeechPage.tsx]
    node_kind: agent
    on_failure: escalate
    verify:
      - npm run typecheck
      - npx tsx --test src/lib/cvd.test.mjs
      - node --test scripts/page-lists.test.mjs
      - VITE_SCANNER_API=https://audit-fixture.invalid npm run build
      - npm run audit-a11y
      - npm run check-links
      - "живьём: CVD-матрицы сверены независимо (protanopia red={145,142,0}), Playwright — converter/canvas/TTS гидрируются без mismatch"
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: false
    notes: "DONE 2026-08-13 (D-151). Приоритет по замеру DataForSEO (colorblind 201k / rgb↔hex 23k / TTS 301k, все LOW ads-comp). Матрицы CVD — набор Wickline/HCIRN, задан родителем дословно. Sonnet-субагент, родитель перепроверил живьём. audit-a11y 67 стр. 0 нарушений. НЕ трогать гранулярные a11y-чекеры (aria/heading/tap-target) — спрос ~0, их закрывает сканер. Задеплоено push'ем в accessatlas."

  - id: G-GUIDES-BATCH-2
    title: "3 новых гайда под пробелы спроса: how-to-write-alt-text, aria-labels, accessibility-statement (EN)"
    owner: growth-strategist
    status: done
    depends_on: []
    inputs: [docs/project/domains/growth.md, data/a11y/guides/wcag-audit-guide.md]
    scope: [data/a11y/guides/how-to-write-alt-text.md, data/a11y/guides/aria-labels-guide.md, data/a11y/guides/accessibility-statement-guide.md]
    outputs: [data/a11y/guides/how-to-write-alt-text.md, data/a11y/guides/aria-labels-guide.md, data/a11y/guides/accessibility-statement-guide.md]
    node_kind: agent
    on_failure: escalate
    verify:
      - node --test scripts/no-fine-amounts.test.mjs
      - VITE_SCANNER_API=https://audit-fixture.invalid npm run build
      - npm run check-links
      - npm run audit-a11y
      - "родитель факт-чекнул первоисточники: ARIA 4 правила (W3C using-aria), accessible-name precedence, WCAG 1.1.1 Level A, MDN-ссылка 200, юр-claims statement-гайда честно захеджированы"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "DONE 2026-08-13. Приоритет по замеру DataForSEO (alt text 5.4k / aria labels 4.4k / accessibility statement ~1k, все LOW) — большие темы (ADA/508/WCAG/VPAT/EAA) уже покрыты, взяты ПРОБЕЛЫ. Sonnet-субагент, родитель факт-чекнул: субагент верно поймал '4 правила ARIA' против моей ошибочной памяти о 5 — пример «сверять первоисточник, не память». Сумм штрафов нет (D-035), гайды кросс-линкуют друг друга + сканер + BFSG-гайды (анти-orphan §23). Автоинтеграция глобом (src/lib/guides.ts) — routes/sitemap не трогали. Полный прогон гейтов перед push (урок после дрейф-фейла D-154)."

  - id: G-CHECKER-PALETTE
    title: "6-й чекер: accessible colour palette generator — /checkers/color-palette-generator/ (кластер color palette 90-165k)"
    owner: frontend-engineer
    status: done
    depends_on: [G-TOOL-CONTRAST, G-CHECKERS-BATCH-1]
    inputs: [src/lib/contrast.ts, src/pages/ContrastCheckerPage.tsx, docs/project/domains/growth.md]
    scope: [src/lib/palette.ts, src/lib/palette.test.mjs, src/components/ColorPaletteGenerator.tsx, src/pages/ColorPaletteGeneratorPage.tsx, src/routes.tsx, src/lib/data.ts, src/pages/ToolsIndexPage.tsx, scripts/gen-a11y-sitemap.mjs, scripts/audit-own-a11y.mjs]
    outputs: [src/pages/ColorPaletteGeneratorPage.tsx]
    node_kind: agent
    on_failure: escalate
    verify:
      - npx tsx --test src/lib/palette.test.mjs
      - npm run typecheck && npm run src:test && npm run scripts:test && npm run worker:test
      - VITE_SCANNER_API=https://audit-fixture.invalid npm run build && npm run audit-a11y && npm run check-links
      - "родитель сверил гармонии независимо: complementary(red)=[0,180], триада 120 apart; инвариант contrastRatio(c,white)*contrastRatio(c,black)=21"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "DONE 2026-08-13 (WRAP-UP сессии). Замер DataForSEO: color palette кластер 90-165k LOW-ads (но органика — гиганты; берём нишевый/длинный хвост + наш WCAG-угол: у каждого образца виден контраст и AA). Sonnet-субагент, родитель сверил математику + полный прогон гейтов (palette 13/13, src 140, scripts 48, worker 504, audit 68 стр. 0 нарушений, sitemap 490, check-links 575). Индекс /checkers/ = 6 карточек. Субагент добавил изящный инвариант-тест (max(contrast к white/black) >= sqrt(21) всегда) и — вопреки инструкции — правки в frontend.md/LEARNING_LOG (D-051, оставлены как корректные)."
