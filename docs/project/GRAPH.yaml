version: 1
updated_at: 2026-08-05
limits:
  max_parallel: 3
  max_nodes_per_iteration: 8
  default_max_attempts: 4
nodes:
  - id: A0-ENRICH
    title: Применить enrich-патчи описаний (119 записей), починить slug the-pixel, валидация + сборка
    owner: data-engineer
    status: done
    depends_on: []
    inputs: [data/a11y/enrich/, scripts/enrich-a11y.mjs, data/a11y/agencies.json]
    scope: [data/a11y/agencies.json, data/a11y/enrich/us-batch.json]
    outputs: [data/a11y/agencies.json]
    verify:
      - node scripts/enrich-a11y.mjs --dry   # 0 неизвестных slug
      - node scripts/enrich-a11y.mjs
      - node scripts/build-a11y.mjs          # 253 агентства, без ошибок
      - npm run typecheck
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-05 (commit e0c9a85): the-pixel — не баг, запись корректно в excluded.json (не оказывает услуг доступности); патч в us-batch.json был устаревшим дублем, удалён. Описания 81→200/253 (79.1%). Побочно: package-lock.json отсутствовал в репо — добавлен, иначе npm ci в CI не мог работать."

  - id: A0-DESC-REST
    title: Описания оставшимся профилям без description (~53) — только проверяемые факты
    owner: data-engineer
    status: done
    depends_on: [A0-ENRICH]
    inputs: [data/a11y/agencies.json, data/a11y/README.md]
    scope: [data/a11y/enrich/, data/a11y/agencies.json]
    outputs: [data/a11y/enrich/desc-final.json]
    verify:
      - node scripts/build-a11y.mjs
      - "покрытие description >= 90%"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-05 (commit 1985a15): 5 параллельных субагентов исследовали сайты, 45 записей получили описание, 8 исключены (мёртвые/заброшенные сайты, поглощения, нет услуг доступности — та же логика, что the-pixel) и удалены из agencies.json + добавлены в excluded.json. Итог: 253→245 агентств, описания 245/245 (100%, было 79.1%). Порог 90% для снятия noindex закрыт."

  - id: A0-CI-FIX
    title: "Починить CI: package-lock.json (добавлен), lint:upl/lint:minimize/check-links.mjs отсутствуют, хотя ci.yml их вызывает"
    owner: devops-engineer
    status: done
    depends_on: []
    inputs: [.github/workflows/ci.yml, package.json, scripts/]
    scope: [package.json, scripts/, .github/workflows/ci.yml]
    outputs: [scripts/check-links.mjs]
    verify: ["npm ci проходит", "npm run typecheck", "npm run build", "npm run check-links — 0 битых ссылок"]
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-05 (commit 969bdf5): ci.yml на этой ветке был скопирован целиком с main (detnav) при заведении ветки. lint:upl/lint:minimize — линтеры юридического контента detnav (content/** на формулировки вида UPL), у AccessAtlas нет такого контента — шаги удалены, не переписаны заглушками (D-009). check-links.mjs — генерик-полезная идея, реализована заново под dist/ этого сайта: 413 внутренних ссылок, 0 битых. npm ci→typecheck→build→check-links проходят локально."

  - id: A0-OWNER-LEGAL
    title: Реквизиты Imprint + покупка домена
    owner: product-lead
    status: blocked
    depends_on: []
    inputs: []
    scope: []
    outputs: [src/pages/ImprintPage.tsx]
    verify: []
    max_attempts: 1
    attempts: 0
    risk: high
    approval_required: true
    notes: "БЛОКЕР НА СТОРОНЕ ВЛАДЕЛЬЦА: юрлицо/ФИО, адрес, HR/VAT (DE), выбранный домен. Без этого нет A0-ORIGIN и A0-DEPLOY"

  - id: A0-ORIGIN
    title: Заменить accessatlas.example на реальный домен, снять noindex с Imprint
    owner: frontend-engineer
    status: blocked
    depends_on: [A0-OWNER-LEGAL]
    inputs: [src/lib/seo.tsx, scripts/gen-a11y-sitemap.mjs]
    scope: [src/lib/seo.tsx, scripts/gen-a11y-sitemap.mjs, src/pages/ImprintPage.tsx]
    outputs: []
    verify: [npm run build, "grep -r accessatlas.example → пусто"]
    max_attempts: 2
    attempts: 0
    risk: low
    approval_required: false

  - id: A0-DEPLOY
    title: Cloudflare Pages деплой + автодеплой из CI
    owner: devops-engineer
    status: blocked
    depends_on: [A0-OWNER-LEGAL]
    inputs: [.github/workflows/ci.yml, docs/deepdive-accessibility-directory.md]
    scope: [.github/workflows/, wrangler/pages config]
    outputs: []
    verify: ["прод-URL отдаёт 200", "dist/404.html работает", "sitemap.xml доступен"]
    max_attempts: 4
    attempts: 0
    risk: medium
    approval_required: true
    notes: "approval: создание внешнего ресурса (Pages-проект) и привязка домена. A0-ENRICH/A0-DESC-REST/A0-CI-FIX все done — единственный оставшийся блокер Фазы 0 это A0-OWNER-LEGAL (ввод владельца)"

  - id: A1-SCAN
    title: "Сканер MVP: POST /api/scan (Browser Rendering + axe-core) -> D1, GET /api/scan/:id"
    owner: backend-engineer
    status: review
    depends_on: []
    inputs: [docs/project/domains/architecture.md, docs/project/INTERFACES.md]
    scope: [worker/, wrangler.jsonc, migrations/]
    outputs: [worker/, wrangler.jsonc, migrations/0001_init.sql, docs/project/domains/backend.md]
    verify: ["wrangler dev: скан example.com возвращает findings", npm run typecheck, npm run worker:test]
    max_attempts: 4
    attempts: 1
    risk: medium
    approval_required: true
    notes: "Владелец одобрил платные ресурсы CF 2026-08-05. Код написан и залит (D-010): routing/валидация/D1/KV/ctx.waitUntil error-handling проверены live через wrangler dev; 9/9 юнит-тестов (score.js, links.js) зелёные; wrangler deploy --dry-run бандлится чисто. НЕ подтверждено: реальная axe-core инъекция против живого сайта — упёрлась в MITM-прокси песочницы (локальный Chromium не доверяет CA), не в код. status=review, не done — нужен один прогон `wrangler dev --remote` или прод-деплой на реальном аккаунте владельца, дальше — А1-REPORT/A1-EXPLAIN разблокируются"

  - id: A1-REPORT
    title: "UI отчёта /report/:id — находки по WCAG, severity, score"
    owner: frontend-engineer
    status: review
    depends_on: [A1-SCAN]
    inputs: [docs/project/INTERFACES.md, docs/project/VISION.md]
    scope: [src/pages/, src/components/, src/routes.tsx, src/lib/scanner.ts, worker/lib/errors.js]
    outputs: [src/pages/ReportPage.tsx, src/lib/scanner.ts, .env.example]
    verify:
      - npm run build
      - npm run typecheck
      - npm run audit-a11y
      - "async-прогресс скана виден пользователю (VISION.md UX-требование 2)"
      - "своё понятное сообщение на каждое error-состояние, не общее (UX-требование 4)"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06: все 5 UX-требований выполнены, 6 состояний (running/done/done-clean/error/not-found/unavailable) верифицированы через Playwright с подменённым API против реального dev-сервера — 0 нарушений axe-core на каждом (см. domains/frontend.md). Попутно найден и исправлен реальный баг контраста + баг самого audit-own-a11y.mjs (D-014). status=review, не done: зависит от A1-SCAN (тоже review) — сама страница отчёта готова и протестирована независимо от него."

  - id: A1-LANDING
    title: "Лендинг сканера /scan — форма URL, Turnstile, честный прогноз времени, ссылка из шапки"
    owner: frontend-engineer
    status: review
    depends_on: []
    inputs: [docs/project/VISION.md, src/lib/scanner.ts]
    scope: [src/pages/ScanPage.tsx, src/components/TurnstileWidget.tsx, src/lib/scanner.ts, src/lib/data.ts, src/components/Layout.tsx]
    outputs: [src/pages/ScanPage.tsx, src/components/TurnstileWidget.tsx]
    verify:
      - npm run build
      - npm run typecheck
      - npm run audit-a11y
      - "клиентская валидация URL не делает лишний сетевой запрос"
      - "успешная отправка ведёт на /report/:id с реальным scanId"
      - "серверная ошибка (напр. rate limit) отображается пользователю"
    max_attempts: 4
    attempts: 1
    risk: low
    approval_required: false
    notes: "Готово 2026-08-06 (D-015). Статическая пререндерящаяся страница (не client-only, в отличие от /report/:id) — 385-я страница сборки, добавлена в sitemap. 4 сценария проверены живьём через Playwright с подменённым `page.route()`: idle-рендер, невалидный URL (0 сетевых вызовов), успешный сабмит → /report/abc123/, серверная ошибка 429 → текст виден пользователю. Все состояния axe-чисты. Explicitly НЕ добавлено поле email — POST /api/scan его не использует, обещать в UI значило бы врать (D-015). status=review, не done: реальный Turnstile-виджет (внешний скрипт challenges.cloudflare.com) не тестировался в этой песочнице — тот же сетевой MITM-барьер, что у D-010; путь без VITE_TURNSTILE_SITE_KEY (виджет не рендерится) полностью проверен."

  - id: A1-EXPLAIN
    title: AI-пояснения находок (Claude Haiku + KV-кэш ruleId×locale)
    owner: backend-engineer
    status: blocked
    depends_on: [A1-SCAN]
    inputs: [docs/project/DECISIONS.md]
    scope: [worker/]
    outputs: []
    verify: ["повторный запрос того же ruleId отдаётся из кэша", "лимит бюджета настроен"]
    max_attempts: 4
    attempts: 0
    risk: medium
    approval_required: true
    notes: "approval: платный API-ключ Anthropic в секретах Worker"
