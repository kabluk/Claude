# HANDOFF — что нужно знать новой сессии

Обновлено: 2026-08-10 · Проект **AccessAtlas**, ветка `accessatlas`.
`main` этого репозитория — ДРУГОЙ проект, никогда не стартовать от него.
**С 2026-08-08 ветка `accessatlas` пересоздана владельцем от рабочей линии
(D-100) и с тех пор держится синхронной — это актуальная точка старта.**
Если харнесс выдал новой сессии ветку `claude/accessatlas-*-<suffix>`,
указывающую на посторонний код (случалось: D-078, D-088), — пересоздай её
от `origin/accessatlas`: `git fetch origin accessatlas && git checkout -B
<своя-ветка> origin/accessatlas`. Проверка, что ветка правильная:
`git log --oneline -3` содержит D-1xx-коммиты и есть
`docs/project/HANDOFF.md`.

⚠️ **Push в `accessatlas` = автодеплой сайта на прод** (D-099/D-101):
CI собирает с `VITE_SCANNER_API`, гоняет все гейты и катит на
Cloudflare Pages. Пушить туда только то, что готово ехать пользователям;
рабочие коммиты — в свою ветку итерации, синхронизация в `accessatlas`
осознанным push'ем.

> Этот файл — точка входа, а не журнал. Он отвечает на «что мне нужно знать,
> чтобы начать работать прямо сейчас». История решений живёт в `DECISIONS.md`,
> фактическое состояние — в `STATUS.md`, задачи — в `BACKLOG.md`/`GRAPH.yaml`,
> детали реализации — в `domains/*.md`. Дублировать их здесь не нужно: файл
> уже однажды разросся до 517 строк и перестал работать как точка входа (D-048).

## Как продолжить

> Работай в ветке от `accessatlas`. Прочитай этот файл и `STATUS.md`, затем
> `/project-orchestrator Продолжи проект: выбери следующий незаблокированный узел
> из docs/project/GRAPH.yaml, выполни через /task-loop, обнови handoff.

Проверка среды: `npm install && node scripts/build-a11y.mjs` — ожидать **245** агентств.

`/project-orchestrator`, `/workflow-graph`, `/task-loop`, `/task-profiler`,
`/next-step-coach` установлены прямо в репозитории (`.claude/`, D-012) — внешний
плагин не нужен. Если хук Model Advisor не срабатывает, откройте `/hooks` один
раз (перезагружает конфиг).

## Что это

Платформа web-доступности (EAA/WCAG/ADA/BFSG/RGAA), развитие
**Directory → Decision Engine → Lead Marketplace → Vertical SaaS** (VISION.md, D-003).

- **Слой 1, каталог** — построен и **в проде**: https://verscala.com (D-094).
- **Слой 2, сканер** — в проде и устойчив после недели боевой отладки
  (D-098…D-110): скан выполняется через **Cloudflare Queues**
  (`accessatlas-scan-queue`, D-110 — снят 30-секундный потолок waitUntil),
  внутри — сторожевой таймаут 120с (D-108), плюс reap протухших `running`
  при GET (D-109). Прямые ссылки `/report/:id` работают (Pages Function,
  D-103). Клиентский опрос переживает сбои сети (D-106).
- **Слои 3–4** — начаты с товара: PDF-план исправлений готов в коде
  (D-114, `GET /api/scan/:id/pdf`), живьём не печатался, гейта доступа нет.
  Монетизация спроектирована разговором 2026-08-10 (см. «Воронка» ниже).

## ⚡ Бренд: публично — Verscala (verscala.com), внутренне — AccessAtlas

С 2026-08-08 (D-091) публичный бренд сайта — **Verscala**, домен —
**verscala.com** (куплен владельцем). Внутреннее имя проекта в docs/project/,
CLAUDE.md, именах ресурсов Cloudflare (`accessatlas-worker`, D1
`accessatlas-scans`) сознательно НЕ менялось. Новые user-facing тексты пишут
«Verscala»; внутренние документы продолжают говорить «AccessAtlas».

## 🚀 Сайт в проде: https://verscala.com

Запущен 2026-08-08 (D-094). Фаза 0 закрыта: `A0-OWNER-LEGAL` → `A0-ORIGIN` →
`A0-DEPLOY` (основная часть). Зона Cloudflare `active`, сайт и `www` отдают
200, почта `info@verscala.com` работает (MX Outlook, DKIM резолвится в
Microsoft — записи держатся `dns-only`, D-092; **не включать проксирование
на почтовых записях**).

Деплой сайта — **автоматический**: push в `accessatlas` → CI (deploy.yml,
D-099/D-101) собирает с `VITE_SCANNER_API`, гоняет гейты, катит на Pages.
Деплой ВОРКЕРА — отдельно и вручную: `npx wrangler deploy` с CF-токеном
владельца (in-memory, никогда на диск; после деплоя выждать раскатку
~30–60с прежде чем проверять — D-105).

## Воронка (решения владельца, 2026-08-10; код НАЧАТ — D-114)

- Бесплатно: скан + score с грейдом (шкала 90/70/50 — D-107, уже в проде)
  + полный список находок. **Находки НЕ блюрить** — подтверждено повторно
  (D-114) после того, как владелец прислал пейволл NYT: блюрится ПЛАН,
  находки остаются открытыми. Они же кормят бесплатную ветку «сделайте за
  меня»: видимые находки доказывают, что проблемы реальны.
- Платно **€19.99** (не 9.99 — якорь: quickscan у агентств от €595, наши
  же данные G-PRICE): PDF-план — приоритеты, юр-контекст (BFSG/EN 301 549),
  оценка усилий, готовое ТЗ разработчику. НЕ «сертификат соответствия».
- Развилка на отчёте: «сделаю сам» → PDF за деньги / «сделайте за меня» →
  заявка агентству, тогда PDF бесплатно (лид ценнее €19.99).
- Лиды агентствам в фазе 1 — **бесплатно** (набираем track record;
  cold-start: продавать непроверенные лиды нельзя). Монетизация лидов —
  фаза 2. Собственный ремонт силами дешёвого QA — отвергнут (channel
  conflict + ответственность, см. DECISIONS-разговор в STATUS 2026-08-10).

## Что осталось

1. **`A0-GSC` — верификация и sitemap сделаны владельцем напрямую (D-123,
   2026-08-10).** Domain property `verscala.com` подтверждена через провайдера
   доменных имён (Cloudflare OAuth, не через наш DNS-токен — владелец выбрал
   этот путь сам), `sitemap.xml` (415 URL) отправлен в Search Console.
   Записано со слов/скриншотов владельца, не перепроверено API (у сессии нет
   доступа к Search Console). **Осталось**: подождать неделю, зайти в
   «Страницы» (Индексирование) и посмотреть реальное покрытие индекса —
   отдельный будущий чек-ин, не блокирует ничего другого.
2. **Воронка задеплоена НАПОЛОВИНУ (D-120). ВОРКЕР на проде и проверен;
   SSG-баг ПОЧИНЕН И ПОДТВЕРЖДЁН В CI (D-121); деплой сайта упёрся в НОВЫЙ,
   отдельный инфра-блокер (D-122) — невалидный Cloudflare-токен в CI secrets.**
   - **Воркер** (Version `145c7628`): PDF-план (D-114), пейволл-гейт (D-115),
     Stripe checkout (D-116) — всё задеплоено, миграция `0008` применена,
     проверено живьём (402 plan_locked без лида; /checkout → 503 без
     Stripe-ключа; planUnlocked в ScanReport). Свободная ветка (лид → PDF)
     готова; платная — код готов, кнопка активна, ждёт Stripe-ключей владельца.
   - **✅ SSG-баг U+FFFD ПОЧИНЕН (D-121), подтверждено в реальном CI (D-122)**.
     Корень оказался не в `vite-react-ssg` (гипотеза D-120 неверна), а в React
     18.3.1: `react-dom/server` (Node) сбрасывал в поток весь 2048-байтовый вид
     вместо заполненной части, дописывая NUL-байты перед многобайтовым
     символом. Правка — `patch-package` + `patches/react-dom+18.3.1.patch`
     (одна строка), `postinstall` в `package.json` переприменяет её после
     `npm ci`. Push в `accessatlas` (349e6ae) → CI run `31439945281`: ВСЕ
     гейты зелёные (typecheck, src:test, scripts:test, worker:test, Build
     с U+FFFD-гейтом, check-links, audit-a11y 53/0) — задача главной сессии
     закрыта по существу.
   - **⛔ НОВЫЙ блокер (D-122), не связан с SSG**: последний шаг `Deploy to
     Cloudflare Pages` упал — `wrangler pages deploy` вернул `Invalid access
     token [code: 9109]` на `/accounts/*/pages/projects/verscala`. Секрет
     `CLOUDFLARE_API_TOKEN` в GitHub repo secrets (Settings → Secrets and
     variables → Actions) невалиден. **Нужен от владельца**: свежий токен
     (права как в D-092 — Pages Edit), обновить секрет, затем перезапустить
     workflow (API-триггер для этой интеграции получал 403 на rerun/dispatch,
     см. D-120 — возможно понадобится либо новый push, либо владелец нажимает
     Re-run в UI Actions сам). Прод-сайт пока старый (D-113), ничего не
     сломано — деплой просто не произошёл.
   - **✅ Хвост `?checkout=success|cancel` сделан (D-124)** — тост + автоанлок
     панели на `/report/:id` после возврата из Stripe Checkout. Живая
     Playwright-проверка поймала и починила реальную гонку (эффект,
     стирающий query-параметр, сам себя перезапускал и убивал retry-поллинг
     до ответа вебхука) — юнит-тесты и axe её не видели. В той же ветке
     итерации, что D-121/D-122, тоже ждёт push в `accessatlas`.
   - **Дальше**: тестовый платёж Stripe (`sk_test_…`, `whsec_…` от владельца,
     кладутся `wrangler secret put`) — финальная проверка платной ветки;
     живой PDF-print (unlocked, через лид) ещё не проверен.
3. ~~`A0-SOURCEREF-LANG`~~ — **done** (D-111, 2026-08-10): все 24 записи
   `sourceRefs[].label` очищены/переведены, факты и цитаты не тронуты,
   0 кириллицы во всём `agencies.json`. Следующий свободный пункт из этого
   меню — `A0-GSC`, `A2-REPORT-PAYWALL` (ждёт старта владельца) или
   `A1-SCAN-BUSY-RETRY`.
4. **Resend**: верификация домена возможна → реальные письма
   (`A2-CLAIM-EMAIL`, `A2-OUTREACH-SEND`).
5. **`A2-STRIPE-LIVE`**: ждёт `STRIPE_WEBHOOK_SECRET` от владельца.
6. ~~`A1-SCAN-BUSY-RETRY` + SCAN-RESILIENCE~~ — **ЗАКРЫТО** (D-112,
   D-113). Всё в проде: сайт 05:46 UTC, воркер `de18d14f` ~07:00 UTC.
   Владелец перешёл на **Workers Paid** — суточный потолок 10 мин снят.
   Серия 3 сканов `en.zebrakita.de` подряд: **все три done** (66/38/43с,
   6 стр., 128 находок, score 44), `/report/:id` отдаёт 200. Долг перед
   владельцем закрыт. Детали и размен по скорости — STATUS.md.
7. ~~`index.html:6` русский HTML-комментарий~~ — **done** (2026-08-10):
   переведён на английский; теперь 0 кириллицы во всех 453 собранных HTML
   (проверено JS-обходом `dist/`). Побочная находка D-111 закрыта.

**Про CF-токены** (стоило нескольких итераций, D-092): права уровня
аккаунта (Workers/D1/KV/Browser Rendering/Pages) и уровня зон (DNS, Zone)
живут в РАЗНЫХ политиках одного токена — под ресурсом «Entire Account»
зонных прав в списке нет вовсе. Проверяй права эмпирически (запрос к
каждому API), а не по названиям чекбоксов. И не заводи два токена с
одинаковым именем — секрет от одного при правах у другого выглядит как
«права не применяются».

Отдельно: перенос репозитория в `kabluk/accessatlas` ждёт GitHub-интеграцию.

✅ **`worker:deploy` реально выполнен 2026-08-08 (D-088).** Владелец выдал
`CLOUDFLARE_API_TOKEN` in-memory прямо в чат (никогда не на диске/в коммите);
5 миграций применены на реальной D1, воркер задеплоен (Version ID `bf8087e6`).
Исправление D-040 (правовая ссылка → находка «заявления нет») и пофазный
прогресс CN-SCAN-PHASES (D-067) оба подтверждены живьём на проде реальными
сканами (`www.w3.org/WAI/`, `countryCode=DE`) — не по логу деплоя. Детали —
`DECISIONS.md` D-088, `STATUS.md`.

## Правила, которые нельзя нарушать

- **Контекст сессии под наблюдением** (D-076): хук
  `.claude/scripts/context_monitor.py` считает реальный расход токенов из
  транскрипта и на ≥70% просит свернуть узел, на ≥85% — передать эстафету,
  выдав готовый промпт новой сессии. Эти сообщения не игнорировать: они
  редкие и приходят ровно тогда, когда «продолжаем» начинает означать
  «теряем ранние договорённости». Порог окна — `CONTEXT_LIMIT_TOKENS`.
- **Перед существенной работой — остановись и дай владельцу выбрать модель**
  (D-075) через `AskUserQuestion`; совет хука Model Advisor — один из входов,
  не вердикт (он занижает: считает по длине промпта). Не нужно на
  разговорных ответах и продолжении выбранного пункта меню.

- **Данные**: ничего не выдумывать. Пустое поле лучше выдуманного. Источник
  истины — сайт самой компании, не сторонние списки и не агрегаторы. Значение без
  источника — мнение, а не данные: у `priceBand` и `founded` это теперь инвариант
  сборки, а не договорённость (D-045, D-047).
- **Деплой, платные ресурсы, рассылки** — только с явного разрешения владельца.
  Одобрение на один узел не переносится на следующие автоматически (D-022).
- **Архитектурные изменения и правки схемы** — только с записью в `DECISIONS.md`.
- **Проверять живьём**, а не по коду: почти каждая находка этой сессии
  (D-014, D-020, D-040, D-046, D-047) была невидима зелёным тестам.
- **Новая страница** обязана попасть в `gen-a11y-sitemap.mjs` И `audit-own-a11y.mjs`
  — у обоих свои захардкоженные списки. Ловит `scripts/page-lists.test.mjs`.
- **Справочник `/wcag/[criterion]`** (D-066) строится ТОЛЬКО из
  `en301549-coverage.json`; порог thin-content (`status !== 'none'`) охраняет
  `scripts/wcag-pages.test.mjs` — новый `ours`-модуль воркера требует описания
  в `src/lib/wcag.ts`, иначе тест красный.
- Агенты возвращают компактный контракт (≤40 строк), детали пишут в `domains/*.md`.

## Ключевые файлы

| Файл | Что там |
|---|---|
| `STATUS.md` | фактическое состояние, что проверено командами |
| `DECISIONS.md` | журнал решений, причины и последствия |
| `GRAPH.yaml` | граф задач; Фаза 0 закрыта (D-094), следующий свободный узел — `A0-GSC` |
| `BACKLOG.md` | все фазы; `ROADMAP.md` — критерии выхода фаз |
| `INTERFACES.md` | контракты API/D1/типов |
| `domains/backend.md` | реализация сканера, команды деплоя |
| `domains/architecture.md` | целевая архитектура (главный документ) |
| `EN301549-COVERAGE.md` | карта покрытия стандарта (генерируется скриптом) |
| `research/STATE.md` | legacy-статус каталога |
| `docs/deepdive-accessibility-directory.md` | обоснование ниши, монетизация |

## Что стоит знать про последние итерации

Подробности — в `DECISIONS.md` по указанным номерам; здесь только то, что меняет
поведение будущей сессии.

- **РЕБРЕНДИНГ: AccessAtlas → Verscala, домен verscala.com, A0-ORIGIN done**
  (D-091, 2026-08-08). Владелец выбрал имя Verscala из ~30 проверенных
  кандидатов (полная история отбора — D-091/STATUS) и купил домен. 18
  файлов user-facing строк переименованы (вкл. email-тексты воркера и
  User-Agent бота VerscalaBot), noindex снят с /imprint/ (sitemap 415),
  воркер редеплоен с CORS на verscala.com — подтверждено живым preflight.
  Keyword-рефреш DataForSEO тем же днём (данные — domains/growth.md):
  голое `rgaa` 8100/мес → новый fr-гайд rgaa-guide.md (D-090), голое
  `wcag` большое на всех рынках, новые рынки ES/SE/FI/DK ≈ +9000/мес.
- **Юр-реквизиты Imprint опубликованы, `A0-OWNER-LEGAL` частично закрыт**
  (D-089, 2026-08-08). Владелец передал напрямую в чат: Murman Express Inc
  (California corporation), адрес, представитель (Evgenii Skliarov, CEO),
  email, телефон — всё реально на `/imprint/`. Живой факт-чек §5 DDG (не по
  памяти) подтвердил требование указывать представителя юрлица, с двумя
  реальными примерами (eRecht24, sipgate). **Побочная находка, требующая
  внимания будущей сессии:** юрлицо оказалось американским (California), не
  европейским — секции Legal basis/Enforcement body на
  `/accessibility-statement/` честно называют это открытым вопросом
  (юрисдикция ЕС над неевропейским оператором не решена с юристом), не
  выдумывают орган. `index={false}` на `/imprint/` НЕ снят — снимает его
  только `A0-ORIGIN`, который по-прежнему ждёт домен, не юр-данные.
- **`worker:deploy` реально выполнен, `CN-SCAN-PHASES` — done** (D-088,
  2026-08-08). Владелец не стал продолжать `G-I18N` (сузили через
  `AskUserQuestion`, выбрал «переключиться»), взял `worker:deploy` вместо
  `G-PITCH-DECK`, затем сам выдал `CLOUDFLARE_API_TOKEN` in-memory прямо в
  чат по нашей рекомендации минимального scope токена (Workers Scripts/D1/
  Workers KV Storage/Browser Rendering, Edit — НЕ «Select all permissions»).
  5 миграций (`0003`…`0007`) применены на реальной D1, воркер задеплоен.
  Оба долгожданных исправления — пофазный прогресс (D-067) и правовая
  пометка юрисдикции на находке «заявления нет» (D-040) — подтверждены
  ЖИВЬЁМ на проде реальными сканами, не по логу деплоя. (`ALLOWED_ORIGIN`
  тогда был плейсхолдером; исправлен на verscala.com в D-091.)
  Секрет использован строго in-memory, ни разу не на диске/в коммите.
- **Тема ТОЛЬКО светлая** (D-073, 2026-08-08) — прямое указание владельца,
  реверс части D-072 того же дня. `@media (prefers-color-scheme: dark)` в
  `src/styles.css` удалён целиком; сайт рендерит светлые токены при любой
  системной теме посетителя. `audit-a11y` — ОДИН проход (light). **Не
  возвращать тёмную тему без нового решения владельца**; dark-значения лежат в
  `BRAND_BOOK.md` §2 как исторические — это источник для возможного возврата,
  но не описание текущего кода.
- **Применён brand book владельца: CN-BRANDBOOK — done** (D-072, 2026-08-08).
  Токены — точные значения из 31 Stitch-макета владельца; спецификация —
  `docs/project/BRAND_BOOK.md`, источник правды — `domains/design.md`
  (палитра D-063 superseded, принципы сохранены).
  Правила, которые обязана знать новая сессия: компоненты стилизуются ТОЛЬКО
  токенами (slate/hex-классы запрещены); кнопки/чипы — pill с mono-лейблом
  (`.label`, JetBrains Mono self-hosted), карточки — компактные радиусы;
  severity-пары при смене палитры пересчитывать скриптом, не на глаз.
  Отклонения от макетов (CDN, Material Symbols, выдуманная телеметрия) —
  сознательные, список в D-072. `pitch_deck_*`-макеты — ОТДЕЛЬНЫЙ инвесторский
  артефакт, не сайт: `G-PITCH-DECK` todo в BACKLOG, ждёт решения владельца.
- **Библиотека компонентов — ЗАКРЫТА ПОЛНОСТЬЮ: 13/13 готовых компонентов**
  (D-068 Accordion/Tabs/Modal, D-070 Toast, D-074 Tooltip, D-077 Breadcrumbs,
  D-078 Combobox, D-079 Menu button, D-080 Select/listbox, D-081 Form field,
  D-082 Switch, D-083 Pagination, **D-084 Data table**). `CN-COMPONENTS-REST`
  10/10 — узел закрыт, следующая новая страница библиотеки не заводится, пока
  не появится 14-й пункт §22 или явное решение владельца. Каждый компонент
  реализован субагентом (модель — выбор владельца через `AskUserQuestion` на
  каждый узел: Opus для Combobox как самого рискованного, Sonnet для
  остальных девяти), результат каждый раз перепроверен родительской сессией
  живьём — диф построчно, полный набор гейтов, независимый Playwright-скрипт,
  написанный заново (не переиспользован от субагента). Полные обоснования
  архитектурных решений каждого компонента (virtual focus vs roving tabindex,
  disabled vs aria-disabled, extract-vs-invent) — в `DECISIONS.md` D-077…D-084,
  не дублируется здесь (см. правило файла в шапке — не разрастаться, D-048).
  Один сквозной урок серии: независимая проверка сама не застрахована от
  ошибок — четыре из последних шести узлов (Menu button/Select/Switch/
  Pagination) ловили баг в СОБСТВЕННОМ Playwright-скрипте, не в компоненте
  (сравнение с посторонним текстом, CSS-селектор не по прямым детям,
  несуществующий синтаксис, индекс без фильтра среди нескольких landmark) —
  Data table (D-084) первым в серии прошёл без единого такого случая.
- **CN-RESEARCH — три data-продукта готовы.** После закрытия компонентов
  владелец спросил про остаток проекта; сессия честно назвала
  `CN-RESEARCH`/`G-I18N` как реально доступные (не заблокированные
  владельцем). Второй кусок (D-085, `/reports/en301549-automation-coverage/`)
  — новый источник (`en301549-coverage.json`, `agencies.json` уже
  выработан первым отчётом), потребовал рефакторинга `reports.ts`/
  `ReportDocPage.tsx` на диспетчер по slug (как `DEMOS`). Цифры: 31/50
  (62%) критериев EN 301 549 гл.9 автоматизировано, та же формула, что
  `/methodology/`/`/accessibility-statement/` (`src/lib/coverage.ts`) —
  проверено чтением, не совпадением текста. Третий кусок (D-086,
  `/reports/jurisdiction-coverage-gap/`) — свежих ИСТОЧНИКОВ больше не
  осталось, владелец выбрал новый УГОЛ: синтез `agencies.json` +
  `worker/lib/jurisdiction.js` (13 юрисдикций, законы уже перепроверены
  по первоисточникам, D-030…D-034 — не новое юр-исследование). Технический
  приём: `jurisdiction.js` — plain ESM без браузерных API, build-time
  Node-скрипт импортирует его НАПРЯМУЮ через уже существующий публичный
  `jurisdictionForUrl()` (TLD==код страны), без нового экспорта и без
  дублирования списка законов. Честная находка: гипотеза «юрисдикции без
  специалистов» не подтвердилась — 0 из 13 с нулевым покрытием (DE
  максимум 42, FI/NO минимум по 2) — заголовок скорректирован под реальный
  результат, не под предположение. Оба прежних отчёта после каждого
  рефакторинга перепроверялись родительской сессией побайтово (`git stash
  -u`, сравнение `dist/`) — ни разу не изменились. `CN-RESEARCH` остаётся
  открытым — следующий data-продукт снова требует нового решения о
  направлении, свежих крупных источников для четвёртого пока не видно.
- **G-I18N первый безопасный кусок: немецкий chrome — done (D-087,
  2026-08-08).** Владелец попросил взять `G-I18N` целиком; полный масштаб
  (UI на 4 языках + `hreflang` + перевод сотен описаний/гайдов) оценён
  живьём и честно назван неподъёмным за одну итерацию — роутинг `/de/`
  удвоил бы сайт до ~900 страниц. Сужено дважды через `AskUserQuestion` до
  «немецкий chrome (шапка/футер) на уже немецких страницах, без новых
  URL» — закрывает реальный пробел `G-I18N-DE`. `risk: high` оправдан:
  `Layout.tsx` рендерится на КАЖДОЙ из 450 страниц. Субагент сам поймал и
  починил тонкий баг ДО показа результата: наивный новый `<Head>`-сиблинг
  в `Layout` для `<html lang>` сдвигал бы `React.useId()` у любого
  интерактивного компонента на ЛЮБОЙ странице — незаметная регрессия
  мимо визуального просмотра. Починка: `<html lang>` внутри уже
  существующего `<Head>` в `Meta` (`src/lib/seo.tsx`); `locale`
  (шапка/футер) и `htmlLang` (документ) разведены, иначе fr/pl-гайды
  потеряли бы верный `<html lang>`. Регрессия проверена родительской
  сессией дважды: полный `dist/` до/после (`git stash -u`) — ровно 6
  HTML отличаются (`/bfsg-check/` + 5 нем. гайдов), ~440 остальных
  побайтово идентичны; собранный HTML прочитан напрямую — реальный
  немецкий nav/футер/aria-label, fr-гайд честно сохранил английский
  chrome. Остаток `G-I18N` (роутинг/hreflang/перевод контента) — отдельные
  будущие решения, каждое требует нового scoping-разговора.
  **Следующий приоритет по прямому указанию владельца (когда до него
  дойдёт очередь): `worker:deploy` и `G-PITCH-DECK`, ранее сознательно
  отложенные до закрытия серии компонентов.**
- **Вторая дизайн-итерация конституции: CN-WCAG22 + CN-SCAN-STREAM — done**
  (D-064, 2026-08-07). Самопроверка сайта — теперь реально **WCAG 2.2 AA**:
  `target-size` (единственное wcag22aa-правило axe 4.13) поставляется
  ВЫКЛЮЧЕННЫМ и дефолтным `axe.run()` не гонялся — включён явно в
  `audit-own-a11y.mjs`, канарейка доказала, что гейт ловит, 26 стр. 0
  нарушений; новым интерактивным элементам нужна цель ≥24×24px или spacing,
  иначе билд красный. Поток скана на `/report/:id` — deploy-подобный
  (`ScanStream`), построен ТОЛЬКО из реальных состояний API
  (running|done|error): пофазного прогресса воркер НЕ отдаёт, выдумывать фазы
  в UI запрещено тем же принципом, что D-035/D-045. Настоящий пофазный стрим —
  узел `CN-SCAN-PHASES` (todo, `approval_required`: контракт API + деплой
  воркера = решение владельца). `prefers-reduced-motion` уважается; elapsed —
  от серверного `createdAt`.
- **Первая дизайн-итерация конституции выполнена: CN-TOKENS + CN-HERO — done**
  (D-063, 2026-08-07). Главная — scanner-first (§7: «Check your website
  accessibility» / «Scan website» / «Free instant scan. No signup required.»),
  логика отправки одна на hero и `/scan/` (`useScanForm`,
  `src/components/ScanForm.tsx`). Дизайн-система — токены `@theme` в
  `src/styles.css`; **источник правды дизайна — `domains/design.md`** (палитра
  hex, типографика, фокус, severity, анти-паттерны) — новые UI-решения сверять
  с ним. Severity теперь семантическая (critical/serious/moderate/minor +
  success/info, не акцент), фокус-кольцо всегда видимо, числа-метрики —
  `.num` (tabular-nums). Fear-заголовок главной снят. Контраст палитры —
  гейт: `audit-a11y` 26 стр. 0 нарушений; при смене палитры чинить палитру,
  не проверку. Скриншоты before/after этой итерации — scratchpad `design/`
  (вне репо). CN-NAV полностью закрыт (D-065, 2026-08-07): ярлыки
  Knowledge/Experts в UI; URL /guides/*//agencies/* сознательно не менялись —
  смена URL, если понадобится, — отдельное решение владельца (301 + sitemap).
  CN-COMPONENTS начат ПЕРВЫМ КУСКОМ (D-068, 2026-08-08): публичная библиотека
  доступных компонентов (§22) — каркас `/components/` + `/components/[slug]` из
  `data/a11y/components.json` (конвенция как guides/wcag) + 3 из 13 реально
  доступных компонента (Accordion/Tabs/Modal). Живые примеры — настоящие
  React-примитивы (`src/components/library/`), код показан их же исходником
  (`?raw`). Собственная a11y — постоянный гейт: индекс + 3 страницы в
  `audit-own-a11y.mjs`, открытая модалка тоже (INTERACT-хук); гейт поймал 3
  реальных нарушения — починены. 10 остальных — честный `Planned` без страниц;
  остаток — узел `CN-COMPONENTS-REST` (по одному за раз). Новый гейт —
  `scripts/components.test.mjs`. **1/10 остатка закрыт (D-070, 2026-08-08):
  Toast / status message** — ARIA live-region (`role=status`/`alert`), примитив
  `src/components/library/Toast.tsx`, INTERACT-гейт по `role=alert` в
  `audit-own-a11y.mjs`; индекс `/components/` = 4 ready, осталось 9 planned.
- **CN-RESEARCH — первый честный data-продукт** (D-071, 2026-08-08):
  `/reports/verified-audit-market` + индекс `/reports/`. Построен на САМОМ
  каталоге (245 записей), не на корпусе сканов чужих сайтов (которого нет,
  D-010) — честно назван разбором наших данных, не бенчмарком чужих сайтов
  (запрещено делать site-scores из ничего, D-045/D-047). Все числа считаются
  `scripts/reports-data.mjs` (agencies.json → `data/a11y/reports.json`),
  страница читает снапшот; гейт `scripts/reports-data.test.mjs` пересчитывает и
  падает на любом расхождении/ручной правке (доказан негативно). Конвенция §43:
  редакционные метаданные (title/dek) в `src/lib/reports.ts`, числа в
  JSON-снапшоте — разделены. **CN-RESEARCH больше НЕ зависит от `A0-DEPLOY`** —
  отчёт на своих данных деплоя не требует (как G-GUIDES). «Reports» в главной
  навигации. Новая поверхность — под тем же правилом: попадает в
  `gen-a11y-sitemap.mjs` (slug регексом по reports.ts) И `audit-own-a11y.mjs`.
- **Принята Design Constitution владельца** (D-062, 2026-08-07):
  `docs/project/DESIGN_CONSTITUTION.md` — направляющий документ продукта/
  дизайна (позиционирование Accessibility Intelligence Platform, UX, IA,
  дизайн-система, SEO, стадии). Вердикт: дополнить, не заменять (§62 сам
  требует сохранять архитектуру). Карта соответствия —
  `domains/architecture.md`; дельты — 8 CN-задач в `BACKLOG.md`; первый
  исполнимый узел — `CN-HERO` (scanner-first главная, домена не требует).
  Сверяй новые UI/контент-решения с конституцией; при коллизии с
  data-инвариантами (D-035/D-045/D-047) — инварианты главнее, конституция
  сама подчиняет себя evidence-принципу (§5.5).
- **У AccessAtlas теперь своё заявление о доступности** (D-060, 2026-08-07,
  `/accessibility-statement/`, узел `A0-A11Y-STATEMENT` done). Повод —
  платное Featured через Stripe подняло вопрос e-commerce-service по EAA для
  самого сайта. Живой фактчек первичных текстов (Art. 3(30) EAA / §2 Nr. 26
  BFSG) показал: Featured продаётся B2B (агентствам), строго по тексту
  закона это НЕ включает классификацию — страница честно называет обе
  стороны вопроса, не разрешает в свою пользу, публикуется независимо от
  исхода. Идентификация Anbieter и надзорный орган — оба ссылаются на
  `/imprint/`, явно помечены как ожидающие `A0-OWNER-LEGAL`, ничего не
  выдумано. Цифры статуса соответствия (0/26, 31/50) берутся из реального
  источника (`coverageSummary`), не вписаны руками.
  ⚠ **Прецедент, стоит запомнить**: отклонённый вызов `Agent`/`Skill` может
  всё равно частично выполниться на стороне харнесса (forked execution),
  прежде чем интеррапт до него дойдёт — файлы этой задачи появились на диске
  ПОСЛЕ явного отказа пользователя от tool-use. Не считай отказ гарантией,
  что ничего не произошло — проверь `git status`/`git diff` перед тем, как
  писать поверх того же самого вручную (у этой сессии в моменте образовался
  дубликат `id: A0-A11Y-STATEMENT` — ручная заглушка поверх уже готовой
  работы, найдено и убрано только при проверке).
- **`G-GUIDES` выбран над `G-I18N` для growth-работы вне графа, первый гайд
  готов** (D-056, 2026-08-07). Причина в двух словах: `G-I18N`'s `hreflang`
  бессмыслен без реального домена (тот же блокер, что `A0-DEPLOY`), а гайды —
  плоские `.md`, растут независимо от домена. Новый узел GRAPH.yaml:
  `G-GUIDES-BFSG-STATEMENT` (done).
- **`G-GUIDES` второй кусок — EAA-кластер для всех 8 юрисдикций D-031 закрыт**
  (D-057, 2026-08-07, узел `G-GUIDES-EAA-8`): по одному гайду на IT/IE/AT/BE/
  SE/DK/FI/NO, все 8 юридических цитат перепроверены живьём (не по коду), одна
  реально мёртвая ссылка найдена и исправлена (AT, RIS deep-link), хаб
  `european-accessibility-act-guide.md` расширен ссылками на все 8.
- **`G-GUIDES` третий кусок — VPAT-кластер закрыт** (D-059, 2026-08-07, узел
  `G-GUIDES-VPAT-CLUSTER`): новый гайд `vpat-en-301-549-mapping-guide.md`
  связывает ACR-структуру с clause-нумерацией EN 301 549 и WCAG (факты — из
  скачанных `.docx`-шаблонов ITI, не со слов существующих гайдов), отличает
  VPAT от Barrierefreiheitserklärung/RGAA déclaration только ссылками, без
  дублирования фактов. Побочная находка исправлена сразу: `en-301-549-explained.md`
  называл черновик EN 301 549 V4 датой ноябрь 2025 — первичный PDF ETSI
  показал более новый final draft (V4.1.0, 2026-06, фаза Vote) — исправлено.
  Разовый axe-прогон поймал `empty-table-header` до коммита — исправлено.
- **`G-GUIDES` четвёртый и последний заявленный кусок закрыт** (2026-08-07,
  узел `G-GUIDES-BFSG-MUSTER`): новый гайд
  `barrierefreiheitserklaerung-muster-checkliste.md` — Platzhalter-Muster-
  Formulierungen для всех пяти пунктов Anlage 3 плюс 7-пунктовая
  Veröffentlichungs-Checkliste. Пять пунктов Anlage 3 не пересказаны заново
  дословно — только ссылка на `barrierefreiheitserklaerung-bfsg-anlage3.md`
  плюс практический слой поверх, как требовал узел. Встречные ссылки в обе
  стороны из `barrierefreiheitserklaerung-bfsg-anlage3.md` и
  `bfsg-pflichten-guide.md` (анти-orphan). Интеграция без ручных списков —
  `src/lib/guides.ts` глобит директорию, `routes.tsx`/`gen-a11y-sitemap.mjs`
  не тронуты. Новых юридических фактов не добавлено (только практический
  слой поверх уже перепроверенных фактов), живой WebFetch-фактчек этим узлом
  не требовался и не проводился. `G-GUIDES` теперь **полностью закрыт**
  (все 4 заявленных куска: BFSG-STATEMENT/EAA-8/VPAT-CLUSTER/BFSG-MUSTER).
- **Побочная находка D-057 закрыта сразу же, не отложена** (D-058,
  2026-08-07, `G-GUIDES-FINE-SWEEP`): `audit-rgaa-guide.md` и строка Германии
  в хабе называли суммы штрафов (50 000 €/25 000 €/EUR 100 000 со ссылкой на
  блог юрфирмы) вопреки D-035 — написаны раньше этого решения, не сверены
  задним числом. Переписаны без цифр. **Добавлен постоянный гейт**
  `scripts/no-fine-amounts.test.mjs` вместо третьего подряд разового фикса —
  падает, если в строке гайда одновременно есть сумма и слово о штрафе;
  легитимный контент (порог микропредприятий, цены аудита) не ловится.
- **Полный аудит `DECISIONS.md` ↔ `BACKLOG.md`/`GRAPH.yaml` по всем 56
  решениям проведён, новых расхождений не найдено** (D-056, 2026-08-07):
  единственные известные случаи (`G-HEADCOUNT`, `G-CERT-UNKNOWN`/
  `G-CERT-FRAGILE`) были синхронизированы раньше той же итерацией (D-054/
  D-055). Разовый аудит — не постоянный процесс; если нужен постоянный, это
  отдельное решение на будущее.

- **Сканер меряет то, с чего начинает надзор** (D-030…D-039). Раньше отдавал
  100/100 сайту без заявления о доступности. Покрытие EN 301 549 — 31 из 50
  критериев, считается скриптом из живых метаданных axe-core, не руками.
- **Суммы штрафов не показываются нигде и не должны** (D-035). Причина не
  осторожность, а проверка: из 13 юрисдикций сумму удалось подтвердить для одной,
  и даже у неё есть условия применимости, которых из скана не видно.
- **Бейдж доверия перепроверен весь** (D-042…D-044). 9 записей были
  самоаттестацией — доказательство лежало на домене самого агентства. Сборка
  теперь падает на таком.
- **LinkedIn отвергнут как источник** (D-047) — не по вкусу, а по замеру:
  расхождение с сайтом агентства в четверти сверяемых случаев.
- **`founded` — очередь `== null` полностью исчерпана** (D-051, D-052, D-053):
  109/245, все 245 записей проверены индивидуально (или заполнены, или явно
  отклонены с причиной). Метод глубже первого прохода D-047 (Impressum/
  реестр/страница истории) дал ~24% попаданий на проверенных записях за три
  прохода. Дальше поле растёт только с НОВЫМ источником/методом, не повторным
  обходом тех же публичных страниц — журнал по каждой из 245 записей в
  `domains/data.md`.
- **Оценка стоимости была раздута нашими же проверками** (D-046). Пороги
  калибровались под старый набор правил; типовой сайт поехал с «€10–30k» на
  «€30k+». Теперь есть тест на исход, а не только на формулу.
- **`headcountBand` удалён** (D-048): невидимое поле без источников, чья шкала —
  таксономия LinkedIn. Прежде чем собирать поле, проверь, что оно куда-то идёт.
- **Немецкий текст `/bfsg-check/` вычитан носителем** (D-049). Приняты 7 правок
  языка, отклонены 3 — все три улучшали не язык, а силу утверждения. Вычитку
  принимай построчно, сверяя с кодом: обе присланные версии одинаково врали про
  `.com` (сканер там не «промахивается», а возвращает `country: 'unknown'`).
- **Сводка списка убивает список** (D-051). «72 записи LinkedIn-only» из D-047
  сохранились в репозитории только числом и 11 примерами через многоточие —
  повторить работу по ним стало невозможно, пришлось реконструировать полный срез
  `founded == null` (180 записей). Результат sweep-задачи пиши построчно в
  `domains/*.md`, а не пересказывай числом.
- **Запрет в промпте субагенту — не ограничение** (D-051). Пятерым агентам было
  прямо сказано «не редактируй файлы, только исследуй»; четверо всё равно
  отредактировали `LEARNING_LOG.md`. Проверяй `git diff` по всему дереву после
  каждого делегированного пакета, а не только по ожидаемым файлам.
- **Непроверенное ≠ отклонённое** (D-052). При параллельном fan-out часть
  пакетов может сорваться на внешнем лимите (API session limit, не ошибка
  метода) — такие записи остаются в очереди как «не начаты», их нельзя
  молча слить с записями, которые проверили и не нашли публикации.
- **Граф `GRAPH.yaml` вновь пуст на незаблокированных узлах** (было уже
  дважды в этом проекте — D-054, затем снова после D-061, 2026-08-07): все
  узлы `done`, ждут владельца (`A0-OWNER-LEGAL`/`A2-STRIPE-LIVE`), `review`
  в ожидании пассивного наблюдения, или `todo`, но функционально заблокированы
  доменом (`A2-OUTREACH-SEND`). `G-GUIDES-BFSG-MUSTER` (D-061) закрыл
  последний известный «свободный» кусок работы — весь `G-GUIDES` теперь
  `done` (4/4 куска). Следующая содержательная работа требует либо ввода
  владельца, либо НОВОГО решения о направлении роста, не автоматического
  продолжения графа: кандидат — `G-I18N` (отложен в D-056, масштаб не
  оценён), либо смысловая вычитка нового немецкого гайда носителем языка
  (по прецеденту D-049), либо периодическая проверка `A0-OWNER-LEGAL`.
- **`BACKLOG.md` может отставать от `DECISIONS.md`** (D-054): `G-HEADCOUNT`
  стоял `todo`, хотя решение фактически принято раньше в D-048 (headcountBand
  удалён, не заполнен) — синхронизировано только этой итерацией. Перед новой
  задачей по бэклогу проверяй `DECISIONS.md`/`git log`, не только статус в
  таблице.
- **`G-CERT-UNKNOWN`/`G-CERT-FRAGILE` тоже были закрыты решением, но не
  бэклогом** (D-055, 2026-08-07): D-043/D-044 закрыли обе задачи ещё
  2026-08-06, `BACKLOG.md` остался `todo (low)`. Переподтверждены живьём
  заново (не по тексту старого решения): `akademia-slonca` усилена —
  Cloudinary-PDF наконец прочитан через `pdftotext`, дословно называет
  исполнителя; `ebizproduction` остаётся барьером песочницы
  (`web.archive.org` целиком недоступен инструменту сессии), не данных.
  `BACKLOG.md` теперь пуст на `todo` в разделе «Немецкий рынок»/данных.
- **Прерванный агент = молчание, не ошибка** (D-053). Оставшиеся 80 записей
  G-FOUNDED-LI дважды сорвались подряд без единой находки — не на лимите API
  (как D-052), а на пользовательском tool-use interrupt (переключение
  `/model`), пришедшем во все 5 фоновых агентов разом. `ListAgents` после
  этого показывал «No reachable agents», как будто они завершились штатно.
  Проверка транскрипта (`agent-<id>.jsonl`, последние 2-3 записи) показала
  `[Request interrupted by user]` вместо итогового ответа — перезапуск с
  нуля был единственным рабочим путём. Очередь теперь исчерпана полностью
  (245/245 записей проверены индивидуально, 109 заполнены) — `domains/data.md`.

## Живой skill (D-012)

Оркестратор (`.claude/commands/`, `.claude/agents/`) — не статичный шаблон;
владелец просил улучшать его по ходу. Нашёл общий, переносимый на другие проекты
урок (не деталь AccessAtlas — той место в `DECISIONS.md`/`domains/*.md`) —
дополни соответствующий файл и отметь строкой с датой ниже.

- 2026-08-05: первая установка + уроки D-009/D-010/D-011 вплетены в
  `project-orchestrator.md`, `data-engineer.md`, `frontend-engineer.md`,
  `devops-engineer.md`, `qa-engineer.md`, `product-lead.md`.
- 2026-08-06 (D-023): в `software-architect.md` — про flow «запрос → секретный
  токен по email → verify»: если API синхронно возвращает то же значение, что
  должно уйти только письмом, верификация тривиально обходится.
- 2026-08-06 (D-024): в `devops-engineer.md` — проверять песочные ограничения
  email-сервиса безопасным пробником, а не отправкой живому человеку.
- 2026-08-06 (D-030): в `backend-engineer.md` — текстовые эвристики проверять на
  реально полученном тексте (свои же фикстуры всегда проходят свой же паттерн);
  состояние браузера читать атомарно, в одном `evaluate()` (TOCTOU).
- 2026-08-06 (D-050): в `project-orchestrator.md` (новый Шаг 4B), `workflow-graph.md`
  (правила 9–11, поля `node_kind`/`on_failure`) и `task-loop.md` — пять принципов из
  присланного скилла `agent-graph-engineering`: сила проверки как главный рычаг
  (проверка обязана уметь провалиться), не каждый узел требует LLM, независимый
  критик вместо самопроверки, восстановление ≠ повтор, сдержанность (граф нужен не
  всегда). Учебный маршрут и привязка к LangGraph отклонены как не наши правила.
- 2026-08-07 (D-052): в `project-orchestrator.md` (Шаг 6) — сорвавшийся на
  внешней причине пакет fan-out не то же самое, что пакет, давший
  отрицательный результат; отчёт обязан явно различать «отклонено» и «не
  начато», иначе несделанная работа тихо читается как проверенная.
- 2026-08-07 (D-053): в `project-orchestrator.md` (Шаг 6) — прерванный
  посторонним interrupt фоновый агент выглядит как молча завершившийся
  (`ListAgents` не показывает его вовсе), а не как ошибка; перед тем как
  считать его молчание результатом, проверь последние записи транскрипта
  на `[Request interrupted by user]` вместо итогового ответа.
