# HANDOFF — что нужно знать новой сессии

Обновлено: 2026-08-06 · Проект: **AccessAtlas** (ветка `accessatlas`; в `main` — чужой
проект detnav, не трогать). Рабочая ветка итерации: `claude/accessatlas-architecture-sk6tcj`.

## Как продолжить

> Работай в ветке от `accessatlas`. Прочитай `docs/project/HANDOFF.md` и `STATUS.md`,
> затем `/project-orchestrator Продолжи проект: выбери следующий незаблокированный узел
> из docs/project/GRAPH.yaml, выполни через /task-loop, обнови handoff.

`/project-orchestrator`, `/workflow-graph`, `/task-loop`, `/task-profiler`,
`/next-step-coach` теперь реально установлены в этом репозитории
(`.claude/commands/`, `.claude/agents/`, D-012) — не нужен внешний плагин/флаг.
Хук Model Advisor подключён в `.claude/settings.json`, если новая сессия не видит
подсказку в начале ответа — откройте `/hooks` один раз (перезагружает конфиг) или
перезапустите Claude Code (см. каверзу с file watcher в `update-config` skill).

Проверка среды: `npm install && node scripts/build-a11y.mjs` (ожидать: **245** агентств).

## Что это

Платформа web-доступности (EAA/WCAG/ADA/BFSG/RGAA), эволюция
**Directory → Decision Engine → Lead Marketplace → Vertical SaaS** (VISION.md, D-003).
Слой 1 (каталог) построен и **готов к деплою** (ждёт только Imprint/домен от
владельца). Слой 2 (сканер) — **реально задеплоен и работает** на аккаунте владельца:
`https://accessatlas-worker.zincroom.workers.dev` (D-021). Слои 3–4 не начаты.

## Состояние на сегодня

**Фаза 0 (агентские узлы) закрыта** (A0-ENRICH, A0-CI-FIX, A0-DESC-REST — все `done`).
Единственный оставшийся блокер запуска каталога — на владельце: `A0-OWNER-LEGAL`.

- Данные: описания **245/245 (100%)**, было 81 в начале дня 05-08. 8 записей без
  реального сервиса доступности удалены из каталога, занесены в `excluded.json`.
  253→245 агентств.
- CI: был скопирован целиком с `main` (detnav), звал несуществующие/неприменимые
  скрипты. Починено (D-009), зелёный локально.
- **A1-SCAN (сканер)**: status **done** (D-021) — реально задеплоен на аккаунт
  владельца 2026-08-06 (`https://accessatlas-worker.zincroom.workers.dev`): D1
  `accessatlas-scans`, KV `RATE_LIMIT_KV`/`EXPLAIN_CACHE` созданы, миграции
  применены, реальные id закоммичены в `wrangler.jsonc`. Настоящий скан
  `https://example.com` через Browser Rendering нашёл 2 подлинные axe-core
  находки (`landmark-one-main`, `region`), score 94, подтверждено прямым SELECT
  из D1. Ошибочный путь тоже проверен живьём (несуществующий домен → `error`/
  `refused`). Закрывает главный пробел D-010.
- **A1-REPORT (`/report/:id`)**: status **done** (D-021) — реализован и
  верифицирован независимо (6 состояний, Playwright + подменённый API, 0
  нарушений axe-core). Заодно найден и исправлен реальный баг контраста
  (D-014). Живой `ScanReport` с реального воркера побайтово совпал с контрактом,
  который страница уже потребляла в мокнутых тестах — единственный
  недостающий кусок (A1-SCAN live) закрыт. Один честно незакрытый остаток:
  реальный браузер этой песочницы не может достучаться ни до какого внешнего
  HTTPS вообще (тот же класс ограничения, что у Browser Rendering, D-010) —
  полный browser+live-backend E2E непроверяем именно в этой среде, не баг.
- **A1-LANDING (`/scan`)**: status **review** — форма отправки URL, Turnstile
  (деградирует без `VITE_TURNSTILE_SITE_KEY`), ссылка в шапке, честный раздел
  «что делает / не делает», без выдуманных функций (без email-поля — сервер его
  не использует, D-015). 4 сценария верифицированы живьём. Реальный внешний
  скрипт Turnstile всё ещё не проверен — тот же сетевой барьер песочницы
  (подтверждён отдельно на example.com при попытке E2E A1-REPORT, см. выше).
- **A1-EXPLAIN**: status **done** (D-020, D-021) — владелец дал отдельное
  одобрение платного `ANTHROPIC_API_KEY`. Live-прогон нашёл настоящий баг:
  Claude Haiku оборачивает JSON в markdown code fence вопреки промпту «ONLY a
  JSON object» — каждый cache-miss падал в 502. Исправлено (`stripCodeFence`),
  5 новых фикстур, 39/39 worker-тестов зелёные. Тот же ключ загружен как
  реальный Worker secret на живой деплой и подтверждён запросом — объяснение
  для `landmark-one-main` (ровно то правило, что нашёл живой скан) пришло
  адекватное. Наблюдение: секрет пропагируется по сети Cloudflare не мгновенно
  (несколько 503 сразу после `secret put`, прошло за секунды).
- **A1-COST**: status **done** — оценка стоимости на `/report/:id`, один price
  band по effort-score (D-017, уточняет D-006). 5 сценариев верифицированы
  живьём против ручного расчёта. Зависел только от A1-REPORT, теперь done.
- **A1-MATCH**: status **done** — блок «подходящие агентства» на `/report/:id`
  + бейджи верификации на `AgencyCard` (D-018). 5 сценариев верифицированы
  живьём против реальных `agencies.json`, включая настоящий empty-state
  (Австралия+remediation). Зависел только от A1-REPORT, теперь done.
- **A1-PRIVACY**: status **done** — `PrivacyPage.tsx` переписан по реальному коду
  сканера (не по предположению): retention 90 дней, IP только транзитно в KV,
  Turnstile при включении шлёт IP в Cloudflare и может ставить куку (старая
  формулировка «no cookies» была неточной, уточнена).
- **A1-RETENTION**: status **review** — ежедневный Cron Trigger (`0 3 * * *`)
  удаляет сканы старше 90 дней (`worker/lib/retention.js`, D-019). 5 юнит-тестов
  на фейковом D1, 39/39 worker-тестов зелёные. Реальный деплой 2026-08-06
  подтвердил Cron Trigger зарегистрированным на аккаунте владельца (`schedule`
  в выводе `wrangler deploy`). Не done: само срабатывание по расписанию (03:00
  UTC) ещё не пронаблюдано в рамках этой сессии.
- **Фаза 1 фактически закрыта по коду**: единственный оставшийся узел в review —
  A1-LANDING (внешний Turnstile-скрипт) и A1-RETENTION (ждёт первого реального
  срабатывания по расписанию) — оба не требуют новой работы, только времени/
  внешнего наблюдения.
- **Вынос AccessAtlas в отдельный репозиторий** (`kabluk/accessatlas`, приватный) —
  начат по просьбе владельца, не завершён. GitHub-интеграция сессии (`add_repo`)
  временно не отвечает (`MCP error -32003`, воспроизведено ~6 раз на разных вызовах
  и репозиториях — похоже на сбой всего коннектора, не конкретного репо). Пустой
  репозиторий `kabluk/accessatlas` уже создан владельцем. Как только интеграция
  отвиснет — довести: `add_repo(push)` → перенести `claude/accessatlas-architecture-
  sk6tcj` как `main` → обновить ссылки на расположение репо в CLAUDE.md/README.md/
  research/STATE.md (заодно уйдёт лишняя оговорка про detnav) → закрыть RISKS.md R9.

## Ключевые файлы

- `docs/project/GRAPH.yaml` — граф задач; review: `A1-LANDING`, `A1-RETENTION`;
  blocked на владельце: `A0-OWNER-LEGAL` → `A0-ORIGIN` → `A0-DEPLOY` → `A0-GSC`
- `docs/project/domains/backend.md` — детали реализации сканера, команды деплоя
- `docs/project/BACKLOG.md` — все фазы; `ROADMAP.md` — критерии выхода фаз
- `docs/project/domains/architecture.md` — целевая архитектура платформы (главный документ)
- `docs/project/INTERFACES.md` — контракты API/D1/типов (ScanReport — уже не черновик)
- `research/STATE.md` — legacy-статус каталога (подробности Рубежей 1–3)
- `docs/deepdive-accessibility-directory.md` — обоснование ниши (7.9/10), монетизация

## Правила

- Данные: ничего не выдумывать; enrich заполняет только пустые поля; excluded.json уважать.
  Прецедент проверки: единственный источник истины — сайт самой компании, не сторонние списки.
- Архитектурные изменения — только с записью в DECISIONS.md.
- Деплой/платные ресурсы/рассылки — только с явного разрешения владельца. Одобрение платных
  ресурсов CF получено 2026-08-05 для A1-SCAN, реальный деплой выполнен 2026-08-06 по
  прямой просьбе владельца (D-021) — не распространяется на будущие узлы автоматически.
- Агенты возвращают компактный контракт (≤40 строк), детали пишут в свой domains/*.md.

## Следующие шаги (по приоритету)

1. ~~Один живой прогон сканера~~ — **сделано 2026-08-06** (D-021): реальный деплой
   на аккаунт владельца, настоящий скан example.com нашёл реальные находки.
   A1-SCAN/A1-REPORT/A1-COST/A1-MATCH все теперь done.
2. ~~Спросить владельца про ANTHROPIC_API_KEY~~ — **сделано 2026-08-06**: владелец
   одобрил, живой прогон нашёл и исправил реальный баг (D-020), секрет загружен
   в прод (D-021), A1-EXPLAIN done.
3. Спросить владельца: реквизиты Imprint + домен — единственное, что блокирует деплой
   каталога (снимает A0-OWNER-LEGAL → A0-ORIGIN → A0-DEPLOY → A0-GSC разом). Тот
   же реальный домен нужен и для `ALLOWED_ORIGIN` в `wrangler.jsonc` — сейчас
   воркер отвечает только на прямые API-запросы (curl/Postman), не с браузерной
   страницы каталога (CORS заперт на `accessatlas.example`-плейсхолдер).
4. Довести перенос репозитория, когда отвиснет GitHub-интеграция (см. выше).
5. Фаза 1 фактически закрыта по коду: `A1-LANDING` (внешний Turnstile-скрипт) и
   `A1-RETENTION` (ждёт первого реального срабатывания по расписанию 03:00 UTC)
   остаются в review, но не требуют новой работы — только времени/наблюдения.
   Новых кодовых узлов Фазы 1 не осталось.
6. Фаза 2 начата 2026-08-06: `A2-LEAD-SCHEMA` **done** (`migrations/0003_leads.sql`,
   таблица `leads` из INTERFACES.md §4, только `--local`). `A2-LEAD-FORM` **done**
   (`/request-quote/`): чистый UI, клиентская валидация, client-only превью
   совпадений, 0 сетевых вызовов при submit. `src/lib/data.ts` (добавлен
   `paths.requestQuote()`) и `src/routes.tsx` (маршрут `/request-quote`) тронуты
   за пределами буквального scope узла — раскрыто явно в GRAPH.yaml notes, не
   тихо (тот же прецедент, что scope `A1-LANDING` уже включал `routes.tsx`).
   Изначально был не done — два пробела вне буквального scope узла: страницы
   не было в `scripts/audit-own-a11y.mjs`/`gen-a11y-sitemap.mjs` (постоянный
   CI-гейт и sitemap её не видели). Оба закрыты той же сессией сразу после:
   386 страниц/349 URL в sitemap, 23 в постоянном a11y-гейте, всё зелёное.
   Заодно найден и закрыт третий, независимый пробел: страница была **orphan**
   (ни одна ссылка в UI на неё не вела) — добавлен CTA «Request a quote from
   matching agencies →» в `MatchedAgencies.tsx` на `/report/:id`, с пробросом
   `?scanId=`.
   `A2-LEAD-API` **done** (2026-08-06): `POST /api/lead` реально пишет в D1
   `leads` и возвращает `{leadId, matched: slug[]}`, тот же алгоритм матчинга,
   что `matchAgencies.ts` (переиспользован через новый `worker/lib/
   matchAgenciesServer.js` — worker не бандлится Vite, не видит `.ts`/алиасы
   напрямую), тот же rate-limit/Turnstile паттерн, что у `/api/scan`. Email
   агентствам НЕ отправляется (TODO — это `A2-LEAD-EMAIL`, approval_required).
   65/65 `worker:test`, typecheck/build чистые, живьём проверено через
   `wrangler dev --local` + прямой `SELECT` из D1 (детали: `domains/backend.md`).
   Разблокировало (технически, approval остаётся отдельным гейтом) `A2-LEAD-EMAIL`.
   `A2-OUTREACH-PREP` **done** (2026-08-06): `research/outreach-99.json` — 96
   реальных агентств (не 99, расхождение с `research/STATE.md` зафиксировано, не
   скрыто) с сертификатом `gov-declared-auditor`. 5 параллельных субагентов с
   WebFetch (fan-out по ~19-20 агентств каждый) нашли 82/96 (85%) реальных
   публичных email с указанием источника и точного URL; 14/96 честно `null` с
   объяснением, почему не найден (403, только форма/телефон, необработанная
   Cloudflare-обфускация у 2 записей, 1 мёртвый домен). `research/outreach-
   letter-draft.md` — черновик письма, ведёт с доказательства (конкретная
   декларация), не с продажи, featured не упоминается, явный opt-out — ничего
   не отправлено. Найден дополнительный, ранее не зафиксированный блокер для
   `A2-OUTREACH-SEND`: письмо ссылается на реальный домен (которого пока нет,
   `A0-ORIGIN`) и упирается в то же требование Imprint-идентичности отправителя,
   что RISKS.md R2 — применительно к email, а не только к сайту.
   `A2-CLAIM-SCHEMA` и `A2-STRIPE-SCHEMA` **done** (2026-08-06): `migrations/
   0004_claims.sql` и `0005_featured.sql`, поля 1:1 с INTERFACES.md §4.
   Применены локально, схема и ограничения подтверждены живьём через `wrangler
   d1 execute` — не только по DDL: реальный INSERT+SELECT в обеих таблицах, и
   отдельно проверено, что повторный INSERT в `featured` с тем же `agency_slug`
   реально падает на `UNIQUE constraint` (продление — `UPDATE`, не новая
   строка, как задумано). Разблокировали `A2-CLAIM-API` и
   `A2-STRIPE-WEBHOOK-CODE`.

## Живой skill (D-012)

Оркестратор (`.claude/commands/`, `.claude/agents/`) — не статичный шаблон. Владелец
явно попросил улучшать его по ходу проекта. Когда находишь общий, переносимый на
другие проекты урок (не деталь AccessAtlas — той место в `DECISIONS.md`/`domains/*.md`) —
дополни соответствующий файл в `.claude/commands/` или `.claude/agents/` и отметь
изменение здесь же пунктом с датой:

- 2026-08-05: первая установка + уроки D-009/D-010/D-011 вплетены в
  `project-orchestrator.md`, `data-engineer.md`, `frontend-engineer.md`,
  `devops-engineer.md`, `qa-engineer.md`, `product-lead.md`.
- 2026-08-06: урок D-014 («не доверяй собственному прошлому заявлению без
  независимой перепроверки другим методом») вплетён в `qa-engineer.md`.
- 2026-08-06: `frontend-engineer.md` уже содержал требование учитывать состояния
  loading/error/empty — A1-LANDING/A1-REPORT подтвердили его на практике, изменений
  в файл не потребовалось.
- 2026-08-06: правило «одобрение платных ресурсов не переносится на будущие узлы
  автоматически» (зафиксировано раньше в `project-orchestrator.md`/`devops-
  engineer.md`) сработало на практике при A1-EXPLAIN — не потребовало изменений
  в скилл, только подтвердило, что формулировка была достаточно чёткой.
- 2026-08-06: обнаружил и исправил процессный пробел — D-004/D-005 оставались
  помечены `proposed`, хотя были полностью реализованы и проверены (A1-SCAN,
  A1-EXPLAIN); статус просто забыли продвинуть после implementation. Добавлено
  в `software-architect.md`: при закрытии узла, реализующего `proposed`-решение,
  обновлять его статус на `accepted` (или пересматривать) тем же коммитом, не
  оставлять расхождение между DECISIONS.md и фактическим состоянием кода.
- 2026-08-06 (A1-MATCH): при живой Playwright-проверке селектор `a.card` на
  `/agencies/` неожиданно вернул 0 элементов — не баг кода, а неверное
  предположение теста: `/agencies/` рендерит компактный A–Z список ссылок
  (`hover:underline`), а не `AgencyCard`; полные карточки рендерит только
  `FilterableList` (страны/сервисы/стандарты/комбо-страницы). Не потребовало
  изменений в `qa-engineer.md` — принцип «не доверяй своему прошлому заявлению,
  перепроверяй независимо» (D-014) уже покрывает и это: селектор 0-результатов
  сам по себе — сигнал перепроверить предположение о структуре страницы, а не
  сразу чинить код под тест.
- 2026-08-06 (A1-EXPLAIN закрыт): ещё раз подтвердило принцип из D-014/
  qa-engineer.md на новом типе бага — live-прогон с реальным `ANTHROPIC_API_KEY`
  нашёл баг (markdown-фенс вокруг JSON), невидимый ни ревью кода, ни 31 юнит-
  тесту на синтетических фикстурах, потому что ни одна фикстура не была взята
  из реального ответа модели. Дополнено `devops-engineer.md`: конкретное правило
  безопасной передачи секрета, переданного пользователем в чат, в живую сессию
  (`wrangler dev --var KEY:value` как аргумент процесса, никогда не в файл/лог/
  коммит) + сама мысль про синтетические фикстуры vs реальный ответ системы.
- 2026-08-06 (A1-PRIVACY): подтвердило существующее правило `product-lead.md`/
  data-дисциплины «ничего не выдумывать» в новом контексте — юридический текст.
  Privacy писался чтением реального `worker/*.js`, а не по памяти о том, что
  «обычно» делает такая фича; это вскрыло, что прежняя формулировка «no cookies
  are set by this site» была неточной (Turnstile может ставить свою) и что
  «retention-политика» из RISKS.md R6 на самом деле не реализована — легко было
  бы написать Privacy так, будто она есть, раз о ней шла речь в риске. Изменений
  в скилл не потребовалось, принцип уже был сформулирован достаточно широко.
- 2026-08-06 (A2-OUTREACH-PREP): подтвердило, что дисциплина «не выдумывать»
  переживает параллельный fan-out на независимые субагенты без явного
  напоминания в каждом промпте по отдельности — все 5 агентов синхронно вернули
  честный `null` там, где email не нашёлся (403, форма-без-текста, мёртвый
  домен), никто не угадал `info@domain.com` по шаблону. Изменений в скилл не
  потребовалось — сам принцип уже сформулирован достаточно чётко в промптах
  каждого узла (см. GRAPH.yaml notes), это просто первое подтверждение на
  параллельном прогоне, а не на одном последовательном агенте.
- 2026-08-06 (первый реальный деплой, D-021): дополнено `devops-engineer.md`
  ещё одним практическим правилом — транзиентная ошибка сразу после создания/
  обновления облачного ресурса (секрет, воркер, миграция) чаще всего просто
  пропагация по глобальной сети, не баг; не чинить код в панике на первый
  провал, повторить 2-3 раза с паузой, но и не списывать молча — зафиксировать,
  что именно самостоятельно прошло. Поймано дважды за один деплой (первые
  опросы `/api/scan/:id` сразу после deploy, `/api/explain` сразу после
  `secret put`), оба раза самостоятельно разрешилось за секунды.
