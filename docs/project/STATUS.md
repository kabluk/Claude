# STATUS — фактическое состояние

Обновлено: 2026-08-05 (см. также подробный legacy-статус: `research/STATE.md`)

## Кратко

Слой 1 (Directory) построен, **не задеплоен**. Слои 2–4 не начаты. Ветка проекта —
`accessatlas`; рабочая ветка этой итерации — `claude/accessatlas-architecture-sk6tcj`.

## Проверено сегодня (командами, не по памяти)

- **A0-ENRICH выполнен** (commit `e0c9a85`): `node scripts/enrich-a11y.mjs` применён —
  119 записей изменено, +170 описаний, +34 города, +110 полей, +135 источников.
  `the-pixel` — не баг: запись корректно в `excluded.json` (не оказывает услуг доступности),
  устаревший дубль-патч в `us-batch.json` удалён.
- `node scripts/build-a11y.mjs` — ✅ 253 агентства, 120 списочных страниц (**81 индексируемых**,
  было 73). `npm run typecheck` — чисто. `npm run build` — 392 страницы, sitemap 355 URL.
- Полнота сейчас: описания **200/253 (79.1%)**, было 81. До порога 90% для снятия noindex
  нужно ещё ~28 профилей — узел `A0-DESC-REST` (ready).
- **Находка:** `package-lock.json` в репозитории отсутствовал вообще — CI (`npm ci`) не мог
  работать; добавлен тем же коммитом. Отдельно: `ci.yml` вызывает `npm run lint:upl`,
  `npm run lint:minimize`, `node scripts/check-links.mjs` — ни одного из них нет в
  `package.json`/`scripts/`. **CI сейчас гарантированно красный.** Новый узел `A0-CI-FIX`
  (ready, devops-engineer), блокирует `A0-DEPLOY`.

## Блокеры запуска (Фаза 0)

| Блокер | Владелец |
|---|---|
| ~~Применить enrich~~ | ✅ готово |
| Закрыть остаток описаний (~53, до 90%) — `A0-DESC-REST` | агенты |
| Починить CI (lockfile ✅, недостающие линтеры) — `A0-CI-FIX` | агенты |
| Реквизиты Imprint (юрлицо/адрес) | **владелец проекта** |
| Домен (замена `accessatlas.example` в `src/lib/seo.tsx`, `scripts/gen-a11y-sitemap.mjs`) | **владелец проекта** |
| Деплой-конфиг Cloudflare Pages | агенты |

## Монетизация

Не начата: нет лид-формы, claim, Stripe. По плану — Фаза 2 (ROADMAP.md).

## Инфраструктура

- DataForSEO: логин zincroom@gmail.com, баланс ~$525, ключи в env, не в репо.
  Открыт вопрос возврата $475 + сброс пароля API (засветился в чате).
- Firecrawl: ключ выдаётся в чате при необходимости.
- CI: `.github/workflows/ci.yml` есть на ветке.
