# Домен: qa

Обновлено: 2026-08-05 · Владелец: qa-engineer

Действующие проверки (запускать перед каждым коммитом с изменением данных/кода):

```bash
node scripts/build-a11y.mjs      # валидация данных: 253 агентства, 0 ошибок
node scripts/build-a11y.mjs --strict   # все замечания полноты (~674, число должно падать)
npm run typecheck                # tsc --noEmit
npm run build                    # полная сборка: данные → страницы → sitemap
```

- Скрипт проверки иерархии заголовков существовал в сессии (383/383 страниц) —
  TBD: закоммичен ли; если нет, восстановить в `scripts/`.
- Сам сайт обязан проходить свои же стандарты: axe-чистота собственных страниц —
  критерий выхода Фазы 1 (сканер сканирует и нас).
- Фаза 1 добавит: тест Worker-эндпоинтов через `wrangler dev`, фикстуры axe-результатов.
