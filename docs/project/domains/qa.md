# Домен: qa

Обновлено: 2026-08-05 · Владелец: qa-engineer

Действующие проверки (запускать перед каждым коммитом с изменением данных/кода):

```bash
node scripts/build-a11y.mjs      # валидация данных: 245 агентств, 0 ошибок
node scripts/build-a11y.mjs --strict   # все замечания полноты, число должно падать
npm run typecheck                # tsc --noEmit
npm run build                    # полная сборка: данные → страницы → sitemap
npm run check-links              # все внутренние href резолвятся (405 ссылок, 0 битых)
npm run audit-a11y               # ✅ 2026-08-05: 21 страница / 14 шаблонов, 0 нарушений axe-core
npm run worker:test              # 9/9 юнит-тестов worker/ (score.js, links.js)
```

- **`npm run audit-a11y`** (Playwright + axe-core, D-011): сам сайт обязан проходить
  свои же стандарты — критерий выхода Фазы 1 закрыт, встроен в CI как блокирующий
  шаг (не разовая проверка). Оговорка: axe-core автоматически ловит только часть
  критериев WCAG (по индустриальным оценкам — около трети), ручная проверка нужна
  отдельно и в бэклоге пока не заведена.
- Скрипт проверки иерархии заголовков существовал в старой сессии (383/383 страниц) —
  TBD: закоммичен ли; если нет, восстановить в `scripts/`.
- Фаза 1 добавит: тест Worker-эндпоинтов через `wrangler dev`, фикстуры axe-результатов.
