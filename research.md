# research.md — фактический фундамент проекта

> **Правило для всех сессий:** код, зависящий от внешнего факта (юридического,
> рыночного, о структуре формы), **не пишется**, пока факт не занесён сюда со
> статусом **VERIFIED**. Факты со статусом UNVERIFIED блокируют включение
> зависящего от них кода в прод. KILLED — проверенные и отклонённые гипотезы
> (чтобы не воскрешать). Стратегические решения — в `docs/DECISIONS.md`.

Статусы: **VERIFIED** · **UNVERIFIED** (нужна проверка) · **KILLED** (опровергнуто).

---

## VERIFIED

### Формы (AcroForm-поля)
- Реальные имена AcroForm-полей **16 привязанных форм** извлечены через
  `inspectFormFields` из официальных fillable PDF с courts.ca.gov и
  зафиксированы в `src/pdf/flXXX.js` (маппинги) + `public/forms/` (сами PDF).
  Формы: FL-100, FL-105, FL-110, FL-115, FL-140, FL-141, FL-142, FL-144,
  FL-150, FL-165, FL-180, FL-190, FL-341, FL-342, FL-343, FL-345,
  **FW-001, FW-003**.
  Каждая форма покрыта demo + read-back (`scripts/demo-flXXX.mjs` /
  `demo-fwXXX.mjs`, 0 missing).
  Актуальность ревизий сверяется по SHA-256 (`npm run check-forms`, месячный CI).
  _Источник: официальные PDF courts.ca.gov; базовый коммит привязки — 077ca7f._

### Fee-waiver: пороги дохода (FW-001, item 5b)
Проверено 2026-07-13 против `courts.ca.gov/documents/fw001.pdf`
(**FW-001, ред. 1 марта 2026**). Валовой месячный доход домохозяйства, при
котором суд освобождает от пошлины (125% федерального порога бедности):
- 1 чел. — **$2 660.00** · 2 — **$3 606.67** · 3 — **$4 553.33**
- 4 — **$5 500.00** · 5 — **$6 446.67** · 6 — **$7 393.33**
- каждый сверх 6 — **+$946.67**

  Зафиксировано в `src/data/feeWaiver.js` (`FEE_WAIVER_INCOME`). Пошлина за
  первую подачу диссолюции — **$435** (см. `src/data/counties.js`).
  Публичные пособия (item 5a), дающие право на освобождение, перечислены там же
  (`FEE_WAIVER_BENEFITS`). Основание: Gov. Code §68632 (a)/(b).
  _При смене ревизии FW-001 обновить таблицу здесь и в feeWaiver.js._

### Таймлайн-вехи (Family Code / CCP)
Проверено 2026-07-13 (leginfo.legislature.ca.gov). Анкор — дата вручения
документов ответчику (service_date):
- **Fam. Code §2339(a)** — развод не может стать окончательным раньше, чем через
  **6 месяцев** от даты вручения summons+petition ответчику ИЛИ его явки (что
  раньше). → веха `waiting_period_end` = service + 6 мес + 1 день.
- **Fam. Code §2104(f)** — предварительная декларация о раскрытии (petitioner)
  подаётся одновременно с петицией или **в течение 60 дней с подачи петиции**. →
  веха `disclosures_due` = filing + 60 дней (fallback на service_date).
- **Code Civ. Proc. §412.20(a)(3)** — у ответчика **30 дней** после вручения на
  подачу ответа (указано на summons FL-110). → веха `response_deadline`.
  Зафиксировано в `src/timeline/milestones.js` (юнит-тест
  `npm run check-milestones`). Тексты вех — фактические, в `t.milestones`
  (UPL-линт покрывает). Планировщик — Supabase pg_cron + Edge Function
  `notify-milestone` (НЕ n8n).

### Право (Business & Professions Code / Rules of Professional Conduct)
Тексты проверены 2026-07-12 (leginfo.legislature.ca.gov):
- **B&P §6402.1** — регистрант LDA обязан иметь: paralegal-сертификат
  (24+ semester units) ЛИБО бакалавриат + 1 год law-related experience под
  надзором лицензированного адвоката. → У основателя гейт **не пройден**.
- **B&P §6400(g)** — LDA прямо запрещено давать "advice, explanation, opinion,
  or recommendation ... selection of forms".
- **B&P §6400(c)(2)** — ограничения на то, кто может быть LDA / как оформлен статус.
- **B&P §6401(b)** — работа для лицензиата State Bar лежит вне LDA-главы
  (основание attorney-review модели).
- Вывод зафиксирован как решение в `docs/DECISIONS.md` (2026-07): основная
  модель — Reviewing Attorney, не LDA.

---

## KILLED (проверено и отклонено)

- **«LegalZoom не был LDA»** — ОПРОВЕРГНУТО: LegalZoom в итоге зарегистрировался
  как LDA в LA County. «Путь чистого софта» в CA заканчивается регистрацией.
- **«Аренда LDA-номера»** (использовать чужой регистрационный номер) — убита
  B&P §6408 + §6400(c)(2).
- **«Fee-splitting $299 со сплитом на бэкенде»** (единый платёж, потом делёж с
  адвокатом) — убита Rule of Professional Conduct 5.4 (запрет раздела гонорара
  с неадвокатом). Единственный чистый путь — **две раздельные транзакции**
  (реализовано в `ReviewCheckout`, деньги адвоката через нас не проходят).

---

## Vision (paystub → FL-150)

### COGS распознавания (оценка)
- Один paystub ≈ 1 изображение (~1–1.6MP) + строгий JSON-вывод. Порядок затрат
  на вызов Claude vision (opus-класс, temp 0, max_tokens 1024): **≈ $0.01–0.05**
  за извлечение (зависит от разрешения изображения и итоговой модели/цены).
  _Статус: ОЦЕНКА — уточнить по факту на актуальном прайсе перед включением в
  прод (обновить здесь). При вкладе ~$288/кейс (Essentials, см.
  gtm-unit-economics.md) один разовый vision-вызов пренебрежим._
- То же относится к `extract-service` (фото доказательства вручения → FL-115 +
  service_date, §8.4 шаг 2): тот же класс vision-вызова, та же оценка COGS и та
  же retention-политика (см. ниже).

## UNVERIFIED (блокирует зависящий код)

### Retention фото зарплатных квитанций — BLOCKING
- Срок хранения загруженных изображений НЕ утверждён. Дефолт в коде —
  **retention: none**: `extract-paystub` обрабатывает фото в памяти и НЕ сохраняет
  (`PAYSTUB_PERSIST` не выставлен). Приватный бакет + RLS по `case_id` заведены
  миграцией `0002_paystub_storage.sql`, но включение персистентности в проде
  **заблокировано** до решения о сроке (напр. удаление сразу после извлечения /
  ≤30 дней) и добавления pg_cron-свипа. До VERIFIED — `PAYSTUB_PERSIST=false`.
  То же для фото доказательства вручения (`extract-service`): по умолчанию
  retention: none, персистентность заблокирована тем же решением.


- **Структура двух транзакций чиста от fee-splitting.** Блокирует включение
  Reviewed-тира в прод. Снимается: консультацией CA-адвоката (бриф готов).
  Пока не VERIFIED — `REVIEWED_TIER_ENABLED=false` в проде (см.
  `src/config/features.js`, `docs/DECISIONS.md`).
- **Спрос hy / ko / ru** (армянский Глендейл, корейский Koreatown, русский).
  Блокирует перевод контента на эти локали. Снимается: Google Keyword Planner
  + подтверждённые каналы. Инфраструктура i18n готова (§6), контент — после
  валидации и legal-translator ревью нейтральности.

### CPC и стоимость привлечения — BLOCKING (§9.3), НЕ спрашивать у агента
⚠️ Ни Claude Code, ни любой агент не имеет доступа к Google Keyword Planner /
Ahrefs / Semrush. Любая CPC-цифра от агента — галлюцинация. В
`gtm-unit-economics.md` числа CPC не вставлять; таблицы CAC там — иллюстрация
формулы, не измерение.

```
FACT: CPC "divorce California" и стоимость привлечения
claim:                    [заполнить после Keyword Planner]
why_it_matters:           Определяет, жизнеспособен ли платный канал вообще.
                          При CPC $15 и конв. 2% CAC = $750 > чек $299 →
                          платный трафик убыточен, GTM только SEO + сарафан.
source:                   Google Keyword Planner (ads.google.com), локация California
verified_against_primary: no
verification_method:      [ручная проверка Евгением]
blocks:                   финализацию gtm-unit-economics.md и любые решения
                          о платной рекламе
```

```
FACT: объёмы поиска по русским и испанским divorce-запросам (языковой wedge)
claim:                    [заполнить после Keyword Planner]
why_it_matters:           Проверяет центральную гипотезу языкового wedge
                          (дешёвый трафик на ES/RU vs дорогой EN Google).
source:                   Google Keyword Planner (ads.google.com), локация California
verified_against_primary: no
verification_method:      [ручная проверка Евгением]
blocks:                   финализацию gtm-unit-economics.md и приоритет локалей
```

**gtm-unit-economics.md не финализируется**, пока оба факта выше не получат
статус, отличный от `no`.
