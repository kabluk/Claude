# Каталог агентств аудита доступности — состояние проекта

> **Точка входа для новой сессии.** Здесь: как продолжить, что сделано, что НЕ сделано, где лежит каждый файл и какие команды запускать.

**Последнее обновление:** 2026-08-05
**Ветка:** `claude/niche-finder-directory-seo-vj520j` (всё запушено, незакоммиченного нет)

---

## 0. Как начать новую сессию

Скажите Claude ровно это:

> Прочитай research/STATE.md и продолжи работу над каталогом агентств доступности.

Дальше проверьте, что среда жива:

```bash
npm install                    # зависимости (node_modules не в git)
node scripts/build-a11y.mjs    # валидация данных → должно быть 254 агентства
npm run build:a11y-site        # полная сборка сайта → 378 страниц
npm run dev:a11y-site          # посмотреть глазами на localhost
```

Если нужны внешние API — см. §6 «Инфраструктура и ключи».

---

## 1. Что это за проект

Мультиязычный каталог агентств аудита цифровой доступности (EAA / WCAG / ADA / BFSG / RGAA).
Ниша выбрана после исследования 150+ кандидатов, подтверждена живыми SERP-замерами и DataForSEO:
**спрос ≈45 000 запросов/мес по 8 рынкам**, ни одного каталога в топ-10 ни по одному ключу, CPC до $86.
Оценка ниши: **7.9/10, вердикт «строить»**. Полное обоснование: `docs/deepdive-accessibility-directory.md`.

Каталог — второй продукт в этом репозитории (первый — detnav, к нему отношения не имеет,
делит только `node_modules` и конфиг TypeScript).

---

## 2. Честный статус готовности

Проект **не готов к запуску**. Построено много, но три рубежа не пройдены.

### Рубеж 1 — «можно показать людям». НЕ пройден.
| Что | Статус |
|---|---|
| Домен | ❌ заглушка `a11y-directory.example` в 2 файлах (см. §5) |
| Деплой-конфиг для каталога | ❌ нет (wrangler/netlify настроены только под detnav) |
| About / Contact | ❌ нет — без них нет E-E-A-T, каталог не вызывает доверия |
| Privacy Policy / Impressum | ❌ нет — для рынков DE/EU это юридическое требование, не украшение |
| 404-страница | ❌ нет |
| Иерархия заголовков | ❌ баг: на страницах стран h1 → h3 без h2 (карточки агентств используют h3). Для каталога про доступность — стыдная ошибка |

### Рубеж 2 — «можно индексировать без вреда». НЕ пройден.
| Поле | Заполнено |
|---|---|
| Описание профиля | **81 / 254 (31%)** ← главная дыра |
| Город | 153 / 254 (60%) |
| Ценовой диапазон | 4 / 254 (1%) |
| Сертификация | 124 / 254 (48%) |
| ≥2 источника | 179 / 254 (70%) |

173 профиля — это название, домен и ссылка на декларацию. Порог ≥3 листинга защищает
списочные страницы от thin-content, но **не защищает профили**. Массовая индексация в таком
виде — риск.

### Рубеж 3 — «можно зарабатывать». НЕ начат.
Лид-форма, claim-поток, Stripe — **ничего не построено**. После деплоя выручка физически невозможна.

### Что действительно готово
- 254 агентства, 21 страна, схема валидируется, дубликатов нет
- 12 редакционных гайдов с фактчекингом по первоисточникам
- 378 статических страниц, JSON-LD (ProfessionalService / ItemList / Article / FAQPage / BreadcrumbList)
- Клиентские фильтры, работающие поверх SSG (без JS список виден целиком)
- noindex,follow ниже порога ≥3; sitemap 347 URL без noindex-страниц
- Воспроизводимый конвейер данных: сбор → слияние → обогащение → валидация → сборка

---

## 3. Карта файлов

### Данные каталога — `data/a11y/`
| Файл | Что это |
|---|---|
| `agencies.json` | **Источник истины.** 254 агентства. Всё остальное производно |
| `types.ts` | Схема (Agency, Guide, Taxonomies, все enum-ы) |
| `taxonomies.json` | Услуги, стандарты, ценовые диапазоны, отрасли, 20 стран с их законами и датами |
| `excluded.json` | «Надгробия» — осознанно удалённые записи. Защищает от воскрешения при слиянии |
| `README.md` | Правила наполнения данных и что считается доказательством |
| `guides/*.md` | 12 гайдов: JSON-frontmatter + markdown |
| `collect/*.json` | Сырьё сборщиков (10 файлов). Остаётся в репо как аудит-след |
| `enrich/*.json` | Патчи обогащения (4 файла) |
| `_generated/` | Индексы, фасеты, route-манифест. **В git нет**, пересобирается |

### Сайт каталога — `a11y-site/`
| Файл | Что это |
|---|---|
| `vite.config.ts` | Свой конфиг сборки (root = a11y-site, алиасы `@dir`, `@data`) |
| `src/routes.tsx` | Все маршруты + `getStaticPaths` для SSG |
| `src/lib/data.ts` | Слой данных: слаги стран, выборки, порог индексации, пути |
| `src/lib/seo.tsx` | **Здесь константа ORIGIN — менять при покупке домена** |
| `src/lib/guides.ts` | Загрузчик гайдов (битый frontmatter валит сборку) |
| `src/components/Layout.tsx` | Шапка, крошки, подвал, мета |
| `src/components/FilterableList.tsx` | Клиентские фильтры |
| `src/components/AgencyCard.tsx` | Карточка агентства (**здесь h3 — источник бага иерархии**) |
| `src/pages/*.tsx` | 9 шаблонов: профиль, страна, комбо, услуга, стандарт, гайд, ярусы, главная |

### Скрипты — `scripts/`
| Скрипт | Что делает |
|---|---|
| `build-a11y.mjs` | Валидация + дедуп + индексы + route-манифест + отчёт полноты |
| `merge-a11y.mjs` | Сливает `collect/*.json` в `agencies.json`. Уважает `excluded.json` |
| `enrich-a11y.mjs` | Применяет `enrich/*.json`. Заполняет **только пустые** поля, ничего не перезаписывает |
| `gen-a11y-sitemap.mjs` | Sitemap + robots. **Здесь второй ORIGIN — менять вместе с seo.tsx** |
| `seo-data.mjs` | DataForSEO: объёмы запросов и SERP |

### Исследования — `research/` и `docs/`
| Файл | Что это |
|---|---|
| `docs/deepdive-accessibility-directory.md` | **Главный документ:** обоснование ниши, архитектура, схема, монетизация, 30-дневный план |
| `research/REPORT-01-discover.md` | 78 международных ниш → топ-10 |
| `research/REPORT-02/03-analyze-*.md` | Разборы GPSR и EPR (обе понижены после проверки выдачи) |
| `research/REPORT-04-blue-ocean.md` | 7 подтверждённых blue-ocean ниш, обоснование выбора |
| `research/dd-a11y-*.json` | Сырые замеры спроса по 8 рынкам (DataForSEO) |
| `research/dd-serp-*.json`, `bo-b-serp-*.json` | Снимки выдачи, доказывающие отсутствие каталогов |
| `.claude/agents/niche-finder.md` | Агент с методикой оценки ниш |
| `.claude/commands/*.md` | Слэш-команды `/discover /analyze /deepdive` и др. |

---

## 4. Команды

```bash
# Данные
node scripts/build-a11y.mjs              # валидация + генерация индексов
node scripts/build-a11y.mjs --strict     # + все замечания о неполноте (сейчас ~700)
node scripts/merge-a11y.mjs --dry        # предпросмотр слияния сборщиков
node scripts/merge-a11y.mjs              # слить
node scripts/enrich-a11y.mjs --dry       # предпросмотр обогащения
node scripts/enrich-a11y.mjs             # применить патчи

# Сайт
npm run dev:a11y-site                    # дев-сервер
npm run build:a11y-site                  # данные → 378 страниц → sitemap (в a11y-site/dist)
```

Порядок при добавлении данных: положить файл в `collect/` → `merge-a11y.mjs` → `build-a11y.mjs` → `build:a11y-site`.

---

## 5. Что делать дальше (по приоритету)

1. **Юридический минимум + фикс бага** (можно без домена):
   About, Contact, Privacy, Impressum (реквизиты — от владельца, выдумывать нельзя), 404,
   починить иерархию заголовков в `AgencyCard.tsx`.
2. **Описания для 173 профилей** — агенты обходят сайты и пишут по фактам. Без этого индексация вредна.
3. **Домен → деплой:** заменить `a11y-directory.example` в `a11y-site/src/lib/seo.tsx` и
   `scripts/gen-a11y-sitemap.mjs`, добавить конфиг Cloudflare Pages.
4. **Монетизация:** лид-форма (Worker + D1), claim-поток, Stripe Payment Links.
   У 99 агентств из деклараций есть готовый повод для письма: «вас назвали аудитором
   в декларации такого-то ведомства — заберите профиль».
5. **Мультиязычность интерфейса** (en/de/fr/pl/es + hreflang) — по плану была, отложена.
   Сейчас интерфейс только английский, мультиязычны лишь гайды.

---

## 6. Инфраструктура и ключи

- **DataForSEO** — логин `zincroom@gmail.com`, баланс ~$525. Пароль в переменных окружения
  (`DATAFORSEO_LOGIN`, `DATAFORSEO_PASSWORD`), **в репо не хранится**.
  Требует разрешённый домен `api.dataforseo.com` в network access.
  Проверка: `node scripts/seo-data.mjs balance`
- **Firecrawl** — ключ выдавался в чате, **в репо и в env не хранится**. При необходимости
  сообщить Claude заново. Расход на этап деклараций — ~2600 кредитов.
- **Открытый вопрос:** у DataForSEO запрошен возврат $475 излишнего депозита; после этого
  сбросить API-пароль (засветился в чате) и обновить переменную окружения.

---

## 7. Хронология

| Этап | Результат |
|---|---|
| Исследование ниш | 150+ кандидатов → 7 blue-ocean → выбран каталог доступности (7.9/10) |
| Дни 1–2 | Схема данных, таксономии, конвейер сборки |
| Дни 3–3.5 | 4 агента собрали 152 агентства (BITV, IAAP, конференции, UK, US, EU) |
| Дни 8–12 | MVP-сайт: 258 страниц, JSON-LD, фильтры, порог индексации |
| Дни 13–20 | 12 гайдов + инфраструктура гайдов |
| Дни 4–7 | Обогащение: +54 города, +111 описаний, +196 полей, +144 источника |
| Дни 24–27 | **+99 агентств из деклараций гос-сайтов ЕС** — защищаемый датасет |

Ключевые находки этапа деклараций: реестр NL не называет аудитора («независимая третья
сторона»), имена пришлось добывать из 1569 PDF-отчётов; польские декларации следуют жёсткому
шаблону и собираются регуляркой; Италия и Испания структурно бедны — их формы либо не называют
аудитора, либо фиксируют самооценку.

**Запасная ниша №2** (если эта не пойдёт): DGSA / опасные грузы ЕС — балл 8.1, но трафик дешевле.
